# 工地台账 · 开发规范

> 适用：本仓库的功能、修复、重构、测试、文档和交付。整理日期：2026-09-18。
> 软件版本以 `VERSION.txt` 第一行为准，规范整理日期不是软件版本。

## 0. 使用约定

**目标：数据准确、操作可恢复、权限边界清楚、改动可验证。** 沿用现有 TanStack Start / React / TypeScript / Zustand 技术栈，优先满足需求的简单实现。

- **必须/禁止**是交付要求；**建议**可根据实际收益调整并说明理由。不要为普通实现细节不断新增硬规则。
- 用户在当前任务中的明确决策优先；涉及账务口径、存储迁移、权限开放范围的变化，必须记录决策并更新相关规则。
- 本文件是开发规则的唯一正文；`AGENTS.md` 是协作摘要；`docs/README.md` 是报告索引和待办入口。历史报告不覆盖现行规则。
- 代码和测试用于核实实际行为，不能以“测试是绿的”证明错误口径正确。规则、代码、测试矛盾时先复现并确认目标，再一起修正。
- 新强制规则须写清保护目标、适用范围和验证方式。历史原文见 [整理前快照](docs/审查与报告/规范历史-20260918.md)。
- 保留 §1–§12 编号，兼容既有引用。版本/交付看 §1–2，数据/金额看 §3–4，API 看 §5，UI/打印看 §6，数据安全看 §7，编码看 §8，完成定义看 §9，测试看 §10，影像/审计看 §11，架构看 §12。

专家协作按 [专家职责](docs/开发协作/专家职责.md) 执行：按任务选择能力、明确文件所有权、在授权允许时安排独立复核，主负责人统一验收。小改不强制启动多人。

## 1. 版本号规范

1. 沿用已决策的 `X.Y.Z`：`X ≥ 0`、`0 ≤ Y ≤ 9`、`0 ≤ Z ≤ 19`。普通优化不更换版本策略。
2. `Z < 19` 时加一；`Z = 19` 时 `Y+1, Z=0`；`Y=9, Z=19` 时 `X+1, Y=0, Z=0`。例如 `1.8.19 → 1.9.0`，不生成 `1.8.20`。
3. `VERSION.txt` 第一行为唯一版本来源。正式发版同步 `package.json`、`README.md` 当前版本行；`app/VERSION.txt` 由构建复制，不手改。
4. 正式发布必须升版并写记录；开发过程、纯文档和测试改动不要求逐次升版。交付明确区分本地修改与线上发布。
5. 更新记录最新在上，写用户可感知变化、兼容影响和真实验证结果：

```text
X.Y.Z

[X.Y.Z] YYYY-MM-DD
- 变更说明
```

验证：`tests/version-consistency.test.ts`、`tests/changelog.test.ts`。

## 2. 工作流程、依赖与交付

### 2.1 开始和实施

- 先检查 git 状态、本规范、文档索引和相关报告，保留用户已有修改，不做全仓还原或清理。
- 明确预期结果、涉及模块和验收方式；已授权的可逆操作直接完成，关键业务取舍集中确认。
- 数据/权限缺陷先补复现测试，再修代码；重构、功能、依赖升级尽量分开，便于评审和回退。
- 只改任务相关文件；生成文件经生成器刷新，不手改 `app/` 和 `src/routeTree.gen.ts`。
- 未授权不得推送、部署、发 Release、删除生产资料或迁移生产数据；完成本地构建不等于发布。

### 2.2 依赖与 lockfile

- 依赖只写精确版本，`pnpm-lock.yaml` 必须入库。禁止 `latest`、`^`、`~`、`*`、范围；别名须精确，tarball URL 自带版本号。
- 初次安装和 CI 使用 `pnpm install --frozen-lockfile`。Node 最低版本以 `package.json.engines` 为准；开发与 CI 尽量使用相同 Node / pnpm 主版本。
- 升级：改 `package.json` → `pnpm install` 生成 lockfile → §10 验证 → `pnpm run build` → `node scripts/build-stamp.mjs --verify` → 两份依赖文件与更新后的 `app/` 一起交付。
- 精确版本不会自动取得安全补丁，必要升级须显式执行。不以“少依赖”为由拒绝安全修复；新增依赖说明用途、收益和环境兼容，不顺带升级全套依赖。
- 可选浏览器等工具通过环境变量指定，缺失时明确报错，不把本机绝对路径作为唯一入口，也不把未运行算通过。

验证：`tests/dependency-pinning.test.ts`、`tests/build-stamp.test.ts`。

### 2.3 构建、CI 与发布

- 源码交付通过类型/测试/构建三道闸；完整命令 `pnpm run check` 还包含 Excel 往返对拍。分级要求见 §10。
- 当前交付包含入库的 `app/`；构建输入改变必须重建并通过源码指纹校验。不把跨平台 bundle 字节完全相同作为硬闸门。
- `.github/workflows/check.yml` / `docker.yml` 是生效配置，`ci/check.workflow.yml` / `docker.workflow.yml` 是维护模板；关键行为改变时两份同一提交同步。
- 允许本地修改 workflow，推送以实际 `workflow` 权限为准；不足时说明失败并提供网页更新路径。不沿用永久禁止本地修改的历史规定，也不假定凭据永久有效。
- 当前触发：推 `main` 构建镜像（`latest`、`sha-…`）；`v*` tag 或手动触发才打 Windows 包与 Release。细节以生效 workflow 和 `ci/README.md` 为准。
- 发版检查 tag 与 VERSION 一致、附件名与 `win/pack.sh` 的 `OUT=` 一致；发布后核验镜像和非空附件确实存在，不能只看任务成功。
- 一键更新保留 SHA 优先、镜像 ID/版本检查、脚本语法检查、失败日志与回滚；新容器确认就绪后才清旧容器/镜像。
- 提交使用 `feat:` / `fix:` / `refactor:` / `style:` / `docs:` / `release:`，说明问题、结果、验证。

### 2.4 文档

- 根目录保留四个入口：README、VERSION、开发规范、AGENTS。报告放 `docs/审查与报告/`，使用文档放 `docs/使用与部署/`。
- 新报告登记到 `docs/README.md`；当前待办只在索引维护，其他文件链接过去，不复制多份清单。
- 处理报告须逐条核验，在原报告末尾追加“处理状态”：已修、无需改、待决策、未验证及原因。历史判断有变化时追加更正。
- 测试计数只在有日期的验证报告记录，不在规范和 AGENTS 重复维护“当前基线”。

## 3. 数据模型、解析与身份

### 3.1 四层一致

变更字段必须检查 `types.ts` → `store.ts`（默认值/迁移/动作/持久化选择）→ `nas-sync.ts`（快照/拉取）→ `excel/` 与 `excel-import.tsx`。不相关层可不改，说明理由。还须检查 `ledger-schema.server.ts` 的运行时校验与 `backup-book.ts/restore.ts` 的备份恢复；当前 schema 主要校验结构，不能宣称覆盖所有业务不变量。

- 数组默认 `[]`、业务字符串默认空串，可选字段按模型处理；不能把缺失、空白与真实 0 强行混同。
- 外部数值用 `num.ts` 的 `numOr/numOrWarn`；Excel 保留“缺列”和“明确 0”的差别。
- 表单数字转换后，保存前检查有限性、范围、必填；禁止非法金额、NaN、Infinity 被默默当 0 写盘。
- 日期走 `dates.ts`，不能只凭 `new Date(字符串)` 可解析判断合法。按真实年月生成日选项，拒绝日期溢出。
- 迁移函数幂等并保留旧数据；磁盘格式变化执行 §7 专项流程。

### 3.2 ID 与历史姓名关联

- 新实体 ID 用 `uid()`；同实体按 ID 增删改，不用姓名覆盖另一条人员记录。
- **现有考勤、发放及部分保险/报销仍按姓名关联**：必须用 `nameKey/ownerKey/receiverOf`，不能把“已全部按 ID”写成现状。
- 改名走既有事务，先校验重名，再同步关联。遇到歧义不自动合并、不猜测归属。
- 新关联优先稳定 ID；历史姓名迁成 ID 须有旧数据、导入兼容、回滚方案，不能只改单边。

### 3.3 Excel 契约

- 模板由代码实时生成，不在 public/app 放静态副本。
- 字段变化同步模板表头、示例、填写说明、解析和导出；必填列缺失明确提示。
- 导出→导入语义不变；去重键日期规范化、收款人走统一函数。历史疑似重复只提示，不自动删。
- 整本备份与普通导出范围按 `backup-book.ts/restore.ts` 的实际契约描述，不宣称 Excel 包含账号、影像和审计。

## 4. 账务与统计口径

| 内容 | 唯一实现与约定 |
|---|---|
| 金额 | `wage.ts` 的 `round2()`；入库和对外合计取整，不复制公式或裸浮点展示。 |
| 工资/加班/餐补 | `getWageAt/monthPay/overtimePay`；历史按月末匹配，批量调薪默认今天生效并保留此前基准。 |
| 年度汇总 | `attendance-summary.ts`；总览、考勤和批量待发复用，不在页面重算。 |
| 已发/代发 | `isPaid`：有日期即已发，按实际收款人 owner 归集、含代发；代发是已发子集。`isPaidSelf` 用于工资条本人视角及代发子集判定，不能作为全局已发条件。 |
| 待发 | 无日期记录按当前工作年单列，不计已发；已发+待发=全部。批量生成用 `pending-batch.ts`，已有待发幂等跳过。 |
| 合同 | `contracts.ts/contracts-totals.ts`；录入金额与含税/不含税区分，派生表不能再次导入为明细。 |
| 保险 | `insurance.ts` 的 `memberDays/isActive/prevDayEnd`；天数夹在保单期内，旧人截至替换日前一天23:59，组合险说明与同步复用。 |
| 出勤/分组 | `work.ts` 区分工作量与纯备注；`buckets.ts` 保留空值桶，`ALL_BUCKETS` 表示全部。 |

列表、筛选、KPI、打印和导出须可核对；有意不同的范围写明标签和测试。性能优化保留重复行、顺序、舍入时机、跨年等语义，用结果对拍证明。

## 5. API、权限与认证

### 5.1 合规入口

- 台账业务用 `withTenant(request, fn, need)`，在解析后的台账上下文执行。
- 上传依赖 body 决定模块时：`gateTenant` 验身份/台账 → 限大小与解析 → `needDenied` 判具体权限 → `runInTenant` 执行。
- 账号册、镜像、软件更新等台账外操作：`resolveTenant(request)` + 适用的角色/归属/同源校验。首次初始化与登录允许匿名，走现有 setup/login 独立门禁。
- 服务端是权限边界；前端 Can/Need/blockedWrite 只管入口和反馈，不能代替服务端授权。
- 全量 GET 至少 `people.view`，PUT 需 `ledger.manage`；保留已决策的整本编辑机制，实体级授权另排专项。
- 管理成员须校验目标台账，不得自我提权、越权授予；敏感导出按 `perms.ts` 和接口权限组合检查。

### 5.2 输入与响应

- 需要认证的写入口先鉴权再读 body；JSON/formData 解析失败返回可理解的 400，不变 500。
- 同时检查声明与实际大小，不能信任 Content-Length；启动器和路由保留双层上限，压缩台账限制解压后体积。
- 空文件、非法 ID、超长文件名、错误金额/日期在写盘前拒绝；失败不覆盖原文件，成功必须对应真实落盘。
- 敏感读取 `Cache-Control: no-store`；文档仅已知图片/PDF inline，XML 等 attachment，并加 `X-Content-Type-Options: nosniff`。前端预览同样按响应 MIME 判定，PDF 必须明确为 application/pdf，不凭显示名创建 iframe。
- 台账 ID 拒绝点目录、父目录、控制字符；路径走既有函数，静态服务用 resolve+relative 验包含关系。
- 对外错误不泄漏凭据/堆栈；细节进服务端日志。接口 kind、错误码、权限名的两端契约需测试。

### 5.3 账号与会话

新密码≥8位，统一 trim，scrypt+随机盐；旧哈希兼容验证成功后升级。保留已有登录限速、随机 tokenSalt、改密失效旧会话、HttpOnly cookie；不同认证入口的限速覆盖情况须分别核实。

Secure/HSTS/CSP、登出撤销、IP 限速等未闭环能力不能写成已实现，见文档索引安全专项；改动兼顾 HTTP 内网和反代。不得记录明文密码/token/完整身份证和银行卡。

验证：`tests/api-input-guards.test.ts`、`tests/auth-before-body.test.ts`、`tests/tenant-book-scope.test.ts`、`tests/perms.test.ts`。

## 6. 页面、交互与打印

### 6.1 页面骨架

列表页保持：标题/说明 → 操作按钮 → 筛选 → 范围/合计 → 表格 → 编辑弹窗 → 独立打印件。总览、照片、设置、查询可采用适合内容的布局，避免机械套模板。

### 6.2 表格

一般列表复用 `WideTable/usePager`；少量数据、矩阵或图片网格可例外。筛选变化重置页码，尾页删除后页码合法；批量选择明确当前页还是全部筛选结果。

宽表在自身容器滚动；屏幕表格贴合内容、打印可铺满。滚动区可键盘聚焦并有焦点样式。拖动在松手、失焦和卸载时清理；存储偏好失败不影响本次操作。空表、加载失败、无权限分别显示。

### 6.3 弹窗

编辑器采用本地副本，随目标记录 ID 重置；编辑 A 后新增不得沿用 A 的 ID。复用 `useGuardedClose`，保存后 resetDirty；未保存修改在关闭/导航/刷新时拦截，嵌套预览只处理最内层 Esc。

手机使用 dvh 限高和内部滚动，避免裸 max-h-screen；按钮可到达。弹窗有可访问名称，打开进入焦点、关闭恢复焦点，验证 Tab/Esc；iframe 内按键须单独实测，不能用图片预览测试冒充 PDF 验证。

### 6.4 操作和反馈

复用 Button 与语义 token，下载/导航可用链接。只读入口隐藏或禁用并说明原因，落盘前调用 `blockedWrite/blockedImport`。

整本导入、批量改动、删除先展示数量/范围/后果；上传会顺带保存表单时明确确认。普通低风险保存不机械增加重复确认。

异步写入检查响应，防止重复提交；区分“本机已修改、正在保存、服务器已保存、保存失败”，更新 store 不等于服务端已保存。失败保留输入，给出重试途径。

### 6.5 筛选

搜索和下拉给可访问名称；日期/年份/状态统一作用于列表与对应统计。分组用 buckets，空字符串可代表未填，不能同时代表全部。导出/打印范围不同须在入口说明。

### 6.6 打印协议

唯一 CSS 来源是 `src/styles.css` 的“打印分页协议”，先读再改：

1. 屏幕在 no-print，打印件 print-only 放外侧；打印件不复制多份，复用同一计算结果。
2. A4 竖版、12mm 边距；真实内容宽约 703px/186mm。分页必须用真 PDF 量，不以 794px 屏幕 DOM 推断。
3. thead 跨页重复，单据抬头进 thead；tfoot 使用 table-row-group，全单合计只印一次。
4. 禁止给整页/大表任意加 break-inside-avoid。允许 `tr`（行）、`.print-keep`（小单元）、`.print-doc`（人/单据）、`.payslip`（工资条）的既有语义。
5. print-doc 能放下就与前项共页，放不下换页，超过整页允许续排；不使用 break-before: page 强制每条新页。
6. 打印态清除一屏高与滚动裁切，行高/内边距/尾部说明按集中 CSS；无待发不印对应提示。
7. 验证少量、临界一页、跨页、长姓名/备注；核对抬头、合计次数、内容完整和留白。使用 `ci/mobile-print-check.mjs print/pages` 与 `ci/print-pdf.mjs`。

### 6.7 权限

新页同步路由、shell/nav 的 NAV、perms 的 NAV_PERM/PERM_GROUPS。整页 Need，操作 Can/useCanSave；总览快捷入口同样遵循权限。

### 6.8 文案

简洁中文，空状态说明下一步，删除说明关联资料哪些删/哪些留。金额、人次、天数、时间范围带单位，不用含糊的“全部”“完成”掩盖范围。

### 6.9 桌面和手机

页面变动至少测 1440px 桌面与 375px 手机，布局变动加 390px；重点看宽表、长文本、弹窗、底部导航和输入后的按钮。

首屏临时被固定底栏覆盖的内容，要验证滚动后可操作；不能把瞬时遮挡全当缺陷，也不能只凭“能滚动”免测。

### 6.10 主题

保留 classic/v2/apple/movie；正文、表单、状态走 @theme 语义 token，少量装饰渐变可集中固定。主题同步 html，登录/门禁跟随；不全局用 !important 截断主题。

### 6.11 批量操作

先确认数量与影响；幂等操作重复执行不重复记账。工资历史、跨年导入、恢复明确覆盖/合并语义。不同入口复用同一 store/业务函数。

## 7. 数据安全、同步与恢复

### 7.1 生产数据和磁盘

- `data/` 不用于测试；接口/浏览器试验指定独立临时 DATA_DIR。生产资料不进入日志、截图、测试夹具和仓库。
- 普通升级只替换程序。改磁盘格式须专项给出迁移、备份、校验和恢复方案；迁移幂等、版本单调、兼容旧数据。能无损逆迁移则提供，否则明确通过备份+旧应用回滚，不承诺虚假“可逆”。
- 台账/账户/审计/上传先临时文件再 rename。**原子替换不等于并发事务**：台账 CAS 检查与替换同一串行区间，账户/审计读改写也串行；多文件设计顺序和补偿。
- 当前队列只在进程内，要求单写进程；多实例共享数据目录必须另做跨进程锁或事务存储，不能视为现有能力。
- 新文件就位→更新指针→清旧，共享文件仍被引用不删。目录初始化并发共享任务，成功后缓存，向外抛出的失败可重试；缓存键含数据根、台账和影像根。
- 坏台账/账户拒绝覆盖，给恢复入口。删实体处理既有级联，不擅自扩大范围。

### 7.2 同步不变量

1. 自动保存只经 `pushNasLedger()` 串行队列，不直接并发 PUT。
2. GET 版本头与 PUT CAS 都用 `ledgerRevisionValue()`；不存在台账用空串。
3. 拉取期间禁止普通推送，迟到响应不能覆盖新册，dirty 时不静默覆盖本机。
4. 切换/新建/删除前 flush，失败保留原册；空册重置 emptyState，不灌入旧册数据。
5. seed:true 只给开机/登录后首次升级旧数据，切换/新建禁止 seed。先完成 hydration、身份/权限与缓存归属检查，再开放同步，由唯一入口决定 seed；安装订阅本身不得拉取或推送。归属改变先清旧缓存；暂停/退出作废旧会话在途响应。
6. 换号/册、退出、401/403 清本机台账缓存并作废在途请求；服务器影像失败不回落旧 IndexedDB。未知或探测失败不能当成确认的本地模式。
7. 现有整本 CAS 能检测冲突，不是实时多人合并。409 由用户明确处理，不声称多端编辑无冲突。

### 7.3 备份恢复

Excel 合并导入式恢复按 `restoreGate` 的 import.use + canManageLedger 权限；不能笼统改成“恢复一律仅管理员”。清空/完全替换/运维目录恢复另按高危入口授权。

恢复前核对备份范围/条数/年份并保存当前可恢复副本，恢复后对实体和金额；Excel 未包含的影像/账号/审计随 data 备份。保留策略只删除本程序符合命名规则的备份。

验证：pull-generation、ledger-server、backup-book、restore-book、rename-person 等测试。

## 8. 编码、错误和性能

- 外部输入用 unknown 并校验；新代码避免 any，遗留库边界可局部适配说明。禁止为压下报错滥用断言。
- 小函数、清晰职责、复用既有模式；清死代码。注释解释原因和约束，事实变化时同步。
- 关键写入失败必须可见，服务端 logServer，客户端保留输入并提示；偏好缓存/尽力清理等可选步骤允许有理由的降级，不吞业务失败。
- 日志统一级别/保留/滚动/慢请求口径，不另造实现；日志不代替审计，不记录凭据或完整敏感值。
- 性能优化先量测，记录合成数据规模、环境、指标与前后结果；不把单函数加速宣称为整站加速。
- 大数组按需要建 Map/Set；useMemo 只用于有成本、依赖完整的纯计算，正确性不得依赖缓存。
- 缓存有界，考虑台账/权限/失效/失败重试；事件、定时器、object URL、数据库连接按生命周期释放。

## 9. 完成定义

按实际改动勾选，不涉及写明原因，不伪造全量验证：

- [ ] 满足任务，业务决策有记录；未改生产 data、未覆盖他人修改。
- [ ] 模型、默认值、服务端校验、同步、模板和备份一致；金额/日期/姓名用统一函数。
- [ ] 服务端权限有效，界面入口与反馈一致；失败不丢输入/原数据，敏感值不入日志。
- [ ] 并发、原子写、同步和恢复约束未回退；新行为有相应验证。
- [ ] 源码三道闸通过，相关专项已测；app 重建且指纹匹配。
- [ ] 规范/报告状态/索引/待办更新；正式发版四处版本一致。
- [ ] 交付说明结果、文件、运行方式、实测和未验证项；没部署就不写已上线。

## 10. 测试与质量闸门

用例数以 `pnpm test` 输出的 tests/pass/fail/todo 为准，不在本规范重复历史计数。

### 10.1 测试分层

- **纯函数**：Node 内置测试器，tests 下 .test.ts，测边界与业务不变量。
- **服务端行为**：临时目录与隔离 DATA_DIR，测真实 handler、权限、磁盘副作用、并发/故障；不连接生产。
- **浏览器**：DOM/焦点/路由/IndexedDB/下载/打印用真实浏览器，脚本在 ci；Node 可 mock fetch 测无 DOM 的分支，不伪造完整浏览器。
- **源码守卫**：防约定回退，不代替行为测试。正向 expectMinHits，负向 expectRegexCatches 坏样本自检，清单防漏项。
- tests/register.mjs 处理相对导入扩展名，TS 测试纳入 typecheck；不无故引入第二个测试框架。
- todo 必须有原因和追踪项，修好后显式移除 todo 并通过断言；Node 不会自动替开发者改标记。
- 不为低风险文案或实现细节写镜像测试；不删断言、降低阈值掩盖失败。需求变化时同步测试并记录理由。

### 10.2 验证矩阵

| 改动 | 交付要求 |
|---|---|
| 源码/依赖/配置 | typecheck、test、build、源码指纹。 |
| Excel/模型/共享金额日期/恢复 | 上述 + test:roundtrip，核对条数/金额/年份/幂等。 |
| 页面交互 | 源码闸门 + 主要流程 + 桌面/手机，不能以编译代替点按。 |
| 打印 | 页面验证 + 真 PDF，检查跨页抬头/合计/内容/留白。 |
| API/存储/权限 | 合法与非法输入、未登录/无权、失败不写盘；修并发就补并发复现。 |
| 纯文档 | 相关文档守卫、链接/命令/事实核对，不要求无意义重建 app。 |
| 正式发布/较大重构 | pnpm run check + 源码指纹 + 相关浏览器/部署验证；缺环境明确列出。 |

```sh
pnpm run check
node scripts/build-stamp.mjs --verify
```

check 包含 typecheck → test → test:roundtrip → build，不含真浏览器或部署验证。CI 在重建前检查仓库 app 指纹；本机重建后须随源码交付 app。发布必须以**同一提交的质量闸门通过**为前提，不以 build 单独代替测试。

## 11. 影像隔离与审计

### 11.1 影像

新影像只写 `<PHOTO_DIR 或 data/photos>/<bookId>/<分类>/`；旧公共目录仅按兼容开关读，不写回。“归入本台账”只复制符合当前记录的文件，不删/不覆盖；完成后可 PHOTO_LEGACY_FALLBACK=off。

服务器模式只信服务器结果，403/404/断网不读无身份维度的旧缓存；纯本地模式保留 IndexedDB，不能擅删用户本地资料。照片仍按姓名文件名匹配，重名风险未消除，迁成 ID 命名须专项。删除只动自己的目录。

### 11.2 审计

增删改/导入/批量/上传/导出按现有 logOp/appendAudit 留痕；关键删除服务端记录。前后值用 audit-diff，金额 round2，日期规范化，身份证/银行卡只记填写状态。

appendAudit 读改写串行、随机临时名原子落盘，损坏历史拒绝覆盖。客户端审计失败首次提示+告警，服务端日志留痕；保留截断与管理员删除同样可追溯。

### 11.3 权限单一来源

PERM_GROUPS 派生 PermId/NeedId/ALL_PERMS，NAV_PERM/PRESETS/接口按它协作。权限先声明再使用。预设“合同财务”与全量读取的缺口仍在待办，不能擅自放开身份证/银行卡权限。

## 12. 模块化、可拓展与低耦合

### 12.1 模块化

模块按职责划分；路由保留装配和骨架，复杂编辑器与业务计算下沉。**约1000行是评估提醒，不是强制拆分线**，职责与可测试性更重要；允许内聚的辅助组件同文件。

纯重构默认机械提取+兼容出口+行为不变；确需行为变化单独说明和验证，不藏在纯重构里。

### 12.2 可拓展

| 模块 | 落位/方向 |
|---|---|
| 存储 | paths.server ← assets.server ← nas-fs.server；影像所需台账由调用方传入。 |
| Excel | common ← 实体 ← full；excel.ts 转发，实体不反向依赖 full。 |
| 外壳/设置 | components/shell、components/settings；导航独立，不反向依赖路由。 |
| 工资/日期 | work 是共同依赖，dates/wage 不互引。 |
| 更新 | lib/update，经 update.server 导出。 |
| 共享小逻辑 | nas-flag、receiver、num 等职责明确的模块，不把一切塞进 utils。 |

### 12.3 低耦合

- 禁新增循环依赖；共同依赖下沉；types.ts 保持叶子，lib 不依赖 routes/components。
- 既有唯一互引例外 `update/docker ↔ update/log`：只在运行期互调、加载期无副作用；注释写明“互引例外”“§12.3”与原因，守卫列白名单。新例外单独评审。
- 拆分同步守卫路径和扫描清单，guards-paths/structure-guards 验证路径、环和方向。
- 架构提案说明问题、目标、备选、成本、验证、迁移和回滚；不因技术更新就引数据库、微服务或新状态框架。
