# 项目审查修复说明

日期：2026-09-10

## 修复内容

### 1. 自动保存请求串行化

`src/lib/nas-sync.ts` 原先在防抖后直接发起整本台账 PUT。网络较慢时，旧请求可能晚于新请求完成，造成新数据被旧快照覆盖。现在所有自动保存请求进入串行队列，前一个请求结束后才发送下一个请求。

### 2. 文件写入改为原子替换

台账自动修复、照片上传和文档上传现在先写入带随机后缀的临时文件，再用 `rename` 替换目标文件。进程异常时不会留下半写入的正式文件。

文档上传会先完成新文件写入，再清理旧文件；写入失败时旧文件仍可用。

## 尚未消除的架构取舍

多设备同时编辑仍然属于整本快照同步，串行队列只能保证单个客户端的请求顺序，不能解决多个浏览器之间的冲突。多人使用时仍应避免同时编辑；如果以后需要完整解决，应增加服务端版本号或 ETag，并在版本不一致时拒绝覆盖。

Docker Compose 为网页一键更新挂载 Docker Socket。该权限无法通过应用层完全收窄，属于部署级风险。不需要一键更新时应删除该挂载，并采用手工拉取镜像的方式升级。

## 验证

```text
pnpm exec tsc --noEmit
pnpm run build
git diff --check
```

以上检查应全部通过。构建会同步更新 `app/` 生产产物；提交前确认产物与源码一起提交或按仓库现有跟踪策略处理。
