# 架构审核报告 · 2026-09-10（只读）

> **范围**：`src/` 全部 17,885 行（不含 `routeTree.gen.ts`）+ 部署形态（Dockerfile / compose / 脚本 / CI）+ 数据目录模型 + 权限模型。
> **基线**：提交 `23e157f`（1.7.1）。**本次审核未修改任何源码。**
> **方法**：静态精读 + 用 node 直调生产函数实测（`/tmp/fixtest`）+ 合成台账量级测算。凡未亲自复核的结论都标注了来源。
> **证据标注**：✅ = 本次亲自读码/实测复核；📋 = 上一轮三个并行审查报告发现，尚未逐条复核。

---

## 0. 结论先行

**需要架构级处理的 6 项**，按"不修会怎样"排序：

| # | 方向 | 性质 | 严重度 | 修复性质 |
|---|---|---|---|---|
| **A** | 多租户边界：数字数据按台账隔离，**二进制资产没有** | 安全 + 数据丢失 | 🔴 | 需改 `data/` 结构 + 迁移 |
| **B** | 持久化模型：整本 JSON 快照 + 无 schema 校验 + 无数据版本 | 可扩展性 + 静默丢数 | 🔴 | 分级改造，可小切口起步 |
| **F** | 回归防线：**0 个测试**、CI 无类型检查 | 工程能力 | 🔴（但最便宜） | 加测试即可，不动架构 |
| **C** | 权限三处手工对齐，没有单一来源 | 越权 + 假成功 | 🟡 | 收敛为声明式权限表 |
| **D** | 失败处理与可观测性：34 处静默 catch、0 日志 | 不可查 | 🟡 | 加日志/错误面 |
| **E** | 交付形态：构建产物入库、push=发版、root+docker.sock | 工程卫生 + 部署风险 | 🟡 | 可并入任意发版 |

**不建议立项的**：UI/交互层重构、计算模块重写、微服务化、换数据库。理由见第 7 节。

---

## 1. A · 多租户边界：数据按台账隔离，二进制资产没有 🔴

### 现状与证据 ✅

台账的**数字数据**按台账隔离得很干净：

```
data/books/{bookId}/ledger.json      ← 人员/考勤/发放/合同/报销/保险
```

但**二进制资产完全不隔离**，落在全局目录：

| 位置 | 代码 | 事实 |
|---|---|---|
| 照片根目录 | `nas-fs.server.ts` `photosBase()` | `PHOTO_DIR` 环境变量优先；compose 里是 `/data/photos`，**不含台账段** |
| 证件照目录 | `nas-fs.server.ts` `kindDir()` | `PHOTO_ID_DIR` / `PHOTO_BANK_DIR` / `PHOTO_IC_DIR`，compose 里是 `/data/photos/id` 等 |
| 文档目录 | `nas-fs.server.ts` `docsDir()` | `photosBase() + DOC_CN[kind]`，例如 `/data/photos/合同扫描件`——同样无台账段 |
| 检索顺序 | `nas-fs.server.ts` `photoSearchDirs()` | 第一个加入的是 `kindDir(kind)`，**即全局目录；它排在所有 `books/{id}/photos/...` 之前** |
| 命中规则 | `nas-fs.server.ts` `findPhotoHit()` | 遍历候选目录，**第一个有命中的目录直接返回**，从不校验归属 |
| 文档命中 | `nas-fs.server.ts` `findDoc()` | 只按 `id` 匹配，同样不校验归属 |
| 写入副作用 | `nas-fs.server.ts` `savePhoto()` / `saveDoc()` | 命中旧文件先 `rm` 再写；`removePhoto()` 删掉所有命中文件 |
| 删除台账 | `nas-fs.server.ts` `removeBookDir()` | 只 `rm -rf books/{id}`，**影像全部残留** |

### 影响（三条独立后果）

1. **跨台账越权读取**：台账 B 的成员（只要 `people.view` / `contracts.view`）用 `GET /api/photo-file?name=张三&kind=id` 就能拿到台账 A 的身份证照；`GET /api/doc?id=<A的合同id>&kind=contract` 同理。身份证、银行卡、合同扫描件都属于敏感资产。
2. **跨台账破坏**：B 上传同名照片/文档会 `rm` 掉 A 的文件；`removePhoto` 会把所有同名文件删干净。
3. **删除不彻底**：删掉一本台账后，它的身份证照、合同扫描件仍留在共享目录里，被其它台账继续匹配到——数据"看起来删了其实还在"。

### 为什么不是改一行

因为**资产里没有任何归属信息**：文件名是 `张三-身份证-正面.jpg`、`某某住宅-合同电子版.pdf`，只含人名/项目名，不含 `bookId`。同一份"张三"可以同时属于多个台账。所以历史资产**无法自动判定属于哪本台账**。

### 迁移路径（三选一，推荐 3）

| 方案 | 做法 | 优点 | 代价/风险 |
|---|---|---|---|
| 1. 全量复制到每本台账 | 把现有全局资产复制进每个 `books/{id}/photos/` | 每本台账立刻自洽 | 泄露面没缩小（A 的证件照进了 B 的目录）、存储翻倍、语义错误 |
| 2. 全部归给 `default` 台账 | 现有资产搬进 `books/default/photos/` | 简单、可回滚 | 其它台账"照片突然全没了"，需要人工重新挂 |
| 3. **保留全局目录为只读遗留层 + 新写入按台账 + 逐本迁移**（推荐） | ① 读写路径一律先查 `books/{id}/photos/{kind}`；② 找不到时**只读**回落全局目录（兼容历史数据，不产生新写入）；③ 各台账在「照片管理」里提供"把历史影像归入本台账"的一次性动作；④ 迁移完成后删掉回落逻辑 | 零数据丢失、可灰度、可回滚（回落逻辑留着即可退回） | 需要改 `nas-fs.server.ts` 全部路径函数 + 一次 UI 动作 + 一轮回归 |

**回滚**：方案 3 的每一步都可停——回落读取保留期间，新旧两种布局都能工作。

### 成本

`nas-fs.server.ts` 的路径层（`photosBase/kindDir/docsDir/photoSearchDirs/docSearchDirs/findPhotoHit/findDoc/savePhoto/saveDoc/removePhoto/removeDocFile/removeBookDir`）全部要加台账段；`/api/photo-file`、`/api/doc`、`/api/photo-scan`、`/api/photo-flags` 要带上并校验 `bookId`。属于**中等改造**，不是小修。

---

## 2. B · 持久化与一致性模型 🔴

### 现状与证据 ✅

| 事实 | 位置 | 说明 |
|---|---|---|
| 一本台账 = 一个 JSON 文件 | `nas-fs.server.ts` `ledgerPath()` | `books/{id}/ledger.json` |
| 每次状态变化整本 PUT | `nas-sync.ts` `sliceState()` + `pushNasLedgerNow()` | 改一个电话号码，也会把整本台账序列化后提交 |
| 写盘用 `indent 2` | `nas-fs.server.ts` `writeLedgerNow()` | 体积比紧凑格式大约 1.6 倍 |
| 每次 PUT/GET 都整本哈希 | `nas-fs.server.ts` `ledgerRevisionValue()` | CAS 版本号 = `sha256(整个 blob)` |
| **读路径会写盘** | `nas-fs.server.ts` `readLedger()` → `reconcileContractScans()` → `atomicWriteFile()` | 读一次台账可能顺手改写整本，**且不在写队列、不比对版本** |
| 合并逻辑挂在读路径上 | `reconcileContractScans()` | 对每份缺 `scanFileName` 的合同，逐个候选目录 `readPointerName()`（最多 7 个目录）；本机磁盘无感，**NAS 网络盘是几十~几百次往返** |
| **PUT body 完全不校验** | `routes/api/ledger.ts` | `await request.json()` 之后直接 `writeLedger(body)`；服务端只查权限，不查结构、不查业务不变量（如出勤 ≤31 天） |
| 声明的校验依赖**从未使用** | `package.json` 有 `zod`，`src/` 里 **0 处 import** | 校验层是"打算做但没做" |
| **数据文件里没有版本号** | `types.ts` `LedgerState` 无 version 字段 | `store.ts` 的 `version: 10` + `migrate()` 只作用于浏览器 localStorage；服务器上的 blob 没有格式版本 |
| 队列只有一层且是进程内 | `nas-fs.server.ts` `writeLedger` 的 `ledgerWriteQueue` | 单容器单进程成立；一旦多副本/多实例立刻失效（当前部署是单实例，可接受） |

### 量级实测 ✅

本机 `data/` 是空的，无法量生产数据，所以按"中等工地"合成后实测（脚本 `/tmp/fixtest/size.mjs`）：

```
规模：100 人 × 3 年 × 12 月 = 3600 条考勤，50 份合同 / 1500 条明细，300 笔报销
紧凑 JSON：0.90 MB
当前写盘格式（indent 2）：1.45 MB
一次 CAS 用的 sha256：1.8 ms
```

也就是说：**这个规模的工地，每一次自动保存（500ms 防抖后）都要把一个 1.45 MB 的文件完整序列化、POST 上去、在服务端整本 parse、整本哈希、整本重写一次**。台账再长两年、人数翻倍，就是 3~5 MB/次。这不是"会不会崩"的问题，是"NAS 上的写放大和网络往返值不值"的问题。

### 会出事的场景

1. **静默丢更新**：`readLedger()` 的自动修复写回用的是**它读到的旧快照**，绕过队列和 CAS。上一轮已用 6 万条合同复现：并发 PUT 的新数据被读路径的旧快照覆盖。📋（我复核了代码路径，未复跑脚本）
2. **409 之后无出路**：`nas-sync.ts` 遇到 409 只弹一次 toast，不刷新版本、不重试、不给"以本机为准"的入口；而 `pullNasLedger()` 会无条件 `setAll()` 覆盖本地，所以"按提示重新加载"= **丢掉本机未保存的改动**。
3. **失败静默化**：`pushFailed` 是模块级布尔，只在首次失败时弹 toast，之后一路静默到某个请求成功为止——用户可能整场都在"以为存上了"。
4. **降级写入**：没有数据版本字段，旧版容器和新版容器可以往同一个文件里反复读写；新版写进去的字段，旧版 pull→push 一轮就可能被抹掉。

### 演化路径（分级，不必一次做完）

**现在就做（小切口，1 次发版内）**
- 把 `reconcileContractScans()` 的写盘从读路径挪走（改成显式维护动作或启动时一次性迁移），消除"读会写"。
- `pullNasLedger()` 在覆盖本地前，若有未推送改动则先确认或先 flush。
- 409 后自动重新 GET 版本 + 提供「以本机覆盖」/「放弃本机」二选一。

**中期（1~2 个专项）**
- `LedgerState` 增加 `schemaVersion` 字段；服务端在写入时校验（这时 `zod` 就派上用场了），至少挡住结构错和明显越界值。
- 把"整本快照"拆成**按实体保存**（人员 / 考勤按月 / 合同 / 报销各自文件 + 一个索引），PUT 改成实体级；版本号改成"索引版本 + 实体版本"，从此改一条考勤不再重写全库。
- 若要支持多设备协同，加"字段级合并"或明确的产品约束（当前 README 已注明"请勿同时编辑"，属可接受的取舍）。

**不建议**：现在就上 SQLite/Postgres。文件 + 目录的方案对这个部署形态（NAS、双击即用、无运维）是对的，问题在**粒度**和**校验**，不在存储引擎。

---

## 3. C · 权限没有单一来源 🟡

### 现状 ✅

同一个权限判断要在**三处**手工保持一致：

| 层 | 位置 | 形式 |
|---|---|---|
| 页面/路由 | `src/routes/*.tsx` + `perms.ts` 的 `NAV_PERM` | `<Need perm="...">` |
| 按钮 | 各页面 | `<Can perm="...">` / `useCan()` |
| 服务端 | 14 个 `routes/api/*` | `withTenant(request, fn, need)` |

权限 id 是裸字符串，没有枚举约束，也没有"这个页面有哪些写入口、分别需要什么权限"的声明表。`hasPerm()` 还有前缀糖：判 `people.view` 时，任何以 `people.` 开头的权限（如 `people.edit`）都算通过——这层隐含继承没有文档，容易在收紧权限时误判。

### 已确认的漏洞/体验问题

| 问题 | 证据 | 后果 |
|---|---|---|
| 导出含身份证/银行卡的人员表只需要 `export.use` | ✅ `routes/api/file/$kind.ts`：`need = kind.endsWith("-template") ? "import.use" : "export.use"`；`excel.ts` 的 `peopleSheetAoa` 含身份证号/银行卡号 | 预设「合同财务」（有 `export.use`、**没有** `people.view`）可以直接导出全库敏感字段 |
| `/api/audit` 写入无权限要求 | ✅ `routes/api/audit.ts` 的 `withTenant(request, async () => {...})` 没有第三个参数 | 任何登录成员可伪造/灌水审计记录（只保留 2000 条，能挤掉真实记录） |
| 「假保存成功」 | ✅ `nas-sync.ts` `pushNasLedgerNow()`：`if (!canWriteLedger(livePerms())) return;` 静默返回；页面照旧 `toast.success("已保存")` | 有 `.edit` 类权限但无 `ledger.manage` 的账号（如「合同财务」）改完合同 → 提示成功 → 刷新即消失 |
| 403 文案误导 | ✅ 同上，`pushNasLedgerNow()` 非 409 失败统一弹"请检查网络后重试" | 权限问题被当成网络问题，用户反复重试 |
| 写按钮覆盖不全 | 📋 上轮抽样：人员行内「编辑」、发放/合同/报销/考勤/照片/文件多处写入口只包了页面级 `Need`，没包 `Can` | 与上面的静默失败叠加成"改得动、存不上、还提示成功" |

### 建议

1. 建一张**声明式权限表**（单个 `PERMISSIONS` 常量：id、分组、说明），`Needs`/`Can`/服务端 `need` 都从它取，禁止裸字符串。
2. 给 `ledger.manage` 与各 `*.edit` 的关系定死一条规则，并让**客户端与服务端用同一个函数**判断，消灭"能改不能存"。
3. 敏感导出改为 `export.use + people.view`（或按 kind 细分 need）。
4. `/api/audit` POST 补 need（或只允许服务端内部写）。
5. 补一条"每个写入口都有 Can"的核对清单/自动化检查。

---

## 4. D · 失败处理与可观测性 🟡

### 现状（实测计数）✅

```
src/ 里静默吞异常的 catch {}         34 处（其中 *.server.ts 占 18 处）
src/ 里 console.* / 日志 / 监控      0 处
```

系统里没有任何一条"发生了什么"的记录。所有失败最终只变成一句 toast，toast 消失后**什么都不剩**：没有日志文件、没有错误上报、`/api/health` 只回答 `persist` 和 `ok`。

### 具体危害

| 问题 | 证据 | 后果 |
|---|---|---|
| 账户库解析失败 = 「没有账户」 | ✅ `accounts.server.ts` `readFileShape()` 的 `catch` 返回空结构 → `needSetup = true` | 前端出现「创建管理员」；一旦 setup，残缺的 `accounts.json` 会被覆盖成单账号，**owner 和凭据全丢** |
| 固定临时文件名 + 无锁读改写 | ✅ `writeAudit()` 用 `${target}.tmp`；`appendAudit()` 读-改-写无锁；`accounts.server.ts` 也是固定 `.tmp` | 并发请求时一个 `rename` 可能搬走另一个写了一半的文件 → 截断；审计条目并发丢失 |
| 写盘失败不可见 | ✅ `readLedger()` 捕获写回异常后 `catch {}` | 自动修复静默失败，下次读还是老样子，永远不知道 |
| 失败状态静默化 | ✅ `pushNasLedgerNow()` 的 `pushFailed` 去重 | 第一次失败有 toast，之后同类型的失败全部静默 |
| 审计覆盖面不全 | 📋 上一轮：合同/报销的导入替换、合同明细增删、单条发放删除不进审计 | 事后追不到"谁改了什么" |

### 建议

1. **给服务端加一层统一日志**（写入 `data/logs/*.log` 或 stdout 交给 Docker 收集）：API 错误、写盘失败、解析失败、CAS 冲突、权限拒绝，各记一行（含 bookId、路由、耗时、结果）。这不需要引入任何框架。
2. **区分"文件不存在"和"读坏了"**：`readLedger()` / `readFileShape()` 现在把两者都当成"空"。读坏必须显式报错并保留原文件（不要被客户端空状态覆盖）。
3. **临时文件名加 pid + 随机后缀，并给 `accounts.json` / `audit.json` 各加一把串行队列**（`nas-fs.server.ts` 里已经有现成的队列写法可以复用）。
4. 审计补全：所有"批量/导入/级联删除"动作都要落审计。

---

## 5. E · 交付形态 🟡

### 现状与实测 ✅

| 事实 | 实测/证据 |
|---|---|
| **构建产物入库** | `app/` 94 个跟踪文件 / 6.4 MB，是 `src/` 的构建输出 |
| **每次构建产生大量改名** | 本次 1.7.1：改 **7 个源文件**，提交 **80 个文件**，其中 **69 个在 `app/`**（hash 文件名变化） |
| **源码与产物可能漂移** | 没有任何检查保证 `app/` 是从当前 `src/` 构建出来的（这次我靠 `pnpm build` 后 `git status` 干净来验证，属于人肉手段） |
| **版本号是运行时读的文件** | `nas-fs.server.ts` `readVersionText()` 从 `process.cwd()/VERSION.txt` 和 `/app/VERSION.txt` 读；历史上 `data/VERSION.txt` 被挂成文件夹导致首页 500，只能靠部署脚本兜底删除 |
| **push 即发版** | CI 的 release 步骤用 `VERSION.txt` 第一行当 tag（`tag_name: ${{ steps.ver.outputs.ver }}`, `make_latest: true`）——**任何一次推 main 都会创建/移动一个 Release**，包括改一个错别字 |
| **CI 没有质量闸门** | `.github/workflows/docker.yml` 只有 `image` 和 `windows` 两个 job，**没有 `tsc --noEmit`、没有测试**。`开发规范.md` 里的"两道闸"全靠人工 |
| **容器以 root 运行** | `Dockerfile` 没有 `USER` |
| **自更新挂 Docker Socket** | 四份 compose（`docker-compose.yml` / `.build.yml` / `.github.yml` / `.ghcr.yml`）全部挂 `/var/run/docker.sock`；`update.server.ts` 拉镜像/换容器，**无校验和/签名验证**（📋 上一轮审查确认过无命令注入，风险在完整性而非注入） |

### 建议

1. **CI 加闸门**（最划算的一条）：`tsc --noEmit` + 测试 + "构建后 `git diff --exit-code app/`"（保证产物与源码同步）。三者都不需要新基础设施。
2. **发版与合并解耦**：release job 改成只在打 tag（`v*`）或手动触发时跑；main 只构建镜像。顺手把 `tag_name` 从"读文件"改成"读 tag"。
3. **产物入库的取舍**：可以保留（双击即用的发行包确实需要），但至少要① CI 校验同步 ② 把 `app/` 的 diff 从 code review 里摘出去（`.gitattributes` 标 `linguist-generated`）。彻底方案是产物走 Release 资产而不是 git。
4. 容器加非 root 用户；不需要网页一键更新时移除 docker.sock 挂载（`AGENTS.md` 已记录该风险）。
5. 版本号只留一个来源（`VERSION.txt` 第一行），并让 CI/构建把版本注入产物，而不是运行时读工作目录里的文件。

---

## 6. F · 回归防线：0 测试 🔴（但最便宜）

### 现状 ✅

- `package.json` 里**没有任何测试框架**，`src/` 下没有 `*.test.*` / `*.spec.*`。
- 这批 6 个 🔴 里有 **3 个是"导出再导入静默改数"**（报量被按含税放大、开票翻倍、跨年考勤年份错位）——都是纯函数层面的、**写个测试就能锁住**的问题，却靠三轮人工审查才发现。
- 我这次的验证脚本放在 `/tmp/fixtest`，**不在仓库里**，下次没人能复跑。

### 建议（这是投入产出比最高的一项）

1. 引入 `vitest`（Vite 项目零配置接入），给**纯函数**建网：
   - `wage.ts`：`monthPay` / `getWageAt`（月末边界、同日多条历史、补零）
   - `excel.ts`：**导出 → 导入往返**（人员/考勤/合同/整本，断言每个字段不丢不变）——直接覆盖刚修的 3 个 bug
   - `idcard.ts` / `dates.ts`：非法日期、15/18 位边界
   - `contracts.ts`：`contractRollup` / `splitTax` 含税不含税口径
   - `store.ts`：`migrate()` 幂等 + 字段默认值
2. 在 CI 里跑（配合 5.1 的闸门）。
3. 约定：**每修一个数据类 bug，先补一条往返/边界测试**。这条约定能把这类问题挡在下一轮。

---

## 7. 不建议做的（避免过度工程）

| 想法 | 为什么不做 |
|---|---|
| 拆 `expenses.tsx` 等 5 个 1000+ 行文件 | 属工程卫生，不是风险；页面已在统一协议下工作，拆分的收益（可读性）低于回归风险 |
| 重写 wage / contracts / dates 计算模块 | 已经收敛为单一实现，是全库最健康的部分 |
| 引入真实数据库替换文件存储 | 部署形态是"NAS 上双击即用、无运维"，文件方案是对的；要修的是粒度和校验（见 B） |
| 把服务端改成"业务权威"（所有校验搬到服务端） | 单租户可信内网场景下收益低；但**结构校验 + 关键不变量校验**该补（见 B 中期） |
| 全量重构为微服务/前后端分离 | 与产品形态不符，纯负债 |
| 引入 Sentry 等外部监控 | 内网 NAS 环境不现实；写本地日志 + Docker 收集足够 |

---

## 8. 建议顺序与投入

| 顺序 | 做什么 | 为什么这个顺序 | 性质 |
|---|---|---|---|
| 1 | **F**：加 vitest + 纯函数测试 + CI 闸门（tsc / test / 产物同步） | 最便宜、当天可完成；之后每个数据类修复都能锁住，其它几项的改造才有安全网 | 半天 |
| 2 | **B-小切口**：读路径不再写盘、pull 前保护未保存改动、409 给出路 | 消除"静默丢数"的三个已知入口，不动数据模型 | 1~2 天 |
| 3 | **D**：服务端统一日志 + 坏文件不再当空库 + temp 名/队列加固 | 让后续一切问题"可查"；账户库自保是数据红线 | 1~2 天 |
| 4 | **A**：资产按台账隔离 + 只读回落 + 逐本迁移动作 | 最大的安全项，但要动 `data/` 结构和回归面，放在有测试网之后 | 专项，3~5 天 |
| 5 | **C**：权限单一来源 + 敏感导出收紧 | 与 A 一起做最省事（都要过一遍 API 层） | 1~2 天 |
| 6 | **B-中期**：实体级存储 + `schemaVersion` + 服务端结构校验 | 决定系统能长多大；建议明确排期而不是顺手做 | 专项 |
| 7 | **E**：CI 发版解耦、非 root、产物策略 | 工程卫生，可并入任意一次发版 | 半天 |

---

## 附：关键证据索引

| 主题 | 文件:位置 |
|---|---|
| 资产全局目录 | `src/lib/nas-fs.server.ts`：`photosBase()` / `kindDir()` / `docsDir()` / `photoSearchDirs()`（`add(kindDir(kind))` 排第一） |
| 资产无归属校验 | `src/lib/nas-fs.server.ts`：`findPhotoHit()`（首个命中即返回）、`findDoc()`（只按 id）、`removeBookDir()`（只删 books/{id}） |
| 整本快照写 | `src/lib/nas-sync.ts`：`sliceState()` + `pushNasLedgerNow()`；`src/lib/nas-fs.server.ts`：`writeLedgerNow()`（indent 2 + CAS） |
| 读路径写盘 | `src/lib/nas-fs.server.ts`：`readLedger()` → `reconcileContractScans()` → `atomicWriteFile()` |
| PUT 无结构校验 | `src/routes/api/ledger.ts`：`await request.json()` → `writeLedger(body)`；`package.json` 声明 `zod` 但 `src/` 0 引用 |
| 无数据版本 | `src/lib/types.ts` `LedgerState`（无 version 字段）；`src/lib/store.ts` `version: 10`（仅 localStorage） |
| 409 无出路 | `src/lib/nas-sync.ts`：409 分支只 toast；`pullNasLedger()` 无条件 `setAll()` |
| 假保存成功 | `src/lib/nas-sync.ts`：`if (!canWriteLedger(livePerms())) return;` |
| 导出权限过宽 | `src/routes/api/file/$kind.ts`（`export.use`）+ `src/lib/excel.ts` `peopleSheetAoa()`（含身份证/银行卡） |
| 审计无权限位 | `src/routes/api/audit.ts`（`withTenant` 缺第三个参数） |
| 静默失败 | 全库 34 处 `catch {}`；`pushFailed` 去重逻辑在 `src/lib/nas-sync.ts` |
| 账户库自保 | `src/lib/accounts.server.ts`：`readFileShape()` 的 catch、`${target}.tmp` |
| 产物入库 | `app/` 94 个跟踪文件；本次提交 80 文件中 69 个属 `app/` |
| push 即发版 | `.github/workflows/docker.yml`（`tag_name` 取 `VERSION.txt` 首行、`make_latest: true`） |
| 版本运行时读文件 | `src/lib/nas-fs.server.ts` `readVersionText()` |

---

> 本报告只读产出，未修改任何源码。若决定推进，建议按第 8 节顺序立项，并在本报告末尾追加"处理状态"小节（项目惯例）。

---

## 处理状态（2026-09-10）

| 项 | 状态 | 说明 |
|---|---|---|
| **F** 回归防线 | ✅ **本轮已做** | `tests/` 建网：52 个用例（48 通过 / 4 个 todo），覆盖 `wage` / `contracts` / `dates` / `idcard` / **Excel 导出→导入往返**；新增 `pnpm test` 与 `pnpm run typecheck`，`tsconfig` 已把 `tests` 纳入类型检查。CI 闸门写好放在 `ci/check.workflow.yml`（按规范未直接改 `.github/workflows/`，需在 GitHub 网页建 `check.yml` 粘贴）。规范同步到 §2 三道闸与新增 §10。 |
| **B** 小切口（读路径不写盘 / pull 前保护未保存改动 / 409 给出路） | ⏳ 未做 | 其中「409 后无出路」的**部分风险已缓解**（1.7.1 修好了空台账误报 409，属于同一痛点的最大来源），但「读路径写盘」「pull 覆盖未保存改动」仍在。 |
| **D** 可观测性 | ⏳ 未做 | 34 处静默 catch 未动。 |
| **A** 多租户资产隔离 | ⏳ 未做 | 需专项 + 数据迁移，建议在测试网之后做（现已具备）。 |
| **C** 权限单一来源 | ⏳ 未做 | |
| **B** 中期（实体级存储 + schemaVersion + 服务端校验） | ⏳ 未做 | |
| **E** 交付形态（CI 发版解耦 / 非 root / 产物策略） | 🔶 部分 | F 顺带补上了「产物与源码同步」的 CI 校验（尚待在 GitHub 网页启用）；发版解耦、非 root 未动。 |

> 本轮只新增测试与文档，未改任何运行时代码。
