1.0.2

[1.0.2]
按 v56 架构：NAS 本地 Docker 构建，不再依赖拉 GitHub 镜像。
修复打包循环引用导致打开首页 500。
Docker 只挂 data，不再单独挂 VERSION.txt。

[1.0.1]
修复 NAS 点版本号时去连 GitHub。版本记录最近 10 条。

[1.0.0]
正式版号。Windows 解压即用。

[0.9.0]
GitHub 自动打镜像（可选）。

[0.8.0]
多账户、台账共享、细粒度权限。

[0.7.0]
合同报量、开票、收款、对账单。

[0.6.0]
工资条打印。宽表列宽可拖。

[0.5.0]
身份证有效期、照片扫描。

[0.4.0]
发放按实际收款人入账。考勤补助扣款。

[0.3.0]
多年度考勤。模板导入去重。
