# 工地台账 gongdi-ledger 代码审查纪要

> 构建状态：✅ `vite build` 通过，无编译错误  
> 版本：`package.json` / `VERSION.txt` = 1.6.8，`README.md` = 1.3.18（未同步）  
> 共发现 2 项严重、4 项中等、6 项建议。

> 状态：2026-09-06 已逐条评估并处理（详见下方「处理结论」）。

## 处理结论（2026-09-06）

- ✅ 已修：① 严重 1 依赖锁版本（按当前实际安装锁定，pnpm-lock.yaml 本来也在库）② 严重 2 README 版本同步 ③ 中 4 replaceContracts 清理孤儿条目 ④ 中 5 upsertPerson 空 id 防御 ⑤ 中 6 删除无意义 filter(Boolean) ⑥ 建议 9 更新错误信息放宽到 500 字符 ⑦ 建议 12（并升级为月末匹配，29/30/31 号生效从当月算）。
- ⏭️ 无需改：建议 8（纪要所指代码已不存在）；建议 10（Label 与 Input 同文件导出是有意设计）。
- ⏭️ 不建议改：中 3 any 类型清理（全库约 10 处，类型纯化收益低、回归风险高；`tsc` 0 错误已保证运行时安全）；建议 7 authOp 返回类型（调用面广、收益低）；建议 11 Link as any（需路由类型化重构）。

注：本节与纪要标题间保留原正文。

---


## 🔴 严重（必须修）

### 1. 依赖版本全部写 `latest` — 构建不可复现
- **文件**：`package.json`
- **位置**：L12–40，共 20+ 个依赖写 `"latest"`
- **风险**：依赖发布 breaking change 后项目会突然无法构建；无法复现问题
- **修复**：全部锁定为当前实际安装的版本号。用 `pnpm list` 查实际版本，例如：
  ```json
  "@tanstack/react-router": "^1.52.0",
  "react": "^18.3.1",
  "tailwindcss": "^4.0.0"
  ```

### 2. `README.md` 版本号与源码严重脱节
- **文件**：`README.md` 第 6 行
- **问题**：README 写当前版本是 **1.3.18**，但 `package.json` / `VERSION.txt` 实际为 **1.6.8**
- **修复**：README 中「当前版本」和版本历史同步到 1.6.8，或至少改当前版本那一行

---

## 🟡 中等（建议尽快修）

### 3. `any` 类型滥用 — 失去 TypeScript 保护
共 10+ 处，影响最大的如下：

| 文件 | 行 | 代码 | 修复 |
|---|---|---|---|
| `src/components/shell.tsx` | 68 | `(useApp as any).persist.rehydrate()` | 给 zustand persist 声明类型，或用 `unknown` |
| `src/components/shell.tsx` | 187 | `(b: any)`（BookSwitcher 中的 books.find） | 用 `BookInfo` 类型 |
| `src/components/shell.tsx` | 417 | `FALLBACK as any` | 用 `Changelog` 类型 |
| `src/components/shell.tsx` | 493 | `(e: any)`（CustomEvent） | `CustomEvent<{ detail: string }>` |
| `src/components/shell.tsx` | 773 | `(await authStatus()) as any` | 直接用 `AuthStatus` |
| `src/routes/api/audit.ts` | 14 | `(await request.json()) as any` | 声明 `AuditBody` 接口 |
| `src/routes/api/photo.ts` | 37 | `(await request.json()) as any` | 声明 `PhotoPutBody` 接口 |
| `src/lib/update.server.ts` | 495 | `{ Image, Cmd, ... } as any` | 声明 Docker Create Container 接口 |
| `src/lib/photos.ts` | 160 | `scanPhotoFolder` 返回 `Promise<any>` | 返回 `Promise<ScanResult>` |
| `src/lib/excel.ts` 多处 | — | `(cell as any).s = {...}` | `(cell as XLSX.CellObject)` |

### 4. `replaceContracts` 不清理孤儿条目，与 `removeContracts` 行为不一致
- **文件**：`src/lib/store.ts`
- **位置**：L463
- **当前代码**：
  ```ts
  replaceContracts: (contracts, entries) =>
    set({ contracts, contractEntries: entries ?? get().contractEntries }),
  ```
- **问题**：只替换合同列表，旧合同的 `contractEntries` 残留成为孤儿数据；而 `removeContracts` 会同时删合同和条目
- **修复**：
  ```ts
  replaceContracts: (contracts, entries) => {
    const keepIds = new Set(contracts.map((c) => c.id));
    const cleanedEntries = (entries ?? get().contractEntries).filter(
      (e) => keepIds.has(e.contractId)
    );
    set({ contracts, contractEntries: cleanedEntries });
  }
  ```

### 5. `upsertPerson` 空 id 字符串导致重复创建
- **文件**：`src/lib/store.ts`
- **位置**：L333
- **当前代码**：`const i = nextP.id ? people.findIndex(...) : -1;`
- **问题**：`nextP.id = ""` 时条件为 false，直接走新增分支，可能创建重复人员
- **修复**：`const i = nextP.id?.trim() ? people.findIndex((x) => x.id === nextP.id.trim()) : -1;`

### 6. `nas-fs.server.ts` 中无意义的 `.filter(Boolean)`
- **文件**：`src/lib/nas-fs.server.ts`
- **位置**：L764
- **当前代码**：`const candidates = [join(process.cwd(), "VERSION.txt"), "/app/VERSION.txt"].filter(Boolean);`
- **问题**：数组元素都是非空字符串字面量，filter 永远原样返回
- **修复**：去掉 `.filter(Boolean)`

---

## 🟢 建议改进

### 7. `authOp` 返回 `Promise<any>`
- **文件**：`src/lib/auth.ts` L148
- **修复**：改为 `Promise<unknown>`，调用处再断言

### 8. `excel.ts` 中 `normalizeEntry` 参数类型过严，导致重复 `as` 断言
- **文件**：`src/lib/excel.ts` L810、870、875、882
- **问题**：同一类型断言 `as Parameters<typeof normalizeEntry>[0]` 出现 4 次
- **修复**：在 `excel.ts` 顶部定义别名 `type EntryInput = Parameters<typeof normalizeEntry>[0]`；或放宽 `normalizeEntry` 参数类型

### 9. `update.server.ts` 错误信息截断过短
- **文件**：`src/lib/update.server.ts` L377、L455
- **当前**：截断到 180/240 字符
- **建议**：至少保留 500 字符，Docker 错误信息通常很长且关键

### 10. `shell.tsx` 中 `Label` 从 `input.tsx` 导入
- **文件**：`src/components/shell.tsx` L28
- **当前**：`import { Label } from "~/components/ui/input";`
- **建议**：确认是否 `input.tsx` 也导出 Label；如果是，建议拆到 `label.tsx` 保持职责单一

### 11. `Link` 的 `to` 属性多处 `as any`
- **文件**：`src/routes/index.tsx` L121、284
- **当前**：`<Link to={to as any} ...>`
- **建议**：通过 TanStack Router 的 `LinkProps` 泛型消除断言

### 12. `getWageAt` 使用 28 号作为月末参考，注释不足
- **文件**：`src/lib/wage.ts` L60
- **当前**：`const queryDate = \`${year}-...-28\`;`
- **建议**：加注释 `// 用 28 号确保所有月份都存在这一天，简化匹配逻辑`

---

## 快速修复清单（给主力 AI）

按优先级排序：

1. [ ] `package.json` — 所有 `"latest"` 替换为实际版本号（`pnpm list` 查看）
2. [ ] `README.md` — 版本号 1.3.18 → 1.6.8
3. [ ] `src/lib/store.ts` L333 — `nextP.id` 加 `.trim()` 判断
4. [ ] `src/lib/store.ts` L463 — `replaceContracts` 清理孤儿条目
5. [ ] `src/lib/nas-fs.server.ts` L764 — 去掉无意义的 `.filter(Boolean)`
6. [ ] 逐文件清理 `any` 类型（按上面表格顺序）
7. [ ] `src/lib/auth.ts` L148 — `Promise<any>` → `Promise<unknown>`
8. [ ] `src/lib/excel.ts` — 提取 `normalizeEntry` 输入类型别名
9. [ ] `src/lib/update.server.ts` — 错误截断放宽到 500 字符
10. [ ] `src/routes/index.tsx` — 消除 `Link` 的 `as any`

## 复审更正（2026-09-08，依据《代码复审报告-20260908》）

- ✅ 更正①文字失实：实际未锁定版本（pnpm 对 h3-v2 别名解析失败），可复现性由 pnpm-lock.yaml 承担；VERSION.txt 说明已改写为真实情况。
- ✅ 更正② README：随 1.6.10 同步（之前 1.6.8 又落后一个版本）。
- ✅ 更正建议8：`excel.ts` 4 处断言确实存在，已提取 `EntryInput` 类型别名消除。
- ✅ 新-1 removeMember 越权：已修（补上与 addMember/setMember 相同的台账二次校验）。
- ✅ 新-2 upsertPolicy 去重条件：已修（无 id 新增也按保单号合并/拦截）。
- ✅ 新-3 密码哈希：服务端账户密码改为 scrypt+随机盐，旧密码登录后静默升级；客户端门禁哈希保留 sha256（仅存本机浏览器，且 http 环境无 WebCrypto，加固服务端才是关键）。
- ✅ 新-4 更新检查接口需登录；新-5 注明「多设备避免同时编辑」。

---
