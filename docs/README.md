# 文档索引 · 工地台账

> 仓库里除了四个**入口文档**留在根目录，其余说明与报告全部收在 `docs/` 下。
>
> | 位置 | 放什么 |
> |---|---|
> | `README.md`（根） | 总入口：仓库结构、版本、部署与使用路线 |
> | `VERSION.txt`（根） | 版本号（构建读第一行）+ 更新记录 |
> | `开发规范.md`（根） | **强制约定**，改代码前必看（正文自述须在仓库根目录） |
> | `AGENTS.md`（根） | 给 AI / 协作方的须知（自动读取位置就是根目录） |
> | **`docs/审查与报告/`** | 历轮审查 / 复审 / 体检 / 架构审核（内部记录，含处理状态） |
> | **`docs/使用与部署/`** | 使用说明、目录结构、部署说明、上传说明（面向使用者/运维） |
> | `ci/` | 质量闸门与发版说明（需在 GitHub 网页启用） |

---

## 一、审查与报告（`docs/审查与报告/`）

按时间顺序。每份报告末尾都有「处理状态」小节，逐条写 ✅已修 / ⏭️无需改 + 理由（项目惯例，见 `开发规范.md` §2）。

| 文件 | 覆盖范围 | 基线 | 状态 |
|---|---|---|---|
| `代码审查纪要.md` | 首轮：依赖锁版本、README 版本脱节、any 类型、replaceContracts 孤儿、upsertPerson 空 id 等 | 1.6.8 | ✅ 已闭环（含 2026-09-08 复审更正） |
| `代码复审报告-20260908.md` | 核验纪要 + 新发现：removeMember 跨台账越权、upsertPolicy 去重、无盐 SHA-256、更新接口鉴权、多端覆盖提示 | 1.6.9–1.6.11 | ✅ 已闭环（两轮终审） |
| `审查报告-UI与数据逻辑-20260908.md` | 打印工资条打款口径、合同合计含税/不含税、身份证日期溢出、查询起止日溢出、导出/导入重名等 13 项 + 3 项遗留 | 1.6.11 | ✅ 已闭环（两轮终审） |
| `收官体检报告-20260908.md` | 前两轮之外的模块：批量工资无确认且不写历史、区间导出、模板年份、影像搜索防御 | 1.6.13 | ✅ 已闭环 |
| `新功能审查-1.6.14至1.6.17-20260909.md` | 餐补导出漏列、批量工资历史字段语义、仅打款打印；1.6.18–1.6.20 核验 | 1.6.14–1.6.17 | ✅ 已闭环 |
| `审查修复说明-20260910.md` | 某轮修复的说明：自动保存串行、原子写 | 1.7.0 | ✅ 已闭环（注：其中「文档上传失败保留旧文件」的说法与后来核对到的 `saveDoc` 先删后写顺序不符，已在架构报告处理状态里更正为待办） |
| `全量功能走查-20260911.md` | 三路走查（接口与权限／Excel 往返／页面）：26 项确认并修复，含回归防线 | 1.7.13 | ✅ 已闭环（见报告末尾「处理状态」） |
| `全量复查-20260912.md` | 操作一致性专项：五处弹窗绕过防误关、按钮顺序、round2 三分裂、考勤月表未保存即丢、audit 失败静默 + 规范本身 3 条建议 | 1.7.13 | ✅ 全部闭环（9→1.7.15，11→1.7.16，10→§6.10 已修订） |
| `架构审核报告-20260910.md` | **结构性**审核 A–F：多租户资产隔离、持久化模型、权限单一来源、可观测性、交付形态、回归防线 + 迁移路径 | 1.7.1 | ✅ A–F 主体已落地（1.7.2–1.7.5）；剩余项见下方「未闭环」 |

## 二、使用与部署（`docs/使用与部署/`）

| 文件 | 用途 | 备注 |
|---|---|---|
| `使用说明.md` | 面向使用者的完整说明（包结构、部署、功能模块、照片命名、常见问题） | ✅ 版本行已改为「以 `VERSION.txt` 为准」不再写死；版本号规则已与 `开发规范.md` §1 对齐（1.7.7）。文中 1.1.6/1.2.22 仅作历史包名保留 |
| `目录结构.txt` | data 目录与部署目录该长什么样 | ⚠️ 停在 **1.2.20** |
| `部署说明.txt` | 飞牛 NAS 部署与升级步骤 | ⚠️ 停在 **1.2.20** |
| `说明.txt` | Windows 包里的三分钟上手 + 历史版本要点 | ⚠️ 停在 **1.2.22**；注意 `win/pack.sh` 会**重新生成**一份进 Windows 包 |
| `程序文件说明.txt` | 覆盖包包含哪些程序文件 | ⚠️ 文中「仓库里本来就没有 .ts/.tsx」与现状不符（仓库有完整 `src/`） |
| `GITHUB上传说明.txt` | 往 GitHub 覆盖上传时的注意事项 | ⚠️ 仍要求上传已被删除的 `app/public/templates`（1.6.15 起模板由软件实时生成） |

## 三、当前未闭环项（唯一清单）

> 以后新增/关闭都改这一节，不要再散落到各文档里。AGENTS.md 里保留一份同样的清单给智能体看。

**A. 需要专门排期的**

1. **实体级存储**：整本台账仍是一个 `ledger.json`，每次改动整本上传（实测「中等工地」约 1.45 MB/次）。
   要拆成按实体保存 + 索引，属专项改造，别在改功能时夹带。方案见 `架构审核报告-20260910.md` §2「中期」。

**B. 需要产品决策的**

2. **预设「合同财务」缺 `people.view`**：全量台账读取需要 `people.view`，所以该预设实际看不到数据。
   要么放开该权限（会连身份证/银行卡一起开放），要么做实体级权限。
3. **容器以 root 运行 / 挂载 docker.sock**：属部署取舍，改非 root 需入口脚本先 chown 再降权；
   见 `ci/README.md` §3。

**C. 明确的小缺陷（测试里以 `todo` 标记，修好后会自动转 pass）**

> 4/5/6 已于 **1.7.8** 修复，7 已于 **1.7.11** 修复；**测试里的 `todo` 已清零（0 条）**。

4. ✅ 已修（1.7.8）：`dates.ts` 的 `ymd()` 现在校验当月天数，`2026-02-31` / 平年 `2026-02-29` 判空
   （`daysBetween` 也按真实日期校验，不再把非法日期进位成 3/3 算成 62 天）。
5. ✅ 已修（1.7.8）：`idcard.ts` 的 16/17 位按「应为 15 位或 18 位（现在是 N 位）」报错，
   `parseIdCard` 对非 15/18 位返回空信息，不再解析出错误生日。
6. ✅ 已修（1.7.8）：`getWageAt` 比较前把 `fromDate` 补零并校验，`2026-7-1` 也能当月生效。
7. ✅ 已修（1.7.11）：人员信息表新增「工资历史」列，导出/导入无损往返；列被手改坏时按「没有历史」处理。
8. ✅ 已修（1.7.13）：启动器加了 52MB 请求体上限（声明超限立刻 413，不再把连接挂住），未捕获异常也落 `data/logs`。
9. `saveDoc` 仍是「先删旧文件再 rename」（崩溃窗口内旧文件已删、新文件还是隐藏临时名）。
10. ✅ 已修（1.7.15）：`pnpm dev` 整站 500（SSR 加载 react 报 `module is not defined`）。
    根因是 `vite.config.ts` 的 `ssr.noExternal: true`（为 Docker 免 node_modules 而加）在 dev 下把 CJS 的 react 内联成 ESM 跑；
    现改为仅构建时生效，dev 恢复默认外部化。

**D. 待现场确认**

（暂无。原第 10 条「飞牛一键更新」已于 2026-09-11 现场验证闭环，见下。）

10. ✅ **已闭环（2026-09-11）**：**飞牛 NAS 上应用内从 1.7.11 更新到 1.7.12 成功**
    （更新卡片显示「上次更新：成功」，日志完整记录 更新容器已启动 → 新容器已创建 → 老容器已停止 →
    新容器已启动 → 已接管名称 → 已清理旧镜像 → 更新成功）。

    这条路的完整历史（三次修、两次误判，逐条留档，避免以后重走）：

    | 版本 | 动作 | 结论 |
    |---|---|---|
    | 1.7.4 | 更新改为「立刻返回 + 后台执行 + 前端轮询」，同源校验兼容 `X-Forwarded-Host` | ✅ 反代场景不再被 60s 超时掐断（有效） |
    | 1.7.5 / 1.7.6 | `dockerReq` 第三参数把容器配置当成了 `{ body }`，请求体被丢成 `{}` → Docker 报 `config cannot be empty in order to create a container` | ✅ 真缺陷，修掉后更新容器才创建得出来 |
    | 1.7.7 | 更新卡片显示上次结果 + 应用内「查看更新日志」 | ✅ 让后续排查第一次有了现场证据 |
    | 1.7.8 | 拉到的镜像 ID 与当前一致就换源/停止更新；更新成功清掉上一个版本的镜像；新增「清理旧镜像」 | ⚠️ 判断方向错（不是镜像缓存），但这两项本身仍有用，保留 |
    | 1.7.9 | 优先按 `sha-<main 提交>` 拉（绕开加速站缓存）+ 读镜像内 `VERSION.txt` 核对版本 | ⚠️ 同上；按提交 sha 拉确实能绕开加速站延迟，保留 |
    | **1.7.10** | **真因：更新容器要执行的脚本本身语法错误** —— `UPDATER_SCRIPT` 由模板字符串拼成，写法里的 `\n` 被模板字符串变成**真换行**，生成的 `.cjs` 里字符串字面量跨行，`node` 解析阶段即 `SyntaxError`（stderr 没人看），容器又被 `AutoRemove` 删掉：界面只看到「已受理」，容器一个没换、日志一个字都没有 | ✅ **真因**。脚本改用 `log()`/`fail()`，并新增「把生成的脚本交给 JS 引擎解析一遍」的守卫测试 |
    | 1.7.10 | 更新容器改为「环境变量传任务 + `Cmd` 内联脚本」（不依赖 data 挂载）、不再自动删除、启动 2 秒后检查存活、失败当场报错并贴出它的输出 | ✅ 以后这类失败不会再静默 |
    | 1.7.12 | `gongdi-updater` 的生命周期写清楚；「清理旧镜像」顺手删掉它，镜像占用统计忽略已退出的它 | ✅ 避免它把旧镜像一直占住 |

    教训（已写进 `AGENTS.md`）：**「用模板字符串生成一段可执行脚本」时必须做语法自检**。
    这个 bug 的症状（「更新说成功、版本没变、日志一片空白」）与「镜像站缓存」几乎一样，
    前两版就是被这个相似性带偏的 —— 直到用户贴出完整的 `update.log`（里面一行来自更新容器的记录都没有），
    才把范围从「网络/镜像」收敛到「脚本根本没跑」。


**E. 文档滞后**

11. `docs/使用与部署/` 下其余 5 份说明（`目录结构.txt` / `部署说明.txt` / `说明.txt` / `程序文件说明.txt` / `GITHUB上传说明.txt`）
    版本行或描述停在 1.2.x / 旧结构（见上表 ⚠️）；内容与现状的差异需要一次集中更新（`使用说明.md` 已于 1.7.7 处理）。

---

## 四、维护约定

1. **新报告放哪**：审查/复审/体检/架构类 → `docs/审查与报告/`；使用/部署类 → `docs/使用与部署/`。
2. **处理报告**（`开发规范.md` §2）：
   - 先逐条核验（文件:行号），区分「真实问题 / 可接受取舍 / 已过时」；
   - 修完**在原报告末尾追加「处理状态」**，逐条写 ✅已修 / ⏭️无需改 + 理由，不留悬案；
   - 表述必须与事实一致：没做的绝不能写「已修」。
3. **同步本索引**：新增报告要加进第一节的表格；未闭环项变动要改第三节。
