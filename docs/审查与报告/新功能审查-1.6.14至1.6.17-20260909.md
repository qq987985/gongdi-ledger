# 新功能审查 · 1.6.14–1.6.17 · 2026-09-09

> 范围：提交 `4c63faa`（1.6.14 收官体检修复）、`fa17b7a`（1.6.15 模板对齐）、`8bf6851`（1.6.16 卡号分组）、`2104403`（1.6.17 仅打款打印）的全部 src diff。
> 结论先行：**新功能本身逻辑都正确；但 1.6.15 模板对齐只做了一半——导出方向漏了餐补列，导出再导入会丢数据**，建议修。另有 2 个小瑕疵。

---

## 🟡 建议修复（1 项）

### 1. 餐补列：模板→导入通了，导出→导入丢餐补
- **文件**：`src/lib/excel.ts`
- **问题**：1.6.15 给人员导入模板补了「餐补/天」列（DEMO_PEOPLE L323），导入解析 `rowToPerson` 也读该列（L148）✓。但**导出方向没跟上**：
  - `peopleSheetAoa`（L553-556）的表头没有「餐补/天」——人员名单导出（`buildPeopleWorkbook`）和整本台账导出（`buildFullWorkbook` L651）都走它；
  - 后果：导出的人员表 / 整本台账再导入时，餐补静默变 0。整本导出本是备份手段，这一列丢了备份就不完整。
- **修复**：`peopleSheetAoa` 表头加「餐补/天」（放在「加班规则」后，与模板同序），数据行加 `p.mealAllowance || ""`。

---

## 🟢 小瑕疵（2 项，不影响使用）

| # | 位置 | 问题 |
|---|---|---|
| 2 | `settings.tsx` `withHistory` | 按月计薪时，工资历史条目的 `dailyWage` 被写成月工资的数值（`dailyWage: nextPayType === "month" ? p.monthWage : wage`），字段语义串了。不影响计算（payType=month 走 monthWage），但数据脏，建议 `dailyWage: p.dailyWage` 保留原值。 |
| 3 | `settings.tsx` `apply` | 「只填空白」+ 勾选记历史时，confirm 文案只说「只填空白」，没提会同时写一条今天生效的工资历史。 |

---

## ✅ 逐项核验通过

**1.6.14 收官修复**（对应《收官体检报告》1 中 3 小）：
- 批量工资：confirm 列明人数与改动内容 ✓；「记为工资历史（今天生效）」默认勾选、过去月份不追溯 ✓；同日重复 apply 会替换当天历史条目（`filter(h => h.fromDate !== today)`）不堆积 ✓。
- 区间导出起止选反自动交换：逻辑推演正确（交换后 from≤to），界面有提示文案 ✓。
- 模板年份缺省当前年 ✓；影像搜索 `String(x || "")` 防御 ✓。
- 加班规则在确认弹窗里显示的是中文编码（`按小时:25`），可读 ✓。

**1.6.15 模板对齐**：静态旧模板删除后无代码引用（仅说明文字提及目录）✓；导入解析按表头名取列，列序变化不受影响 ✓；示例手机号 78 → 13900005678 笔误已修 ✓。

**1.6.16 卡号分组**：`formatCardNo` 纯数字才分组、非数字原样返回，只作用于打印Sheet，编辑/复制不受影响 ✓。

**1.6.17 仅打款打印**：
- 仅打款模式下过滤掉区间无打款的人，不会印出空白单 ✓；
- 分组行数统计按模式分别计月份行/打款行 ✓；
- 仅打款时工资明细表、应发合计、「未打款」列正确隐藏 ✓；
- 打印按钮计数与 toast 文案随模式切换 ✓。

**环境**：四处版本号一致 1.6.17；`tsc --noEmit` 通过；`vite build` 通过。

---

## 🔧 处理状态（2026-09-09）

> 上表 3 条均已逐条对原报告「文件:行号」核验属实后修复，特此闭环。

| # | 结论 | 处理 |
|---|------|------|
| 1 | 餐补列导出遗漏（导出再导入丢数据） | ✅ 已修：`src/lib/excel.ts` `peopleSheetAoa` 表头加「餐补/天」（加班规则后、性别前），数据行补 `p.mealAllowance \|\| ""`。人员导出与整本台账导出均走此函数，现导出再导入不丢餐补。 |
| 2 | 批量工资 `withHistory` 日工资字段语义串 | ✅ 已修：`src/routes/settings.tsx` `withHistory` 按月计薪时 `dailyWage` 改为保留原值 `p.dailyWage`，不再误写为月薪数值。 |
| 3 | 「只填空白」确认文案漏提工资历史 | ✅ 已修：`src/routes/settings.tsx` `apply` 只填空白且勾选「记为工资历史」时，确认文案补充「会为本次填写的人记一条今天生效的工资历史（过去月份保持原工资，不追溯重算）」。 |

### 审查期间额外修复（1.6.17 增强，`src/routes/query.tsx`）

- 仅打款记录模式标题改为「台账 · 打款记录」，并隐藏「工资/加班」背景字段（只留姓名/班组）。
- 打印内容按模式对称过滤：仅工资 → 只印有出勤月的人；仅打款记录 → 只印有打款的人；工资+打款记录 → 不设限。

> 版本已随修复更新为 **1.6.18**（`VERSION.txt` / `package.json` / `app/VERSION.txt` / `README.md` 四处一致）。

---

## 终审（2026-09-09，提交 702ce76 / 1.6.18 + 8f5a03d / 1.6.19）

### 1.6.18 审查修复核验

1. ✅ **餐补导出列**：`peopleSheetAoa` 表头与数据行均已补「餐补/天」，且做了**实测闭环**——构造 mealAllowance=12 的人员，导出 xlsx 再 `parsePeopleSheet` 回读，餐补=12 完好保留。导出→导入不再丢餐补。
2. ✅ **批量工资历史字段**：`dailyWage: p.dailyWage` 保留原值，字段语义修正。
3. ✅ **onlyBlank 文案**：确认弹窗已说明会记一条今天生效的工资历史。
4. ✅ **仅打款打印收敛（顺手优化）**：pays 模式标题改「台账 · 打款记录」、隐藏工资/加班表头；wage 模式过滤无出勤月的人，避免工资空单。

### 1.6.19 新功能核验（超龄提醒可展开）

- ✅ 新旧两版首页都接入 `OverAges`；`count=0` 不渲染；展开列出姓名 + 身份证号。
- 🟢 两个可待办可不待办的小点：
  1. 身份证号在首页**明文展开**——登录后页面、风险可控，但投屏/截图场景会一次性暴露所有超龄人员证件号，如需更稳可中间位打码（如 `1101********1210`）；
  2. 展开列表无高度上限，超龄人数特别多时首页会被拉得很长，可加 `max-h-64 overflow-y-auto`。

### 环境

四处版本号一致 1.6.19；`tsc --noEmit` 通过；`vite build` 通过。

> **结论：本报告此前全部条目闭环；1.6.18 / 1.6.19 无阻塞问题，两个 🟢 小点随下个版本顺手处理即可。**

---

## 追加处理（1.6.20）

> 终审两个 🟢 小点已在 1.6.20 顺手处理，特此闭环。

| 小点 | 处理 |
|------|------|
| 身份证号明文展开 | ✅ 已修：`src/routes/index.tsx` `OverAges` 用 `maskIdCard` 中间位打码（保留前 4 / 后 4，如 `1101********1210`），短于等于 8 位原样返回。 |
| 展开列表无高度上限 | ✅ 已修：`<ul>` 加 `max-h-64 overflow-y-auto`，超龄人多时列表内部滚动，首页不被拉长。 |

> 版本已随修复更新为 **1.6.20**（`VERSION.txt` / `package.json` / `app/VERSION.txt` / `README.md` 四处一致）。
