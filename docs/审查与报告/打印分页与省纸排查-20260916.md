# 打印分页与省纸排查（全入口，20260916，1.8.10 + 1.8.11）

> 起因：用户实测「发放记录汇总，我测试了 26 条记录打印汇总就会分页，第一页还有很多空缺就会把尾巴放在第二页」，
> 随后补一条口径：**「所有的打印都排查一下，除非打不开下一个人或者下一条记录才会出现大空白；能打开还是尽量省纸；我可以拆开分发。」**
>
> 结论：**6 个打印入口全部量过**（发放明细 / 发放汇总 / 报销单 / 保险合同清单 / 合同对账单 / 工资条），
> 1.8.10 修掉「整块被推走留下大片空白」，1.8.11 再修掉「合计每页重复」与「续页认不出是哪张单」，
> 1.8.12 按用户追加口径把「**一条 = 一个人 / 一份单据**」定死（整条放得下就并排、放不下才另起一页）。
> 判据不是「有没有分页」，而是**每一页填得多满**、**有没有整页空白**、**裁开之后每页认不认得出**。

---

## 1. 判据与量法（可复现，零依赖，不进构建）

| 项 | 做法 |
| --- | --- |
| 出 PDF | 真浏览器（本机 Chrome + `playwright-core`）打开**构建产物**，点真实打印按钮，`emulateMedia("print")` 后 `page.pdf({ preferCSSPageSize: true, printBackground: false, margin: 12mm })` —— 与用户 Ctrl+P 同一条排版路径、同一套 `@page` 页边距 |
| 量留白 | `ci/print-pdf.mjs`：自写 PDF 内容流解释器（只用 `node:zlib`），算每页**文字**墨迹的最低/最高点 → 底部/顶部留白 mm。外框与表格线不计（否则单据边框会被当成内容） |
| 认得出 | 同一解释器按 `/ToUnicode` CMap 解码文字，输出**每页首行/末行**，用来验证续页是否重复了表头与单据抬头 |
| 命令 | `DATA_DIR=/tmp/… PORT=4731 node app/server/index.mjs &` → `DATA_DIR=… PORT=4731 node ci/mobile-print-check.mjs pages [用例名…]` |
| 数据规模 | 2 人 / 26 笔发放（用户那条）+ 40 报销 + 40 条合同明细 + 40 参保人；另一组 45 人 / 46 笔发放（把汇总清单也压到多页） |

## 2. 1.8.10：大片空白（用户报的那条）

复现（改动前，26 笔 / 2 人，含代收折行）：**明细清单第 1 页只有一个人的记录，页底留白 112.3mm，
另一个人的整节被推到第 2 页**。三条根因：

1. 打印件里用了**容器级** `break-inside-avoid`（每个人的明细 `section`、整张对账单 `article`、小表外层 `div`）——
   页底放不下就**整块**推移，上一页留一大片空白；
2. 外壳 `.app-bg` 的 `min-h-screen/min-h-dvh` 在打印媒体里也生效（容器至少一屏高），
   `overflow-x-hidden` 在打印态等于滚动容器（会裁内容）→ 提前断页/多空白页；
3. 打印表格没有跨页表头规则，第 2 页起没有表头。

修法（规则集中到 `src/styles.css` 的「打印分页协议」一处）：一屏高清零 + `.app-bg { overflow: visible }`；
「不许拆页」下沉到**行/小单元**（`.print-only tr`、`.print-keep`）；`.print-only thead { display: table-header-group }`。

## 3. 1.8.11：两条「拆开分发」才暴露的问题

### 3.1 合计行被浏览器印在每一页页脚（口径）

- **复现**：45 人台账 → 发放「汇总清单」2 页：第 1 页只有第 1~32 行，页底却印着
  `总计（全部实际收款人）46 笔 ¥343,500.00`；保险合同清单 2 页：第 1 页只有 29 人，页底印着**全单**合计 `10,691.53`。
- **根因**：`tfoot` 的 UA 默认值是 `table-footer-group`，分页媒体下浏览器按规定在**每一页**页脚重复它。
- **修法**：`.print-only tfoot { display: table-row-group }`（`src/styles.css`）→ 整单合计只在表格结束时出现一次。
- **实测**（同一份 PDF 按文字位置统计）：汇总「总计」第 1 页 **1 处 → 0 处**（第 2 页仍 1 处）；
  保险「合计」第 1 页 **1 处 → 0 处**（第 2 页 2 处：明细表 + 按班组汇总表，都合法）。

### 3.2 续页认不出是哪张单（可用性）

- **复现**（改动前每页首行）：保险合同清单第 2 页 `序号 姓名 队长 开始日期 结束日期 使用天数 保费(元)`；
  合同对账单第 2 页 `收款`；报销单第 2 页 `序 购 买 时`（列标题）。
  用户要**拆开分发**，这些续页拿到手里不知道属于哪张保单 / 哪份合同 / 谁的报销。
- **修法**：把**单据抬头搬进 `<thead>` 第一行**（表头本来就跨页重复）：
  · 保险：`团体保险人员清单 · PICC-2026-0001 · XX小区二期团体意外险 · 队长：… · 在保`（唯一实现 `policyCaption()`，明细表与「按班组汇总」都带）；
  · 合同对账单：每张小表带 `合同对账单 GD-2026-001 · 开票`（`MiniTable` 新增 `caption`）；
  · 报销单：表头带 `报销人：… · 收款人：… · 开户行：… · 打款账户：…`（原来那张独立小表并进表头，第 1 页行数不变）。
- **效果**：第 1 页**不多占行**（抬头原来也在），只有续页顶上多一行（≈4mm，续页本来就空着）。
  实测续页首行变成：保险 `团体保险人员清单 · PICC-2026-0001 · …`、合同 `合同对账单 GD-2026-001 · 开票`、
  报销单 `报销人：张建国 · 收款人：木工一班 · 开户行：— · 打款账户：—`。

## 4. 全部入口的量测结果（同一把尺子，逐页「文字结束处留白 mm」）

**A. 2 人 / 26 笔发放 + 40 报销 + 40 合同明细 + 40 参保人**（改动前 1.8.9 ↔ 改动后 1.8.11）：

| 用例 | 改动前（页数 / 每页留白） | 改动后 |
| --- | --- | --- |
| 发放明细 · 全部人 | 2 页 / **121.3** · 122.4 | 2 页 / **7.0** · 226.6 |
| 发放明细 · 单人 | 1 页 / 103.1 | 1 页 / 103.1（无变化） |
| 发放汇总 · 全部人 / 单人 | 1 页 / 174.2 · 197.3 | 同（1 页够用） |
| 报销单（30 行未报销） | 1 页 / 207.6 | 1 页 / 216.6（抬头合并后少一行） |
| 保险合同清单（40 人） | 2 页 / 5.9 · 196.4 | 2 页 / 4.5 · 189.7 |
| 合同对账单（30 条明细） | 3 页 / **71.3** · **21.1** · 74.2 | 3 页 / **7.0** · **2.7** · 127.4 |
| 工资条（2 人） | 4 页 / 6.8 · 241.9 · 4.4 · 268.3 | 同（设计未改，见 §5） |

**B. 45 人 / 46 笔发放（把汇总清单也压到多页）**：

| 用例 | 结果 |
| --- | --- |
| 发放明细 · 全部人 | **6 页，前 5 页留白全部 0.1mm**（页页填满），第 6 页为尾巴；第 2~6 页顶端都有重复表头 |
| 发放汇总 · 全部人（46 行） | 2 页 / 0.1 · 130.8；**总计只在第 2 页出现一次** |
| 保险合同清单（45 人） | 2 页 / 2.7 · 150.9；第 2 页首行为保单抬头；第 1 页不再出现合计 |
| 合同对账单（30 条明细） | 3 页 / 0.1 · 0.1 · 173.4；第 2/3 页首行分别是 `合同对账单 GD-2026-001 · 收款` / `…-002 · 开票` |
| 报销单（30 行） | 2 页 / 0.1 · 124.7；第 2 页首行为报销单抬头 |
| 工资条 | 2 人 2 页（每人半页），与设计一致 |

**没有整页空白页，没有一页出现「上一页留一大片、尾巴掉到下一页」**（尾巴页留白是内容结束的自然结果）。

## 5. 1.8.12：一条 = 一个人 / 一份单据（口径细化）

用户在 1.8.11 之后追加了判据：「**能单独一页的就不要和别的混到一起**」＋
「**如果第一页有空白，第二条能全部打到第一页就 2 条打到一起，反之就另开一页，后边依次类推**」。

- **规则**：**一条 = 一个人 / 一份单据**（发放明细里每个人的整节、合同对账单里每份合同）——
  整条放得下就与上一条**并排塞满**、放不下才**整条另起一页**，**绝不把一条劈到两页**；
  比一整页还高的条（例：几十条明细的一份合同）允许自己跨页续排（用户「可以拆开分发」）。
- **实现**：唯一一处规则 `.print-only .print-doc { break-inside: avoid }`（`src/styles.css`），
  组件里只加 `print-doc` 类名；**不许**给 `.print-doc` 用 `break-before: page`
  （那是「宁可留白也不并排」，与口径相反，守卫会拦）。
- **与 1.8.10 的关系（重要，避免来回翻）**：1.8.10 当时把这些「整块不拆」**全删了**，
  是因为那时**还有两个「无缘无故提前断页」的根因没修** —— 外壳 `.app-bg` 的
  `min-h-screen/min-h-dvh` 在打印媒体里撑满一屏、打印表格没有跨页表头；整块被推走的空白
  因此被放大成用户报的 **112.3mm**。**根因修掉之后再把它加回来**，实测同样的数据页数不变、
  第 1 页依旧填满，而人的记录不再被拆页。

**实测（真浏览器出 A4 PDF，量每页文字结束处留白 mm）**：

| 数据 | 1.8.11（允许拆人） | 1.8.12（整条不拆） |
| --- | --- | --- |
| 2 人 / 26 笔发放 · 明细 | 2 页 / 7.0 · 226.6（第 1 页含第 2 个人的前几笔） | 2 页 / **0.1** · 97.4（第 1 页 = 第 1 个人整节，第 2 页 = 第 2 个人整节） |
| 3 人 / 27 笔发放 · 明细 | 2 页 / 0.1 · 175.2（第 3 个人被劈开） | 2 页 / **0.1** · 142.4（第 3 个人整节） |
| 45 人 / 45 笔发放 · 明细 | 6 页 / 前 5 页 0.1 · 尾页 172.0 | 6 页 / 前 5 页 0.1 · 尾页 **136.1** |
| 合同对账单（每份合同 ~185mm） | 3 页，第 1 页 = 第一份的尾巴 + 第二份的开头（**两份混在一页**） | 3 页，第 1~2 页 = 第一份（自己续排），第 3 页 = 第二份**独占一页** |

**结论**：三组数据的**总页数一个没涨**，第 1 页依旧 0.1mm 填满，而「一个人的记录被劈到两页」
「两份单据混在一页」都消失了 —— 只在「下一条整条放不进剩下的空白」时才留白（这正是用户的口径）。

## 6. 已知取舍（未改，等用户决定）

**工资条**与 1.8.12 的规则本来一致（一张 = 一条，两张能并排就并排、放不下才单独一页），
仍是「一张或两张一页 + 中间裁切线」的设计（`.payslip` 是唯一允许容器级不拆页的例外）。
单个人的内容超过一页时（例：9 个月考勤 + 14 笔打款，约 303mm 高），会印成 **2 页，第 2 页只有
「领款人签字」一行**（实测该页留白 268.3mm）。这是「一人一张、便于签字裁剪」的取舍；
若要与其它单据一致地「续排填满」，需要重排工资条版式（去掉 flex 拉伸、允许表体跨页），
属版式改动，本轮按用户「不好定夺的让我来确定」的口径留给用户拍板。

## 7. 证据与工具

| 内容 | 路径 |
| --- | --- |
| 量测工具（浏览器 + PDF 解析） | `ci/mobile-print-check.mjs`（`pages` 子命令）、`ci/print-pdf.mjs` |
| 改动前 / 改动后的 A4 PDF | `/tmp/gd-before/pdf/`（1.8.9 构建，`git archive 5e7346c`）、`/tmp/gd-before/pdf-after/`（1.8.11） |
| 最近一次全入口运行 | `browser-screenshots/print/*.pdf` + `*.png`（已在 `.gitignore`） |
| 静态守卫 | `tests/ui-guards.test.ts`（一屏高清零 / 表头跨页 / 不许容器级不拆 / 合计只在最后一页 / 单据抬头必须在 thead 里） |
| 版本记录 | `VERSION.txt` 的 `[1.8.10]`、`[1.8.11]` 两节 |

> 工具本身**不进构建、不进 CI**（需要真实浏览器，`package.json` 里没有它）——与 `ci/mobile-print-check.mjs`
> 既有的定位一致；仓库自动回归在 `pnpm test`（含上面两条新的打印守卫）。

## 8. 1.8.13：把「最后一行被挤到第 2 页」压掉（用户截图：那么大空白，竟然分了 2 页）

**复现**：把用户数据复刻成 30 人 / 140 笔发放（其中 70 笔代发 → 汇总每行备注都带一长句），
走真实入口打印「汇总清单」：**2 页**，第 1 页填满、**第 2 页只印「打印日期」一行、留白 243.7mm**。

**根因**：纸面内容比一页只多 ~12mm，多出来的全是「屏幕上舒服、纸上是纯浪费」的内边距：

| 项 | 屏幕上的值 | 纸上的代价 |
| --- | --- | --- |
| 表格单元格纵向内边距 | `py-1`（每格上下各 4px） | 31 行的汇总表 ≈ **33mm** |
| 单据 `article` 内边距 | `p-4`（每边 4.2mm） | ≈ **8mm**（`@page` 已有 12mm 页边距，这层是多余的） |
| 尾部「打印日期」上边距 | `mt-4`（4.2mm） | 把最后一行顶到下一页 |
| 「无日期的待发放记录按当前年份显示」 | 恒定印一行 | 没有待发放时纯占位（本例正好是压垮第一页的最后一行） |

**量法坑（本轮记下来）**：真实打印的内容宽是 **703px（186mm）**，比屏幕按 A4 宽 794px 量的更窄
⇒ 换行更多、纸面更高。只按屏幕 DOM 高度量会得出「956px < 每页 1032px，1 页装得下」的错误结论
（本轮确实先被误导过一次），**分页必须在真 PDF 上量**。

**修法**（全部落在 `src/styles.css` 打印协议一处，屏幕观感零变化）：`th/td` 纵向内边距 → 1px；
`article { padding: 2mm 3mm }`；尾部段落 `margin-top: 1mm` + `break-before: avoid`；
`payment-sheets.tsx` 的那句提示改成只在 `breakdown.pendingCount > 0` 时印。

**实测（真 A4 PDF，1.8.12 → 1.8.13）**：

| 数据 | 1.8.12 | 1.8.13 |
| --- | --- | --- |
| 30 人 / 140 笔 / 70 代发 · 汇总清单 | 2 页（第 2 页留白 243.7mm，只印「打印日期」） | **1 页**（第 1 页余 46.5mm） |
| 45 人 / 45 笔 · 明细清单 | 6 页 | **5 页**（前 4 页仍 0.1mm 填满） |
| 工资条（9 个月考勤 + 14 笔打款，2 人） | 4 页（每人多一页只印「领款人签字」） | **2 页** |
| 2 人 / 26 笔 · 明细清单 | 2 页（第 1 页留白 25.7mm，尾部 1 行掉页） | 2 页（第 1 页 **0.1mm 填满**，尾部 = 口径行 + 打印日期）※ 纸面 ~285mm，纸真不够 |
| 报销单 / 保险清单 / 合同对账单 | — | 页数不变或更满；合同对账单仍「一份合同一整条」 |

**结论**：「内容只比一页多一点点 → 第 2 页只印一行」这类浪费已经压掉；剩下的 2 页情况都是内容本身
确实超过一整页（例：285mm 对 273mm 的可打印高度）。
