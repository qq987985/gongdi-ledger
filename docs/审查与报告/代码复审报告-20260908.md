# 代码复审报告（对《代码审查纪要》1.6.9 处理结论的核验 + 全面复审）

> 复核时间：2026-09-08　复核对象：提交 `8f6fbca`「fix: 按《代码审查纪要》逐条处理（1.6.9）」之后的全部源码
> 验证：`tsc --noEmit` 0 错误；`vite build` 构建通过（933ms）

---

## 一、纪要「已修」清单核验

| 纪要条目 | 核验结果 | 说明 |
|---|---|---|
| ① 严重1 依赖锁版本 | ❌ **未修，且结论失实** | `package.json` 仍有 15+ 个依赖写 `"latest"`（react、zustand、xlsx 之外的几乎全部）。提交说明里已承认「保留 latest」，但纪要处理结论写「✅ 已修…按当前实际安装锁定」、`VERSION.txt` [1.6.9] 更新说明写「不再写 latest，构建可复现」——**两处文字与事实相反**，面向用户的更新记录在说谎。缓解：`pnpm-lock.yaml` 已入库，部署侧用 `pnpm install --frozen-lockfile` 可复现。 |
| ② 严重2 README 版本同步 | ⚠️ **修了但没修完** | README 从 1.3.18 改到 1.6.8，但同一提交把版本升到 **1.6.9**——README「当前版本」和版本历史节又落后一个版本，同类问题当场复发。 |
| ③ 中4 replaceContracts 清理孤儿条目 | ✅ 已修且正确 | `store.ts` L464-469，按 `keepIds` 过滤 entries，与 `removeContracts` 口径一致。 |
| ④ 中5 upsertPerson 空 id 防御 | ✅ 已修且正确 | `store.ts` L333-341，`pid = nextP.id?.trim()`，匹配与新增分支都用了 trim 后的值。 |
| ⑤ 中6 删除 filter(Boolean) | ✅ 已修 | `nas-fs.server.ts` L764。 |
| ⑥ 建议9 错误信息放宽 500 字符 | ✅ 已修 | `update.server.ts` L377、L455 均为 500。 |
| ⑦ 建议12 工资月末匹配 | ✅ 已修（升级为月末） | `wage.ts` L60-61 用 `new Date(year, month, 0).getDate()` 取当月最后一天。属行为变更（29/30/31 号生效当月生效），更新说明已写明，合理。 |

## 二、纪要「无需改 / 不建议改」判断核验

| 纪要条目 | 核验结果 |
|---|---|
| 建议8「纪要所指代码已不存在」 | ❌ **判断错误**：`excel.ts` L825、L873、L880、L888 共 4 处 `as Parameters<typeof normalizeEntry>[0]` **仍然存在**。如不改，纪要应写「不处理」而非「已不存在」。 |
| 建议10 Label 与 Input 同文件 | ✅ 属实，`input.tsx` 确实导出 `Label`，是有意设计。 |
| 中3 any 类型「全库约 10 处」 | ⚠️ 计数偏低：手写代码里 `as any` / `Promise<any>` 实际约 25 处（不含自动生成的 `routeTree.gen.ts`）。不改的决策本身可接受（tsc 0 错误），但纪要低估了近 3 倍。 |
| 建议7 authOp 返回类型 / 建议11 Link as any | ✅ 现状与纪要描述一致，不改属合理取舍。 |

---

## 三、本次复审新发现的问题

### 🔴 新-1 `removeMember` 存在跨台账越权（建议尽快修）
- **文件**：`src/lib/accounts.server.ts` L559-568
- **问题**：`addMember`/`setMember` 在操作目标台账前有二次校验（L541：`me.role !== "admin" && book.ownerId !== me.id && !hasPerm(permsOf(me, book), "members.manage")` → 403），**`removeMember` 缺这一段**。`canManageMembers()` 只校验了「当前台账」的权限，随后 `body.id` 可指定**任意**台账 id。
- **后果**：任何在某台账拥有「分配权限」权限的成员（预设「全部权限」即含此项），可以对服务器上**任何其他台账**移除其成员（创建人除外）——把别人台账的成员全部清空，受害者无法登录访问自己的协作成员配置。
- **修复**：把 L541 的校验同样加到 `removeMember` 分支。

### 🟡 新-2 `upsertPolicy` 保单号去重条件与注释相反
- **文件**：`src/lib/store.ts` L533-535
- **代码**：`let i = list.findIndex((x) => x.id === p.id); if (i < 0 && p.id) i = list.findIndex((x) => x.policyNo && x.policyNo === next.policyNo);`
- **问题**：注释写「新增时按保单号去重」，但 `&& p.id` 意味着**只有调用方传了 id 才按保单号去重**；纯新增（无 id）时同保单号照样会重复建档。要么去掉 `&& p.id`，要么改注释。

### 🟡 新-3 密码哈希为无盐 SHA-256
- **文件**：`src/lib/auth.ts` L88-92
- **问题**：`sha256("gongdi-ledger::" + 密码)`，无盐、无慢哈希。`accounts.json` 一旦泄露（NAS 目录误共享、备份外传），弱密码可被秒破。
- **建议**：改为 `crypto.scrypt`（Node 内置，带随机盐）。自托管小工具场景可接受现状，但值得排期。

### 🟢 新-4 `GET /api/update` 无鉴权
- **文件**：`src/routes/api/update.ts` L21-29
- **说明**：任何人可查版本信息并触发服务端外联 GitHub（有 2 分钟缓存，危害有限）。低危，建议加登录校验。

### 🟢 新-5 多端同时编辑会互相覆盖
- **文件**：`src/lib/nas-sync.ts` L122-133
- **说明**：任何状态变更 500ms 防抖后**全量 PUT** 整本台账，无版本号/etag。两个浏览器同时操作会互相覆盖且无任何提示。当前架构下的已知取舍，建议在 README「使用说明」注明「避免多人同时编辑」。

---

## 四、结论

1. 纪要宣称的 7 项修复中，**5 项属实且质量良好**；**① 依赖锁版本实际未修**（提交说明已自认，但纪要结论和 VERSION.txt 更新说明写成「已修/不再写 latest」，必须更正文字或真修）；**② README 版本当场又脱节**（1.6.8 vs 实际 1.6.9）。
2. 纪要「无需改」中**建议 8 的判断错误**，所指代码仍在。
3. 新发现 **1 项权限漏洞（removeMember 越权，建议尽快修）**、2 项中等问题、2 项低危/取舍项。
4. 整体状态健康：tsc 0 错误、构建通过、服务端权限校验体系（withTenant + perms）设计完整，账户/会话/限速/原子写等加固到位。

---

## 五、终审（2026-09-08，对提交 `2830e52`「按本报告核验修正（1.6.10）」）

### 修复核验：全部落实 ✅

| 报告条目 | 终审结果 |
|---|---|
| 新-1 removeMember 跨台账越权 | ✅ 已修：补上与 addMember/setMember 相同的台账二次校验（accounts.server.ts L600-602），且 adminFromOldLedger 旧格式哈希经 verifyStoredHash 兼容路径可正常登录。 |
| 新-2 upsertPolicy 去重条件 | ✅ 已修：去掉 `&& p.id`，注释同步改写。 |
| 新-3 无盐 SHA-256 | ✅ 已修：服务端改 scrypt+随机盐+timingSafeEqual，登录/验证成功后旧格式静默升级，改密/重置/新建全走新格式。 |
| 新-4 更新接口无鉴权 | ✅ 已修：GET /api/update 在开启账户时要求登录。 |
| 新-5 多端覆盖提示 | ✅ 已修：README 已注明「多设备请勿同时编辑」。 |
| ①/② 文字失实 + README 版本 | ✅ 已修：VERSION.txt [1.6.9] 节改写为真实情况；README 同步 1.6.10，VERSION.txt/package.json/app/VERSION.txt 四处一致。 |
| 建议 8 excel.ts 断言 | ✅ 已修：提取 `EntryInput` 别名，4 处断言消除（grep 复核为 0）。 |

验证：`tsc --noEmit` 0 错误；`vite build` 通过（1.03s）；工作区 git 干净。

### 终审新发现（本轮修复引入的边界回归，1 项）

- 🟡 **改密/重置密码未 trim，与登录校验口径不一致**：`scryptHash` 不去空格，而登录时 `password = (body.password||"").trim()` 后再比对。`setup`/`createUser` 显式 trim 了，但 `changePassword`（L447）和 `resetPassword`（L465）直接 `scryptHash(body.password)`（未 trim）；客户端（settings.tsx L706、L848）也原样发送。若密码首尾带空格（手机输入法常见尾随空格）被原样存入，登录时因 trim 永远不匹配 → **账户锁死**。旧代码靠 `hashPassword` 内部 trim 规避了此问题。
  - **修复（一行×2）**：在 `scryptHash` 和 `verifyStoredHash` 开头各加 `raw = raw.trim();`，恢复与旧 `hashPassword` 完全一致的口径（改密时的「当前密码」比对同样依赖此口径）。

### 终审结论

1.6.10 修复质量高，报告所列问题全部闭环；仅剩上述 1 项一行可修的 trim 口径回归，修后即可发布。

**终审收尾（1.6.11，提交 `9176400`）**：trim 口径回归已按建议修复（`scryptHash`/`verifyStoredHash` 入口统一 trim，且空串防御到位）；VERSION.txt / README / package.json / app 副本同为 1.6.11；`tsc --noEmit` 0 错误、`vite build` 通过、git 干净。**全部问题闭环，可发布。**


---

## 六、复审核验（2026-09-08，对提交 `1.6.11`）

- ✅ 终审发现的 trim 口径回归已修：`scryptHash`/`verifyStoredHash` 开头统一 `raw.trim()`，与旧 `hashPassword` 口径一致（改密「当前密码」比对、登录、重置全部同口径）。
