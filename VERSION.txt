1.8.16

[1.8.16] 2026-09-18
- 修复启动同步竞态：确认身份、权限和缓存归属后才同步，避免旧缓存写入另一套空台账；认证失败可重试，不误入本地模式。
- 修复审计并发覆盖：追加、修改、删除统一排队，删除先留痕；损坏记录按台账隔离并拒绝覆盖。
- 加强附件安全：服务器拒绝访问时不读取旧册本机缓存；按响应类型预览，阻止 XML 伪装 PDF；空文件上传不覆盖原附件。
- 优化年度统计：相同合成数据完整结果一致，1,000 人规模的统计函数中位耗时约 727ms → 12ms。
- 修复目录初始化并发和失败重试、表格拖动与存储异常；完善预览焦点操作，修正考勤月表合计金额挤列。
- 重整开发规范、智能体入口和 9 类专家职责，保留历史决策与唯一待办清单；镜像和 Windows 发布新增同一提交的质量门禁。
- 验证：650 项回归测试、70 项 Excel 往返、类型检查和构建通过；已完成桌面/手机浏览器及 12 项打印检查。
- 本次不更改生产数据格式；保留现有业务口径。NAS/Windows 现场升级效果需在实际环境确认。

[1.8.15] 2026-09-17
- **用户拍板的「功能四件 + 依赖锁版本」**（业务评估/架构评审里排后续的项），另含两处**评审没抓到、做恢复入口时才暴露**的数据缺口。
- **① 备份「恢复」：从没有入口 → 有入口且真能落回**（业务评估 B4）
  · 设置页「数据」卡片新增「从备份恢复」：写清会恢复什么 / 不在 Excel 口径内（影像·账号·操作记录）/ 恢复前先备份 /
    恢复后核对清单 / 合并语义；按钮直达整本导入（不另写一套导入）；只读或缺 `import.use` 权限时禁用并写明原因
    （唯一实现 `src/lib/restore.ts` + `readonly.ts` 的 `blockedImport()`）。
  · **顺带修掉一个一直存在的数据缺口**：整本导入**从来不解析合同** → 拿备份恢复会**整块丢合同**（界面却说导入完成）。
    现在整本导入解析合同 5 张表（复用合同导入的同一实现），并给合同导入加了幂等（同一份备份导两次不翻倍、
    不会用旧备份冲掉现场改过的合同）。
  · **同型「假成功」一并堵掉**：7 个导入入口（人员/考勤/发放/报销/合同/保险人员/整本）在写盘前统一过 `blockedImport()`，
    只读账号不再「界面说导入完成、服务端 403」；真实路由断言：被拒时 `ledger.json` 一字节未变、403 原因可读。
- **② 批量「按应发生成待发放」**（业务评估 B15）：30 人发工资不用再开 30 次弹窗 + 30 次确认。
  · 先出预览（名单 / 每人金额 / 合计 / 跳过几人）再二次确认后落盘；
  · 金额口径 = **未发（应发 − 已发）**（用户拍板），与年度表「未发」同源；已有该人该年的待发放记录自动跳过（幂等）；
    0 元 / 已结清 / 无应发不生成；发放方跟随当前「发放方」筛选；
  · 整批只写 **1 条**操作记录。
- **③ 操作记录留「改动前 → 改动后」**（业务评估 B17）：原来只记条数，误改误删无法凭记录重建。
  · 新模块 `src/lib/audit-diff.ts` 是唯一序列化/截断实现（金额走 `round2`、日期走 `dates.ts`、
    敏感字段只记「已填（值不记录）」、无变化不写标记、超长截断）；关键改写/删除动作（人员改名与工资、发放、考勤、合同、报销）
    全部改走它；操作记录页内容列可折叠展开看前后值。
- **④ 三处打印入口补齐**（业务评估 B16）：考勤月表、全年 12 个月、年度工资汇总、人员名单。
  · 新 `src/components/ledger-print-sheets.tsx` + `src/lib/attendance-month.ts` + `src/lib/print-cols.ts`；
    严格按 `src/styles.css` 的打印分页协议：抬头写进 `thead` 首行（续页重复、拆开也认得出）、合计只进 `tfoot`、
    `table-fixed` + 列宽加满 100%（12 人带长备注从 2 页压回 1 页）；打印入口**是只读操作**，不受编辑权限限制。
  · 真浏览器 PDF 实测：本月月表 **1 页**（底空 65.5mm）、全年月表 **3 页**（0.1 / 0.1 / 47.4mm，每页首行都是
    「2026年M月 · 考勤月表 · 本表 12 人」）、年度工资汇总 **1 页**（130mm）、人员名单 **1 页**（91.9mm）；
    无整页空白、无横向裁切（最右文本 545pt ＜ 可打印区 561pt）。
  · **人员名单不印身份证/银行卡**（纸面会贴墙）；年度工资汇总只印年度列（12 个月列在 A4 竖版放不下，
    按月明细请用「打印全年月表」）。
- **⑤ 依赖锁版本**（用户拍板；架构/工具链评审 C4②）：`pnpm-lock.yaml` 原本就在库里，缺的是另一条腿 ——
  21 个依赖里 **19 个写 `latest`**。现在**全部改精确版本**（含别名 `npm:rolldown-vite@7.3.1`），
  lockfile 重生成（`packages:` 段逐字节不变，只改 21 行 `specifier`），`pnpm install --frozen-lockfile` 通过。
  新增守卫 `tests/dependency-pinning.test.ts`（禁 latest/^/~/范围；lockfile 与 package.json 逐字一致；
  node_modules 实装一致；lockfile 未被忽略且在库里；CI 真用 frozen；文档口径），并用隔离副本上的 4 个坏样本证明「不是假绿」。
- **测试 543 → 611**（611 pass / 0 todo）；Excel 往返 **70/70**；四道闸 + 构建戳校验（app/ 与源码指纹一致）。
- **已知取舍**：年度工资汇总打印件不印 12 个月列（要横向须改共享 `@page` 协议）；月表打印件不印「加班规则（人员表）」
  长文本列（实测会把 12 人撑到第 2 页）；人员名单不印身份证/银行卡。

- **发版链路今天实测到的两次「静默失败」**（都已修 + 加守卫，教训留给下一次发版本的人）：
  · **Release 没挂上 Windows 包**：工作流的 Release 步骤写 `files: gongzi-windows.zip`，而 `win/pack.sh` 写的是 `gongdi-windows.zip`
    —— 差一个字母。`softprops/action-gh-release` 匹配不到文件时**只打一行警告、照样报成功**：
    Release 建出来了、镜像也推上去了、**zip 一个字节都没挂**（用户根本下载不到 Windows 包）。
    已改正模板并在 `ci/README.md` §1b 补「粘贴后自检（两个 zip 名必须一模一样）」；新增守卫
    `tests/deploy-guards.test.ts`「Release 挂的 zip 名必须与 `win/pack.sh` 的 `OUT=` 逐字一致」。
    1.8.15 的 zip 已**手工补传**（本机 `sh win/pack.sh` 产出，与提交的 `app/` 同源）。
  · **本地推不动 `.github/workflows/`**：本机 token 只有 `gist/read:org/repo`、没有 `workflow` 权限 ——
    线上两份只能在 GitHub 网页粘贴；所以守卫只断言 `ci/` 下的模板（断言线上那份会让 CI 直接红死）。
- **文档同步**：`AGENTS.md` / `开发规范.md` / `README.md` / `docs/README.md`（待办改成「1.8.15 之后」、
  勾掉 B4/B15/B16/B17 与依赖锁版本、记上你已完成的两项）/ `使用说明书.md` §5（4 个新打印入口）/
  专家评审汇总新增 §10「处理状态（1.8.15）」。

[1.8.14] 2026-09-17
- **8 位专家评审后的批量修复**（评审汇总 `docs/审查与报告/专家审查/00-汇总-专家评审-20260917.md`，含逐条证据与 §9 处理状态）。
  按评审的 A/B 分级落地，覆盖**数据安全、业务正确性、现场数据丢失、前端可见性、可观测性与交付**五类。
- **数据与安全（A 级）**：
  · **跨册静默回落**（`accounts.server.ts`：显式请求的册子不可访问时改用用户第一本，读写都落错册）→ 改判 **404**，
    只有「没指定」才回落；客户端 404 提示「请重新选择台账」并锁住推送、清本机缓存；
  · **PUT 版本号与 GET/CAS 不同源**（响应头用请求体算、CAS 用服务端读视图，且读视图会就地补 `scanFileName`）
    → 客户端存下错误基准，**下一次保存必 409** 并诱导「以本机覆盖」丢别人改动 → 新增 `writeLedgerEx`，
    写后在串行队列里按**服务端读视图**算版本号，GET/响应头/CAS 三者同源；
  · **台账 `accessHash` 可当管理员凭据**（无盐 sha256、`people.view` 可读）→ 首启不再由它建管理员、
    读视图不返回、写盘丢弃并留痕、schema 去掉该字段；已是旧 hash 的账号仍能登录并静默升级为 scrypt；
  · **4 个写接口鉴权在读 body 之后**（未认证灌 40MB 先吃内存）→ 抽出 `gateTenant` 把鉴权前置，
    权限位与 4xx 语义不变（守卫断言未登录时 `request.bodyUsed === false`）；
  · **审计可被无痕批量删除、影像删除零审计** → 删除前先 `appendAudit` 留痕（新记录 id 服务端生成、
    不会被同一次请求删掉），doc/photo 删除补服务端审计；
  · **在途 pull 无代际/互斥**（迟到的旧册响应覆盖内存、随后整本 PUT 把旧册数据推上去，架构师已复现）→
    代际号 + 串行队列 + 唯一入口 `switchBook/createBookAndEnter/deleteBook`，迟到响应整包丢弃（内存/本机缓存/推送三处都断言）；
  · **`flushPendingLedger()` 从不返回结果**（切册/换年份照旧可能丢本机改动）→ 改为返回 `ok|skipped|failed`，
    切换入口读结果、失败即中止并提示（默认取消，避免把用户困在不可用册子里）。
- **业务正确性（A/B 级）**：**姓名 trim 收敛**（唯一 `nameKey/ownerKey`，年度表不再把一个人拆成两行、出勤不再静默归零）；
  **发放/合同金额入库 `round2`**（打印件不再出现「已发 0.01 + 待发 0.01 = 0.01」）；
  **`round2` 边界修死**（负数对称、大额、半分容差 1e-6，NaN/Infinity 回 0、消 -0）；
  **工资条收款人判定收敛**到 `receiverOf`（第 3/4 套裸比较清零）；**保单期未填不再静默 0**（屏幕与打印都写明原因）；
  **负出勤天数**输入即时警告 + 月表警示条 + 保存拒绝；**备份内容校验**（非空垃圾/截断 xlsx 也 400 且不动已有备份）。
- **现场数据丢失（业务评估 P0）**：
  · **考勤未保存离开被静默丢弃** → 一处实现（`src/lib/unsaved.ts` + `components/unsaved-guard.tsx`）盖住
    所有页内跳转（`useBlocker`）、浏览器返回/刷新/关标签（`beforeunload`）、换台账/新建/换年份；
    只读账号不拦；真浏览器 390×844 **9 项实测全过**；
  · **备份 Excel 漏报销、合同不在任何备份里** → 备份工作簿补齐**全部实体**（含合同 5 张表 + **所有年份**考勤），
    并在设置页回显「已备份 N 人 / M 条考勤 / K 笔发放 / J 条报销 / …」；
  · **改名导致考勤/发放脱钩** → 改名改事务：考勤 `name`、发放 `owner`、`receiver` 等于旧名者、
    参保人 `name`、报销 `claimant` 一起改（**不碰**物品名与自由文本），同名/空名整笔拒绝，记操作记录。
- **前端可见性**：查询页只读入口按权限传 `readOnly`（进守卫扫描清单，修掉假绿）；7 处写操作补 `catch` + `toast.error`；
  新增**未同步横幅**与 401 单独文案；保存成功后复位脏标记；内层预览自己吃 Esc（不再连带触发未保存确认）；
  409 弹窗文案写明「取消/Esc ＝ 放弃本机改动」。
- **可观测性与交付**：日志**一套实现**（抽出 `scripts/log-core.mjs`，启动器与 `src/lib/log.server.ts` 共用，
  超限**滚动 `.log.N`** 且受保留策略管辖）；`SLOW_MS` 未设置时默认 2000（原来解析成 0 → 每个请求都记慢请求）；
  `Dockerfile` 改 `exec node`（PID 1 = node）+ `HEALTHCHECK` + 启动器 SIGTERM 优雅退出；
  **一键更新加 `waitReady()`**（容器真就绪才删老容器/旧镜像，失败删新容器并拉起老容器、日志写明）；
  Windows 启动器自适应「安装根 vs `win/`」；**CI 第四道闸改硬失败**（`cmp` 三个复制件 + 源码指纹
  `scripts/build-stamp.mjs`）+ `ci/docker.workflow.yml` 让 docker job 构建前自行 `pnpm build`。
- **结构红线有了机械守卫**：新增 `tests/structure-guards.test.ts`（禁环 + 三条主线方向 + `types.ts` 必须是叶子；
  白名单只有 §12.3 允许的 `update/docker ↔ update/log`，且要求写明原因、注释里真有那段说明）；
  机械消掉两个类型环（`types ↔ contracts`、`assets.server ↔ nas-fs.server`）；
  新增 `tests/min-hits.ts`（命中数下限 + 坏样本自检）接进 6 个扫描类守卫，并加元守卫「扫描类守卫必须用 min-hits」
  —— 治「正则 0 命中也是绿」的假绿；新增 `tests/version-consistency.test.ts` 比对**版本四处一致**；
  `package.json` 补 `engines.node >= 22.18`；删掉死代码 `ExpenseSheets`（旧版式，真在用的是 `routes/expenses.tsx`）。
- **测试 392 → 543**（543 pass / 0 todo）；Excel 往返 **70/70**；四道闸 + **构建戳校验**（app/ 与源码指纹一致）。
- **两件需要你做**：① GitHub 网页粘贴 `ci/check.workflow.yml` → `check.yml`、`ci/docker.workflow.yml` → `docker.yml`
  （本地无 workflow 权限，见 `ci/README.md` §0）；② Windows 真机验证「解压 → 双击根目录 `启动.bat` → 应用内更新 → 服务是否自己回来」。
- **已知取舍**：`round2` 用 1e-6 容差（真值 `8.074999x` 也会进位成 8.08）；负出勤天数行仍不算「有内容」
  （年度表会漏该人，但界面已醒目提示 + 保存拒绝）；切年份/换主题不再上传 `year` 字段（纯本机视图状态）；
  `accessHash` 之后「开机口令」只存各设备本机（不再随台账跨设备同步）。

[1.8.13] 2026-09-16
- **打印省纸收尾（用户截图：打印预览「那么大空白，竟然分了 2 页」）**。
- **复现**（把用户数据复刻成 30 人 / 140 笔发放、其中 70 笔代发）：发放「汇总清单」
  **2 页**，第 1 页填满，**第 2 页只印了「打印日期」一行、留白 243.7mm**。同一份数据在
  真实 A4 PDF 上量：纸面内容 ~285mm，比可打印高度 273mm 只多 ~12mm —— 差的正好是
  「屏幕上舒服、纸上是纯浪费」的那些内边距。
- **量法提醒（本轮踩到的坑）**：真实打印的内容宽是 **703px（186mm）**，比屏幕上按 A4 宽 794px
  量的更窄 ⇒ 换行更多、纸面更高。所以分页只能在**真 PDF** 上量（`ci/mobile-print-check.mjs pages`
  + `ci/print-pdf.mjs`），不能只看屏幕 DOM 高度（本轮 DOM 量出 956px 以为 1 页装得下，实际装不下）。
- **修法**（都在 `src/styles.css` 打印分页协议一处，屏幕观感零变化）：
  · **②d 行高压缩**：`.print-only table th/td` 纵向内边距 → **1px**（横向不动，可读性靠列宽）。
    实测 31 行的汇总表光行内留白就吃掉 ~33mm —— 这就是多出来的那 12mm 的主要来源；
  · **②e 纸面留白压缩**：`.print-only article { padding: 2mm 3mm }`（`@page` 已有 12mm 页边距，
    单据再 `p-4` 等于白占 ~8mm）+ 尾部段落 `margin-top: 1mm` + **`break-before: avoid`**
    （「打印日期」不许单独另起一页）；
  · **空提示行不再印**：`payment-sheets.tsx` 里「无日期的待发放记录按当前年份显示」只在
    **真有待发放记录**（`breakdown.pendingCount > 0`）时才印 —— 没有待发放时它纯占一行，
    实测正好是把「打印日期」挤到第 2 页的临界量。
- **实测（真 PDF，改动前 → 改动后）**：
  · 30 人 / 140 笔 / 70 代发 · 汇总清单：**2 页 → 1 页**（第 2 页那 243.7mm 的空白页消失，第 1 页余 46.5mm）；
  · 45 人 / 45 笔 · 明细清单：**6 页 → 5 页**（前 4 页仍页页填满 0.1mm）；
  · 9 个月考勤 + 14 笔打款的工资条（2 人）：**4 页 → 2 页**（改前每人多出一页只印「领款人签字」一行）；
  · 2 人 / 26 笔 · 明细：仍 2 页（纸面内容 ~285mm，纸真的不够），但第 1 页由「留白 25.7mm」变成
    **填满 0.1mm**，第 2 页只剩口径行 + 打印日期；
  · 报销单 / 保险清单 / 合同对账单：页数不变或更满（合同对账单第 2 页留白 143.2 → 148.8mm，两份合同仍各自整条）。
- **测试 390 → 392**（392 通过 / 0 todo）：`tests/ui-guards.test.ts` +2 条 ——
  ① 打印态必须压行高（`th/td` 1px）与纸面留白（`article { padding: 2mm … }` + 尾部 `break-before: avoid`）；
  ② 「无待发放不印那句提示」必须用 `breakdown.pendingCount` 包住（都带坏样本自检）。
  Excel 往返 **70/70**（未动 Excel 相关代码）。
- 四道闸：`pnpm run typecheck` 0 错误；`pnpm test` **392/392**；`pnpm run test:roundtrip` **70/70**；`pnpm build` 成功。

[1.8.12] 2026-09-16
- **打印口径细化（用户追加）**：「能单独一页的就不要和别的混到一起」＋
  「**如果第一页有空白，第二条能全部打到第一页就 2 条打到一起，反之就另开一页，后边依次类推**」。
- **规则**：**一条 = 一个人 / 一份单据**（发放明细里每个人的整节、合同对账单里每份合同）——
  **整条放得下就与上一条并排塞满、放不下才整条另起一页，绝不把一条劈到两页**；
  整条比一整页还高时（例：一份合同几十条明细）浏览器会让它自己跨页续排（用户「可以拆开分发」，允许）。
  唯一实现：`.print-doc` + `src/styles.css` 的 `.print-only .print-doc { break-inside: avoid }`；
  **不许**给 `.print-doc` 用 `break-before: page`（那是「宁可留白也不并排」，与口径相反），守卫会拦。
- **为什么 1.8.10 当时反而把这个删了**：那时**还有两个「无缘无故提前断页」的根因没修** ——
  外壳 `.app-bg` 的 `min-h-screen/min-h-dvh` 在打印媒体里撑满一屏、打印表格没有跨页表头，
  于是「整块被推走」的空白被放大成用户报的 112.3mm。**根因修掉之后**再把「整条不拆」加回来：
  实测三组数据**页数与 1.8.11 完全一致**、第 1 页依旧填满（留白 0.1mm），而**人的记录不再被拆页**。
- **实测（真浏览器出 A4 PDF，量每页文字结束处留白）**：
  · 2 人 / 26 笔发放（用户那条）：明细 **2 页**，第 1 页留白 **0.1mm**（第 1 个人的 13 笔整节刚好填满）、
    第 2 页是第 2 个人整节（97.4mm）——**没有任何人被拆到两页**；
    合同对账单 3 页：第 1~2 页是第一份合同（它比一整页高，自己续排），第 3 页是第二份合同**独占一页**
    （改前是「第一份的尾巴 + 第二份的开头」混在第 1 页）；
  · 3 人 / 27 笔：明细 **2 页**，第 1 页 0.1mm（两个人整节）+ 第 2 页第 3 个人整节（142.4mm）；
  · 45 人 / 45 笔：明细 **6 页**，前 5 页留白 **0.1mm**，尾页 136.1mm（比 1.8.11 的 172mm 更满）；
    保险 2 页、汇总 2 页、报销单 2 页，页数与 1.8.11 相同。
- **测试 389 → 390**（390 通过 / 0 todo）：`tests/ui-guards.test.ts` +1 条 ——
  ① 发放明细「每个人的整节」与「合同对账单每份合同」必须带 `.print-doc`；
  ② 打印态 `.print-doc` 必须 `break-inside: avoid`，且**不许**出现 `break-before: page`（带坏样本自检）。
  Excel 往返 **70/70**（未动 Excel 相关代码）。
- 四道闸：`pnpm run typecheck` 0 错误；`pnpm test` **390/390**；`pnpm run test:roundtrip` **70/70**；`pnpm build` 成功。

[1.8.11] 2026-09-16
- 打印分页第二轮（用户口径：「除非放不下下一个人或下一条记录才允许大空白；能塞下就省纸；**我可以拆开分发**」）：
  在 1.8.10（去掉容器级不拆页 + 跨页表头 + 打印态一屏高清零）之上，把**全部打印入口**用真浏览器出 A4 PDF
  逐个量了一遍，修掉两处**拆开分发时会误读 / 认不出**的问题。
- **①【口径·合计每页重复】浏览器会把 `tfoot` 合计行印在每一页页脚**。
  复现（45 人台账走真实入口）：发放「汇总清单」2 页 —— **第 1 页只有前 32 人，页底却印着
  「总计（全部实际收款人）46 笔 ¥343,500.00」**；保险合同清单 2 页 —— 第 1 页只有 29 人，
  页底印着**全单**合计 10,691.53。根因：`tfoot` 的 UA 默认值是 `table-footer-group`，
  分页媒体下浏览器按规定在**每一页**页脚重复它。
  修法（`src/styles.css` 打印分页协议 +1 条）：`.print-only tfoot { display: table-row-group }`
  → 整单合计**只在最后一页印一次**。
  实测（同一份 PDF 按文字位置统计）：汇总「总计」第 1 页 **1 处 → 0 处**、第 2 页仍 1 处；
  保险「合计」第 1 页 **1 处 → 0 处**（第 2 页 2 处：明细表 + 按班组汇总表，都合法）。
- **②【可用性·续页认不出是哪张单】单据抬头原来只印在第 1 页**。
  复现：保险合同清单第 2 页首行只剩列标题「序号 姓名 队长 开始日期 结束日期 使用天数 保费(元)」；
  合同对账单第 2 页首行只剩「收款」两个字；报销单第 2 页首行只剩列标题。
  用户要**拆开分发**，这些续页拿到手里不知道属于哪张保单 / 哪份合同 / 谁的报销。
  修法：把**单据抬头搬进 `<thead>` 第一行**（表头本来就跨页重复）——
  · 保险：`团体保险人员清单 · PICC-2026-0001 · XX小区二期团体意外险 · 队长：… · 在保`
    （明细表与「按班组汇总」两张表都带；唯一实现 `policyCaption()`）；
  · 合同对账单：每张小表带 `合同对账单 GD-2026-001 · 开票`（`MiniTable` 新增 `caption`）；
  · 报销单：表头带 `报销人：… · 收款人：… · 开户行：… · 打款账户：…`
    （原来那张独立的小表合并进表头，第 1 页行数不变）。
  效果：**第 1 页不多占行**（抬头原来也在），只有续页顶上多一行（≈4mm，续页本来就空着）。
- **判据与量法**（`ci/mobile-print-check.mjs pages` + `ci/print-pdf.mjs`，零依赖、不进构建、CI 不跑）：
  真浏览器（本机 Chrome + playwright-core）按 CSS 的 12mm 页边距出 A4 PDF，再用自写的 PDF 内容流解释器
  算「这一页**文字**印到哪儿为止」→ 每页底部留白 mm（单据外框/表格线不计，免得把边框当内容）。
  同一份数据、同一把尺子对拍（2 人 / 26 笔发放 + 40 报销 + 40 条合同明细 + 40 参保人）：
  · 发放明细（全部人）：第 1 页留白 **121.3mm → 7.0mm**（仍 2 页；第 2 页 122.4 → 226.6mm，
    即内容整体前移、第 1 页填满，**总页数不变**）；
  · 合同对账单：第 1 页 **71.3 → 7.0mm**、第 2 页 **21.1 → 2.7mm**；
  · 报销单 / 保险清单 / 汇总清单 / 工资条：页数与留白**无回退**（报销单因合并抬头少一行，仍 1 页）。
  45 人 / 46 笔的极端数据：发放明细 6 页，**前 5 页留白全部 0.1mm**（页页填满），
  第 2~6 页顶端都有重复表头，无整页空白。
- **已知取舍（本轮不改，等用户决定）**：工资条仍是「一张或两张一页 + 裁切线」的设计 ——
  单人内容超过一页时（例：9 个月考勤 + 14 笔打款）会印成 2 页，**第 2 页只有「领款人签字」一行**
  （实测该页留白 268.3mm）。这是「一人一张、便于签字裁剪」的取舍；要改成续排需重排工资条版式。
- **测试 387 → 389**（389 通过 / 0 todo）：`tests/ui-guards.test.ts` +2 条守卫 ——
  ① 打印态必须 `.print-only tfoot { display: table-row-group }`（合计不许每页重复；带坏样本自检）；
  ② 保险 / 合同 / 报销三个打印件的**单据抬头必须落在 `<thead>` 里**（跨页重复、拆开也认得出；带坏样本自检）。
  Excel 往返 **70/70**（未动 Excel 相关代码）。
- 四道闸：`pnpm run typecheck` 0 错误；`pnpm test` **389/389**；`pnpm run test:roundtrip` **70/70**；`pnpm build` 成功。

[1.8.10] 2026-09-16
- 修**打印分页留白**（用户实测原话：「发放记录汇总，我测试了 26 条记录打印汇总就会分页，
  第一页还有很多空缺就会把尾巴放在第二页」）。**先复现、再定根因、再改**，全过程用真实浏览器出
  A4 PDF 量「页数 + 每页底部留白（mm）」。
- **复现方法与修改前的数字**（真实 Chromium + 构建产物 + 26/60 条发放数据；走页面上的
  「打印汇总 / 打印明细」，`emulateMedia('print')` 后 `page.pdf()` 由 Chrome 自己分页，
  再按页解析 PDF 内容流取「墨迹下沿 → 距 12mm 页边距的留白」，脚本落在 `browser-screenshots/`（未入库））：
  · 26 笔 / 2 人（13+13，含代收折行）**明细** = 2 页 · **112.3** / 130.8 mm；
  · 26 笔 / 5 人（13,5,4,2,2，含代收）**明细** = 2 页 · **50.9** / 107.3 mm；
  · 26 笔 / 26 人（各 1 笔）**明细** = 4 页 · 19.2 / 9.7 / 9.7 / 225.3 mm；
  · 60 笔 / 60 人 **明细** = 7 页 · 19.2 / 9.7×5 / 17.9 mm；
  · 60 笔 / 6 人（12,12,10,10,8,8）**明细** = 3 页 · 21.8 / 22.1 / 179.0 mm；
  · 26 笔 **汇总** = 1 页 · 30.6 mm（底纹外没有可断点，未分页）；
  · 60 笔 **汇总** = 2 页 · 4.9 / 54.9 mm；
  · 30 行 **汇总**（含代收折行）= 2 页 · 2.8 / **264.4** mm —— 第 2 页只剩「合计行 + 口径说明 + 打印日期」；
  · 真实入口（`ci/mobile-print-check.mjs` 的 49 笔 / 12 人种子）**发放明细** = 3 页 · 15.5 / 29.0 / 96.7 mm；
  · **合同对账单** = 2 页 · **50.1** / 75.0 mm。
- **根因（逐条确认，不是照猜改）**：
  ① `src/components/payment-sheets.tsx` 每个人的明细一节的 `<section className="mt-3 break-inside-avoid">`
     是**容器级**「整节不许拆页」：页底放不下就把**整节**推到下一页 → 上一页留一大片空白
     （上表 112.3mm / 50.9mm 就是它，节越高留白越大）；
  ② `src/components/shell.tsx:140` 外壳 `.app-bg min-h-screen min-h-dvh` **在打印媒体里也生效**：
     容器至少一屏高；且 `overflow-x-hidden` 在打印态等于 `overflow-y:auto` 的滚动容器，
     是「提前断页 / 多一张空白页」的经典成因（本机 Chrome 实测在 26 行汇总上没多出页，
     但只要内容跨页就会放大留白，按规范清零）；
  ③ 打印表格**没有跨页表头规则**（未显式声明 `thead { display: table-header-group }`），
     第二页起来源上没有任何保证。
  同类容器级不拆页还有两处：**合同对账单** `article.break-inside-avoid`（就是上表 50.1mm）、
  对账单 MiniTable 与保险「按班组汇总」的 `div.break-inside-avoid`。
- **修法（规则集中到 `src/styles.css` 的「打印分页协议」一处，组件里不许各写一套）**：
  · `@media print` 里把一屏高清零：`.min-h-screen / .min-h-dvh / .min-h-svh / .h-screen / .h-dvh
    → min-height:0`，外壳 `.app-bg { overflow: visible }`；
  · 「不许拆页」**下沉到行/小单元**：`.print-only tr { break-inside: avoid }`（一行不腰斩）、
    `.print-keep`（单据抬头这类小单元，推走也只丢十几毫米）；**容器级 `break-inside-avoid` 全部删掉**
    （`payment-sheets` 的明细节、`contracts` 的对账单 article + MiniTable、`insurance` 的按班组汇总 div），
    组件源码里 `break-inside-avoid` 已清零；
  · **跨页表头**：`.print-only thead { display: table-header-group }`（第二页起同样有表头，
    见下方实测），合计行留在 `<tfoot>`（一行，天然在表尾、不会被拆开）；
  · 小标题 `print-title`（`break-after: avoid`）只给**小表**用：实测把它加在明细大表标题上，
    浏览器会把「标题+表」当一组，每页反而多留 5~15mm 空白（21.8 → 14.7mm 的教训），故明细节标题不加。
- **修改后实测（同一套数据、同一把尺子）**：
  · 26 笔 / 2 人 明细 = 2 页 · **4.6** / 228.5 mm（112.3 → 4.6）；
  · 26 笔 / 5 人 明细 = 2 页 · **2.8** / 145.4 mm（50.9 → 2.8）；
  · 26 笔 / 26 人 明细 = 4 页 · **12.0 / 8.9 / 8.9** / 231.6 mm；
  · 60 笔 / 60 人 明细 = 7 页 · 12.0 / 8.9×5 / 24.2 mm（页数不变，7 页已是下限）；
  · 60 笔 / 6 人 明细 = 3 页 · **14.7 / 5.4** / 192.0 mm；
  · 26 笔 汇总 = 1 页 30.6 mm、60 笔 汇总 = 2 页 4.9 / 54.9 mm（本来就满，不变）；
  · 30 行 汇总 = 2 页 **0.0** / 264.4 mm；发放明细真实入口 = 3 页 **8.3 / 5.7** / 116.3 mm；
  · **合同对账单 = 2 页 0.1 / 100.7 mm（50.1 → 0.1）**；
  · 报销单 40 行 = 2 页 0.0 / 264.4 mm；保险清单 40 人 = 2 页 2.0 / 93.0 mm（不变）。
  · 打印内容**一个字没改**（口径、合计、代收标注、排序都照旧）：对拍 PDF 文本段数，
    `26 笔明细` 前后都是 4 页 2131 段、`60 笔汇总` 都是 2 页 982 段；`26 笔/2 人` 从 1053 → 1061 段，
    **多出的正好 8 段 = 一张重复的表头行**（明细表 8 列），即「跨页表头」生效的直接证据。
  · 跨页表头另有一处铁证：`60 笔汇总` 第 1 页表头行是 5 段（字体 `/F52 F53 F54 F59 F64`），
    第 2 页**第一行就是同样 5 段、同一组字体**；`26 笔明细` 第 2/3 页首行同样是 8 段、同一组字体。
- **同类排查（结论）**：**报销单打印、保险合同清单打印、工资条打印没有**「提前分页 / 第二页无表头」问题
  （报销单与保险清单本来就允许跨页、`thead` 是浏览器默认跨页重复；工资条是刻意的
  「一张/两张一页 + 裁切线」设计，`.payslip` 是唯一保留的容器级例外，属半页高的最小单元）；
  **合同对账单有**（容器级不拆页，第一页留白 50.1mm），已一并修；三个模块的打印 CSS 现在共用
  `styles.css` 那一处规则，不再各写一套。
- **测试**：`tests/ui-guards.test.ts` **+3 条守卫**（① 打印态一屏高必须清零且外壳 `overflow: visible`、
  打印表格必须有 `thead { display: table-header-group }`；② 打印件不许用 `min-h-screen/min-h-dvh`、
  **不许出现容器级 `break-inside-avoid`**（白名单只有 `tr` / `.print-keep` / `.payslip`）；
  ③ 坏样本自检：把旧写法贴回去必须变红），`开发规范.md` §6.6 打印协议同步修订
  （旧条文「汇总/合计类整块不拆、用 break-inside-avoid」正是本次缺陷的来源）。
- 测试 384 → **387**（387 通过 / 0 todo），Excel 往返 **70/70**（未动 Excel 相关代码）。
- 四道闸：`pnpm run typecheck` 0 错误；`pnpm test` **387/387**；`pnpm run test:roundtrip` **70/70**；
  `pnpm build` 成功（`app/` 已随构建重建）。


[1.8.9] 2026-09-16
- 落地 CTO 已拍板的**两条口径决策**（对应 A 组逐项功能测试报告第 22 项与「`attendance.edit` 整本写台账」）：
  **① 新增台账数量上限 `MAX_OWNED_BOOKS`（默认 5，普通成员自建限数量）；② `attendance.edit`
  整本写台账「保持现状」不改代码**（理由与决策记录见下）。
- **决策一（改代码）：只读/普通成员自建台账 —— 采用「限数量」方案**。
  背景：`POST /api/auth` 的 `op=createBook` 原来**无任何门禁**，任何登录用户（含只有 `*.view`
  的只读账号）都能**无限创建**台账，且自建台账里他自己是 owner、拿 `*`（等于绕过预设权限另开一套数据空间）。
  口径：**允许成员自建**（他需要有自己的数据空间，也看不到别人的数据），但**限制数量** ——
  **普通成员最多 5 本「自己作为 owner」的台账**；**管理员/超管不受限**；可用环境变量
  **`MAX_OWNED_BOOKS` 覆盖默认 5**（非法值回落 5，上限兜底 1000）。
  超限返回 **400 + 可读提示**「你已创建 5 套台账，已达上限；需要更多请联系管理员。」，
  **不创建成功、不写盘**；**只统计他作为 owner 的台账**（被管理员加为成员的不算）。
  唯一实现 `src/lib/book-quota.ts`（`maxOwnedBooks()` / `ownedBookCount()` / `ownedBooksLimitMessage()`），
  服务端门禁在 `accounts.server.ts` 的 `createBook` 分支、且**在 `data.books.push()` 之前**。
  前端两个新建入口（设置页「我的台账」卡片、左侧台账下拉的「＋ 新建一套台账」）
  把服务端 400 文案**原样** `toast.error` 出来（左侧下拉原来没有 catch，会变成界面无任何提示）。
  **端到端**（构建产物 + 临时 `DATA_DIR` + 真实浏览器）：普通成员连续建 5 本全部成功
  （`books` 5 → 6 → 7 → 8 → 9 → **10 本**，含 admin 默认台账 5 本），第 6 本 **400**
  「你已创建 5 套台账，已达上限；需要更多请联系管理员。」，台账总数 **10 → 10 不变**；
  管理员连续建 6 本**全部 200**；截图 `book-quota-member-5-books.png`、`book-quota-member-6th-400.png`。
- **决策二（不改代码）：`attendance.edit` 可整本写台账 —— 保持现状**。
  理由：整本 PUT 是客户端同步机制（自动保存 = 整本台账快照），**能改考勤数据的人必须能保存**；
  收严会导致「能改考勤却保存不了」。要收严就得改成**按模块增量写**（较大的存储层改造），
  不在本轮范围。已在 A 组报告「处理状态」记为**已决策：保持现状**，并同步 `AGENTS.md` 与 `docs/README.md`，
  免得下次又被当缺陷提一遍。
- **A 组 4 项「未能验证」按用户决定不补测**（特殊环境项：无 `docker.sock` / 跨设备并发 /
  真实 print 引擎 / 整本导入构造样本等）：已在 A 组报告第九节与「处理状态」标注
  「用户决定不做补测（特殊环境项）」，并同步 `docs/README.md` 未闭环清单。
- **测试 375 → 384**（384 通过 / 0 todo）：新增 `tests/book-quota.test.ts` 9 条 ——
  默认 5 / `MAX_OWNED_BOOKS` 覆盖 / 非法值回落 / **只统计 owner**（成员关系不算）/ 提示文案可读，
  以及**真实路由**三条（普通成员第 5 本 200、第 6 本 400 且台账总数不变；上限设 2 时第 3 本即 400；
  管理员设 `MAX_OWNED_BOOKS=1` 仍可继续建）+ 2 条静态守卫（门禁必须在 `push` 之前且走唯一实现；
  两个前端入口必须 try/catch + `toast.error(err.message)`，不许自己拼文案）。
  Excel 往返 **70/70**（未动 Excel 相关代码）。
- 四道闸：`pnpm run typecheck` 0 错误；`pnpm test` **384/384**；`pnpm run test:roundtrip` **70/70**；`pnpm build` 成功。


[1.8.8] 2026-09-16
- 修 C 组逐项功能测试报告（`docs/审查与报告/功能测试-C-合同保险查询影像导入导出-20260916.md`，
  122 项 / 116 通过 / 6 失败）里**已确认的 6 条缺陷** + B 组报告
  （`docs/审查与报告/功能测试-B-人员考勤发放报销-20260916.md`，65 项 / 60 通过 / 5 失败）里
  **已确认的 5 条缺陷** —— 共 11 条**逐条先复现再修**（复现结论与证据见两份报告末尾的「处理状态」）。
- **①【中·数据可信】发放/整本导入不幂等：非补零日期（C 组 D5）**。
  复现（单测级，真实 `buildPaymentWorkbook`→`parsePaymentSheet`→`mergePayments`）：台账里 5 笔（含 `2026-9-5`
  与 `2026/9/5`）→ 导出后原样导回 → **7 笔 ¥6,400**（多记 2 笔）；根因：导出把日期**原样**写成 `2026-9-5`，
  导入端 `rowToPayment` 又 `normalizeDate` 成 `2026-09-05`，**去重键两遍不相等**。
  修法（`src/lib/excel/common.ts`）：去重键里的日期**先规范化**（新增 `keyDate()`，`paymentKey` 与
  `expenseKey` 共用）→ 导出→导入→再导入幂等。
  **端到端**（构建产物 + `/tmp/gd-fixC` + 真实浏览器）：导出发放（2026 年 6 条，其中 2 条非补零）→
  「导入发放」预览 `6 条 · 跳过 6 条重复` → 确认导入后**仍 7 笔 ¥15,000**，再导入一次仍是 7 笔 ¥15,000。
- **②【中·历史重复数据的处理方式（与 D5 配套）】只提示、不自动删**。
  修版前重复导入留下的重复记录**金额已经偏大**，本版**不动用户数据**：导入预览与整本导入确认框里
  会多一句「注意：本台账现有发放记录里有 N 组重复（同人、同日期、同金额，共 M 条）…请到对应页面核对后
  手动删除 —— 系统不会自动删你的数据」（唯一实现 `duplicateNotice()` + `findDuplicatePayments()`，
  纯报不删）。**本版只保证以后不再新增重复**。
- **③【中】合同三类明细（报量/开票/收款）缺「改」入口（C 组 D1）**。复现：编辑合同 HT-2026-001，
  三类明细行内只有「上传/删除」，非 file 控件数 0，录错只能删了重记（还会连带丢掉那笔的影像文件名）。
  修法：明细行加「改」（铅笔，`aria-label=编辑`）→ **复用上面同一份新增表单**带入编辑，按钮变
  「保存这笔报量的修改 / 保存这张发票的修改 / 保存这笔收款的修改」+「取消修改」；
  保存前 `confirm` 逐项列出改动（改动清单抽成纯函数 `contractEntryChanges()`，`src/lib/contracts.ts`）；
  落盘走新的 store 动作 `updateContractEntry()` —— 与新增**同一个 `normalizeEntry`**（金额/不含税/税率口径
  不许在编辑路径分叉），`id` 不变（影像文件仍挂在同一条上），并写一条操作记录「修改合同明细」。
  **端到端**：报量 300,000 → 360,000，确认框 `金额：300000 → 360000`；六个金额口径 **逐行 == 表尾 == KPI**：
  合同金额 1,200,000 / 报量（含税）360,000（不含税 330,275.23 = 360000÷1.09）/ 应收 288,000（=360000×80%）/
  开票 100,000 / 已付 0 / 合同未付 288,000 / 剩余款 100,000；操作记录出现「合同 / 修改合同明细 / report 360000」，
  明细 id（`ce-ats5mm07`）不变。
- **④【中】影像资料类型下拉缺「保险合同」（C 组 D2）**。复现：保单里传了 2 份保险合同 PDF，
  影像资料页类型下拉 = [全部,考勤影像,报量单,电子发票,收款回单,合同扫描件,报销凭证,报销打款]，
  **看不到也筛不出**（前端从没收集 `insurancePolicies[].contracts`；后端 `doc.ts` 的 `kind=insurance`
  与 `DOC_KIND_LABEL.insurance` 早已支持，属**只补前端**）。
  修法：类型下拉加「保险合同」；列表收集保单合同（纯函数 `insuranceContractFiles()`，新模块
  `src/lib/file-list.ts`）；归属显示「保单号 项目名」、说明显示保险期；替换/删除落到**那张保单**的
  `contracts` 上（不再掉进合同明细的 else 分支）；`taken` 名单与页头/空态文案一并补上。
  **端到端**：类型选「保险合同」→ `共 2 份`（`保险合同-PICC-2026-0001-A.pdf` / `…0002-A.pdf`，各带保险期）。
- **⑤【低】影像资料缺 year/month 时显示 `undefined年undefined月考勤`（C 组 D3）**。复现：年份切「全部年份」，
  两条缺年月的考勤影像归属列都是 `undefined年undefined月考勤`。修法：归属文案走纯函数
  `attendanceDocBelong()`（年月齐全才拼「YYYY年M月考勤」，缺任一项显示「**未标注年月**」）。
  **端到端**：两条显示「未标注年月」，`2025-12考勤.xlsx` 仍显示「2025年12月考勤」，整页无 `undefined`。
- **⑥【低】合同导入模板比导出合同表少 3 列（C 组 D4）**。复现：模板「合同管理表」22 列、导出 30 列，
  模板缺 `有无合同 / 合同扫描件 / 无合同原因`（按模板填表的人无法声明这几项）。
  修法：`contractTemplateWb()` 补上这 3 列（位置与导出表一致：`结算应收金额` 之后、`备注` 之前），
  DEMO 示例行与「填写说明」同步。**剩下的 5 列（应收/代付农民工/到分包公司/合同未付/剩余款）是派生列，
  故意不进模板**。**端到端**：模板 25 列、导出 30 列；**模板列顺序是导出列的子序列**（「导出→当模板→再导入」
  列结构对齐）；导出文件再导入仍是 2 份合同 / 4 条明细，再导出再导入也不变。
- **⑦【低】375×667 下弹窗顶部被裁（C 组 D6）**。复现：`375×667` 的「新增发放」面板 `top=-20、h=667`，
  标题与「关闭/确认新增」被顶出屏幕（390×844 正常，`top=109`）；根因：面板 `max-h-screen`（=100vh）
  比可视区高。修法（只改布局，不动信息架构）：面板改 `max-h-[calc(100dvh-4rem)]` +
  `md:max-h-[calc(100dvh-3rem)]`（`dvh` 跟随动态视口，并留出余量），同类弹窗一并改（发放/保单/照片/人员 4 处）。
  **端到端**（本机 Chrome + CDP，精确设置视口）：375×667 下 5 个弹窗（发放/合同/报销/保单/人员）
  **全部 `top ≥ 0`、`bottom ≤ vh`、首个按钮可命中**，其中「新增发放」`top=-20 → 44`、`h=667 → 603`；
  390×844 同样全部合格；截图 `d6-375x667-新增发放.png` 等 10 张。
- **⑧【高·丢数据】报销弹窗不重置，点「新增报销」会覆盖正在编辑的那条（B 组 E11）**。
  复现：新增「E11覆盖测试A / 11」保存（弹窗保持打开）→ 点「新增报销」→ 标题变「新增报销」但表单**仍是
  A 的项目与金额** → 改成 B / 22 保存 → 台账里 **A 整条消失**（B 顶了 A 的 id）。
  修法：`ExpenseEditor` 补**随 draft 变化的同步 effect**（`[draft.id]`，照 `payments.tsx` 的 `PaymentEditor`），
  初值逻辑抽成纯函数 `expenseFormFromDraft()`（`src/lib/expense-rules.ts`，挂载与重置**共用一处**）。
  **端到端**：A 保存后再点「新增报销」→ 项目 `""`、金额 `0`（已重置）；填 B 保存 → **A、B 都在**，
  报销条数 4 → 6（旧行为：A 消失、只多 1 条）。
- **⑨【中】人员弹窗同样不重置：「新增人员」沿用上一条档案（B 组 P19）**。复现：编辑「张三」→ 弹窗开着时点
  「新增人员」→ 标题变「新增人员」但姓名输入框仍是「张三」，电话 13800000001、身份证 110101199001011234
  也一并带进新档案。修法：`PersonEditor` 补 `[person.id]` 同步 effect（初值抽成 `personForm()`），
  顺带清掉上一轮的校验状态。**端到端**：再点「新增人员」→ 姓名 `""`、电话与身份证都没带过来；
  保存「重置校验甲 / 测试班」→ 人数 7 → 8，新档案电话/身份证为空，**张三档案一个字段没动**。
- **⑩【低·与 ① 同源】发放导出→导入不幂等：空收款人（B 组 Y14）**。复现：收款人为空（落盘 `""`）的记录，
  导出→导入时 `rowToPayment` 会把空收款人**回填成实际收款人**，而旧去重键用原值 `receiver` → 键不等 →
  预览 `7 条 · 跳过 6 条重复`、提示「已追加 **1** 条」、条数 7 → 8。
  修法：**收款人判定下沉成唯一实现 `src/lib/receiver.ts` 的 `receiverOf()`（空 = 同实际收款人）**，
  `paymentKey` 改用它（与 ① 的日期规范化同处一个键），`src/lib/payments-stats.ts` 保留转发出口。
  测试同时覆盖三类：**非补零日期 / 空收款人 / 两者叠加**（各自的导出→导入→再导入都断言笔数与金额不变）。
  **端到端**：台账补两笔空收款人（`孙八 2026-08-01 ¥1,200`、`周九 2026-8-9 ¥700`）后，
  导出发放（8 条）→ 导入 **2 次**都是「跳过 8 条重复」，**始终 9 笔 ¥16,900 不变**。
- **⑪【低·口径矛盾】空收款人被标「代收」，与「其中代发」统计打架（B 组 G04）**。复现：搜索钱七那笔
  （收款人字段为空）→ 列表徽标 **「代收」**、打印明细写「钱七（代收）」，而同一筛选的汇总行是
  **其中代发 ¥0.00（0 笔）**。修法：列表徽标、打印明细、工资条（屏幕 + 打印）的「代收」判定
  **统一改用 `isProxyReceiver()` / `receiverOf()`**（与统计同一口径），并加源码守卫禁止再写裸比较
  `owner !== receiver`。**端到端**：`孙八`（空）与 `周九`（空 + 非补零）徽标 = **本人**、打印件无「（代收）」；
  真代收（李四 → 张三）仍显示「张三 代收」，「其中代发」仍是 **1 笔 ¥900.00**（两边一致）。
- **⑫【低·文案】「没设加班规则」被显示成「不计加班」（B 组 A14）**。复现：6 月考勤顶部警示
  「有 2 人**还没在人员表设加班规则**」，同一页「加班规则（人员表）」列里那两人却显示「**不计加班**」，
  无法区分「没设」与「明确选了不计加班」。修法：`parseOtRule("")` 的 label 改为新常量
  `OT_RULE_UNSET_LABEL =「未设加班规则」`（唯一实现，月表/人员页/个人查询/批量改工资预览共用）；
  非空但认不出的老文本（手写的「不计加班」）**照原文显示**，两种从此能区分；**计算口径不变**
  （两种都是 `kind: "none"`，加班费 0）；人员编辑里那个下拉选项文案改为「未设（不计加班）」。
  **端到端**：人员页 王五/钱七/李四/孙八/周九 = 「未设加班规则」（张三/赵六 仍显示各自规则）；
  2026 年 1 月月表同一列 = 「未设加班规则」，与警示「有 1 人还没在人员表设加班规则」**数目一致**。
- **顺带（同类排查 + 一处实测白屏）**：
  · **合同编辑弹窗有和 ⑧⑨ 完全同类的重置缺陷**（`ContractEditor` 同样只在挂载时取一次 `draft`）——
    已一并补 `[draft.id]` 同步 effect。端到端：编辑「某项目主体工程」后点「新增合同」→
    项目名称 `""`、合同金额 `0`（旧行为会把上一条带进来并在保存时覆盖它）。
  · **合同编辑弹窗在老数据上会整页白屏**（实测：`data` 里的合同明细缺 `amountExcl` 时
    `money(undefined)` → `Cannot read properties of undefined (reading 'toLocaleString')`）。
    金额展示的唯一实现 `money()` 加最后一道防线：非有效数字按 `0.00` 显示、**不抛异常**（字段默认值仍由
    数据层负责，见开发规范 §3）。
- **测试 344 → 375**（375 通过 / 0 todo）：新增 `tests/import-idempotency.test.ts` 10 条
  （日期规范化 / 空收款人 / 两类叠加的导出→导入→再导入幂等 + 历史重复只提示不删 + 真新记录仍进得来）、
  `tests/receiver-and-editor-guards.test.ts` 8 条（`receiverOf`/`isProxyReceiver` 语义 + 「代收」与
  「其中代发」必须同口径 + 三个页面禁止裸比较 owner/receiver + A14 两种文案可区分 + 四个编辑弹窗
  必须有「随目标记录 id 重置」的同步 effect）、`tests/file-list.test.ts` 4 条（保险影像收集与
  未标注年月兜底 + 页面接线守卫）、`tests/contracts.test.ts` +5（`contractEntryChanges` 三类 + 编辑口径、
  `updateContractEntry` 源码守卫）、`tests/excel-roundtrip.test.ts` +2（合同模板列对齐与「无合同+原因」回灌）、
  `tests/ui-guards.test.ts` +1（弹窗不许用裸 `max-h-screen`，带坏样本自检）、`tests/utils.test.ts` +1
  （`money(undefined)` 不白屏）。Excel 往返 **70/70**（未改往返用例数量）。
- **端到端环境**：构建产物 `app/` + 临时 `DATA_DIR=/tmp/gd-fixC/data` + 端口 4712 + 真实浏览器
  （小屏弹窗用本机 Chrome + CDP 精确设视口）；11 条逐条复现与修复后复测的证据见两份报告末尾的「处理状态」。
- 四道闸：`pnpm run typecheck` 0 错误；`pnpm test` 375/375；`pnpm run test:roundtrip` 70/70；`pnpm build` 成功。


[1.8.7] 2026-09-16
- 修 A 组逐项功能测试报告（`docs/审查与报告/功能测试-A-账户权限设置-20260916.md`，69 项 / 52 通过 / 11 失败）
  里**已确认的 10 条缺陷**（第 22 项「只读账号能否自建台账」是待拍板的口径项，本版**不动**）。
  11 项失败全部逐条复现过（含第 30b 项观察项），4 项「未能验证」按报告要求不猜着改。
- **①【阻断·数据可信】只读账号能改数据、还弹「已保存」，实际只在本机、刷新即丢**（报告第 17 项）。
  复现：`u_read`（只读预设）在人员页点「编辑」改电话 → 表格立刻显示新号码 + 弹 **「已保存」**
  （同时弹「改动只保留在本机」），而服务端 `PUT /api/ledger` 回 **403**、`books/default/ledger.json`
  一个字节没变，刷新即丢。
  根因：保存动作只改本机 store 再 `toast.success`，**从没问过「这次改动能不能落盘」**。
  修法：新增唯一实现 `src/lib/readonly.ts`（`canSaveToServer(perm)` = `canManageLedger()` **且** 该模块
  `.edit`，判据与服务端 `ledger.manage` 完全一致）+ `blockedWrite(perm, what)`（拦下时只弹
  「你是只读账号（缺「人员 新增/修改」权限），改动不会保存」）。**编辑入口就拦**：
  人员/考勤/发放/合同/报销/保险/照片/影像 8 个模块的「编辑/新增/删除/保存」按钮对无权限账号
  **禁用或隐藏**（`disabled` + `title` 说明），页面顶部 `ReadonlyNotice` 横幅写明原因；
  改密码失败、年度增删（报告 30b）一并处理。
  注意一种容易漏的账号：**合同财务预设（u_contract）有 `contracts.edit` 但整本台账写不了**
  （`canManageLedger` 为 false），他的合同/报销改动同样只在本机 —— 现在也按只读拦下。
- **②【阻断·数据可见性】换账号不清本机缓存，越权看到上一个账号的工资数字**（报告第 30 项）。
  复现：admin 退出 → `u_contract`（无 `people.view`）登录，总览仍显示「在册人员 7 / 应发 ¥10,885.00 /
  已发放 ¥4,400.00 + 班组明细」。根因：本机 zustand 台账缓存按浏览器存，换账号不重置，
  而拉取失败（403）时只弹提示、**保留旧数据**。
  修法（`src/lib/nas-sync.ts`）：新增 `dropLocalLedger()`（内存 + localStorage 一起清空，
  保留界面风格与访问密码）+ 缓存归属键 `gongdi-ledger-v5-owner`（`账号::台账`）。
  ① `checkCacheOwner()` 发现归属变了 → 先清空再拉；② `pullNasLedger()` 收到 **401/403** → 直接清空
  （拉不到就不许留着别人的数字）；③ 退出登录（两处入口）、切换台账（左侧下拉与设置卡片「进入」）
  都先清空再拉。留给老用户的兼容口子：归属键为空（老版本升级上来）时**保留**本机数据，
  让「把本机旧数据升级进空台账」那条 seed 路径照旧可用。
  实测：换 `u_contract` 后总览变成 `在册人员 0 / 应发 ¥0.00 / 已发放 ¥0.00`。
- **③ 改密码「当前密码不对」没有任何提示**（报告第 12 项）。复现：当前密码填错点「保存新密码」→
  界面**什么都不显示**，只有控制台 `unhandledrejection: Error: 当前密码不对`（接口其实是
  `400 {"error":"当前密码不对"}`）。根因：`accounts-card.tsx` 的 `await authOp(...)` 没有 catch。
  修法：try/catch + `toast.error(服务端原因)`，失败不清空输入框（别让用户以为改成功了）。
- **④ 新建台账后左侧台账下拉不刷新**（报告第 32 项）。复现：设置页新建「待删测试台账」→
  卡片列表立即出现，但左侧下拉仍是旧的两本，刷新整页才出现。根因：`createBook` 分支漏发
  `gongdi-books` 事件（进入/改名/删除都发了）。修法：新建后 `dispatchEvent(new Event("gongdi-books"))`；
  左侧下拉自己的「建/改名」也补发（多个下拉实例一起刷新）。
- **⑤ 导出操作不入操作记录**（报告第 37 项）。复现：连导两次（人员名单 + 整本），
  `/api/audit` 条数 **8 → 8**。根因：`src/routes/api/file/$kind.ts` 里没有任何审计写入。
  修法：六种导出（人员/考勤/发放/报销/合同明细/总台账）统一经 `saveXlsx()` 写一条操作记录，
  与其它模块同格式：**模块「导出」+ 操作「导出人员名单」+ 内容=文件名**，记的是**实际导出人**；
  先出文件再记，审计写失败不影响用户拿到文件但会落 error 日志。**空白模板下载不算导出**（不记，免得刷屏）。
  实测：8 → 10，两条记录分别是「导出 / 导出总台账 / 2026年总台账.xlsx / 管理员」与
  「导出 / 导出人员名单 / 人员名单.xlsx / 管理员」；只读账号导出记的是他自己。
  配套：`withTenant()` 现在把解析好的 tenant 传给处理函数（少读一遍 accounts.json）。
- **⑥ 操作记录界面缺批量删**（报告第 41 项）。复现：接口 `DELETE /api/audit?ids=a,b` 一直支持
  （实测 76→74），界面上只有逐行「删除」。修法：管理员多出一列勾选框 + 表头全选（只勾当前页）
  + 「删除所选（N）」按钮，确认文案写明**删几条 + 删除后无法恢复**；
  记录被删/被筛掉后自动把失效的勾选去掉（不会删错行）。
- **⑦ 备份只增不减**（报告第 46 项）。复现：预置 35 份时间戳备份 → 备份一次 → `data/backups`
  从 37 涨到 38 份，永不清理（日志有 `LOG_KEEP_DAYS`、操作记录有 2 万条上限，备份什么策略都没有）。
  修法（`src/lib/nas-fs.server.ts`）：**保留最近 N 份**（默认 **30**，环境变量 `BACKUP_KEEP` 可覆盖，
  范围 1–1000，非法值回落默认）；固定名「考勤表.xlsx」（最新备份入口）**永不删**；
  写完新备份后清理。删除范围用 `isManagedBackupFile()` 卡死 —— **只删自己生成的名字形状**
  （`YYYYMMDD_HHMMSS_考勤表.xlsx` / `考勤表.xlsx`），用户手放进 backups 的其它文件一个不删。
  设置页「数据」卡加提示（份数从 `/api/health` 的 `backupKeep` 来，与服务端默认值同源）。
  实测：38 份 → 点击「立即备份 Excel」→ 30 份时间戳 + 1 固定名 + 1 用户自己的文件 = 32 份，
  删掉的是最旧的 11 份，最新一份与固定名都在。
- **⑨ 首页快捷入口未按权限过滤**（报告第 29 项）。复现：`u_hr`/`u_contract`/`u_min` 的快捷入口
  照样渲染「人员管理 / 合同管理 / 导出」，点进去只有「没有此项权限」。
  修法：`Quick` 按 `people.view / attendance.view / payments.view / contracts.view / expenses.view /
  export.use` 判断，没权限的**隐藏**，并在下方写明「另有 N 个入口因为当前账号没有权限，已隐藏」
  （实测 u_contract 剩 3 个入口 + 该提示）。
- **⑩ 删除台账的确认文案没写影像一起删**（报告第 34 项）。级联删除本身是对的（`books/<id>` 与
  `photos/<id>` 都清掉），但确认框只说「该套数据会删掉」。修法：文案改为
  「该套数据会删掉，**该台账的影像资料（证件照、银行卡、合同扫描件、考勤影像等）也会一起删除**。
  删除后无法恢复，确定继续？」，并补了失败提示（原来删除出错会静默）。
- **⑪ 考勤影像上传提示硬编码「2026年3月」**（报告第 64 项）。复现：编辑 2026-01 时提示仍是
  「自动命名为「考勤-2026年3月」」（实际落盘文件名按年月是对的，只有提示写死）。
  修法：提示按**当前正在编辑的年月**生成 → 「自动命名为「考勤-2026年1月」」。
- **顺带**：只读账号不再看到「新增/删除年度」（报告 30b 观察项，服务端 `/api/year` 要 `settings.year`）、
  考勤页未按权限包裹的「导入考勤」按钮补进 `import.use`、`DocActions` 新增 `readOnly` 开关
  （只读账号只留查看/下载/复制，替换/删除隐藏）。
- **测试 315 → 344**（344 通过 / 0 todo）：新增 `tests/readonly.test.ts` 6 条（只读/考勤发放/合同财务/
  管理员/空权限 五种账号的放行与拦截 + 文案）、`tests/backup-retention.test.ts` 6 条（保留份数默认与
  覆盖、非法值回落、只删自己生成的文件、N+5 造数据后只剩 N、0 字节仍拒写）、
  `tests/readonly-guards.test.ts` 13 条静态守卫（8 个模块页必须有 `blockedWrite`、换账号清缓存三处入口、
  改密码 catch、`gongdi-books` 事件、导出留痕、批量删文案、备份保留、快捷入口权限、删除台账文案、
  考勤影像年月不写死）、`tests/export-audit.test.ts` 4 条（真实路由：管理员/只读账号导出各留一条痕、
  模板不记）。Excel 往返 70/70（未动 Excel 相关代码）。
- **端到端（构建产物 + 临时 `DATA_DIR=/tmp/gd-e2e` + 端口 4711 + 真实浏览器）**：
  ① `u_read` 在人员页：7 个「编辑」全部 `disabled` + title、顶部红字「你是只读账号（缺「人员 新增/修改」
  权限），改动不会保存」、「新增人员」消失、点不动（编辑器打不开、**没有任何「已保存」**）；
  ② admin 退出 → `u_contract` 登录：总览 `在册人员 0 / 应发 ¥0.00 / 已发放 ¥0.00`（不再显示上一个账号的
  ¥10,885 / ¥4,400）、快捷入口只剩合同/报销/导出 + 「另有 3 个入口…已隐藏」；
  ③ 导出两次后操作记录 8 → 10 条（模块「导出」+ 文件名 + 实际导出人）；操作记录页勾 2 条批量删 →
  确认框「删除选中的 2 条操作记录？…删除后无法恢复」→ 10 → 8 条；
  ④ 预置 40 份时间戳备份 + 1 份用户自己的文件 → 点「立即备份 Excel」→ 只剩 30 份时间戳 + 固定名
  「考勤表.xlsx」+ 用户文件；
  ⑤ 填写错误的当前密码 → toast「当前密码不对」（服务端 400 原文），输入框不被清空；
  ⑥ 设置页新建台账 → 左侧下拉**立即**出现新台账（无需刷新）；
  ⑦ 编辑 2026-01 时上传提示为「考勤-2026年1月」；⑧ 删除台账确认框含「影像资料…也会一起删除」。
- 四道闸：`pnpm run typecheck` 0 错误；`pnpm test` 344/344；`pnpm run test:roundtrip` 70/70；`pnpm build` 成功。


[1.8.6] 2026-09-16
- **纠正 1.8.5 的一条口径理解错误**：用户先说「代发算没发吧」，1.8.5 据此把「代收/代发」从已发放合计里**扣除**了；
  用户随后纠正 **「待发不计，代发要计入实际收款人」** —— 1.8.5 那条改错了，本版改回。
- **新的汇总口径（发放页 / 年度表 / 总览 KPI 一律按此）**：
  · **已发放 A** = 所有**填了发放日期**的记录，按「实际收款人」（`owner`）计入其名下，**包含代发/代收**。
    不因为"别人代领"就把它从已发里扣掉（钱已经出去了）。
  · **待发放 C** = 没填发放日期的记录，不算已发，单独列「待发放 N 笔 ¥X」。
  · **代发 B** = 已发的**子集**（B ⊆ A），仍单列标注，但**不扣减**：显示成 **「其中代发 N 笔 ¥X」**（口径行）；
    明细/清单里每笔照旧保留「代收」标记。
  · 新恒等式（替换 1.8.5 的 A+B+C）：**已发（含代发）¥A ＋ 待发放 ¥C ＝ 全部合计**，且 **B ≤ A**。
  · **数字会变化及原因**：「已发放」重新**变回含代发**的金额（发放页汇总、按实际收款人面板与两种打印、
    「按实际收款人」面板每行金额、考勤年度表逐行"已发"、总览「已发放」KPI、Excel 整本导出"已发放金额"列
    都会**变大**，差额就是 1.8.5 被扣出去的代发金额）；跨年台账里，代发金额回到它实际收款人的那一年。
    例（E2E 实测数据）：1.8.5 会显示「已发（本人）¥1,600.00 + 代发 ¥700.00 + 待发放 ¥900.00」，
    1.8.6 显示「已发 ¥2,300.00（其中代发 ¥700.00） + 待发放 ¥900.00 = ¥3,200.00」。
- **判定命名理清**（`src/lib/payments-stats.ts`）：新增 `isPaid()`（有日期即算）= 「已发放」的唯一判定，
  发放统计 / 年度汇总 / Excel 汇总共用；`isPaidSelf()` 收窄为**只服务两件事** ——
  ① **工资条**（`query.tsx`）的「已打款合计（本人）」（单人视角：这笔钱他本人有没有拿到）；
  ② 「其中代发」的**子集统计/标注**（`isProxyPaid`）。注释与守卫测试都写死"不许再拿它判已发"，
  免得下次又把两种口径混起来（1.8.5 就是这么错的）。
- **工资条（单人视角）保持不变**：`src/routes/query.tsx` 仍区分「本人收款 vs 代收」，不改成混算 ——
  那是"这笔钱他本人有没有拿到"，与发放页/年度表/总览的汇总口径是两回事（1.8.6 起明确分开并写进文档）。
- **打印两种清单的口径小字分别写清**（不再共用一句含糊的话）：
  · 明细清单 = 「已填发放日期的按实际收款人计入已发；待发放也列入实际收款人名下，逐笔标注「已发 / 待发」，
    小计拆「已发小计 / 待发小计」；代发（收款人非本人）已计入实际收款人名下，小计单列「其中代发」。」
  · 汇总清单 = 「已发 = 只计已填发放日期的记录，按实际收款人计入（含代发，单列「其中代发」）；
    待发放单列一组，不计入已发。」
- **按用户补充要求改「打印明细」**：待发放记录**计入对应实际收款人名下**（和已发同一节），
  每笔标注「已发 / 待发」，节尾小计拆成 **「已发小计 ¥X」+「待发小计 ¥Y」** 两行，
  满足：已发小计 == 汇总里该人的已发、待发小计 == 汇总「待发放」组里该人的部分、
  已发行数 + 待发行数 == 该筛选下该人的全部记录数。**打印汇总维持待发单列**，不并进已发。
- **断言覆盖（tests/payments-stats.test.ts 15 条 + tests/attendance-summary.test.ts 9 条 + 守卫）**：
  本人收款 1 笔 / 代发 1 笔 / 待发放 1 笔 / 同一人混合 / 跨年 / 金额 0 六种边界；
  `A + C = 总计`、`B ⊆ A`、`明细逐笔之和 = 汇总各行之和 = 总计 = A + C`、
  「其中代发」在**面板 / 明细 / 汇总**三处数字一致、**含待发放与不含待发放两种筛选下都成立**；
  年度表逐行「已发」之和 == 总览「已发放」KPI，Excel 汇总「已发放金额」也含代发。
  自动测试 314 → **315**（315 通过 / 0 todo）。
- **端到端（构建产物 + 临时 `DATA_DIR=/tmp/gongzi186` + 端口 4621 + 真实浏览器）**：
  灌入「本人收款 2 笔 / 代发 2 笔 / 待发放 1 笔 / 金额 0 一笔 / 跨年一笔 / 无考勤者收款」的边界数据，界面数字与手算逐项对齐：
  发放页 `6 笔 · 合计 ¥3,200.00 · 已发 ¥2,300.00（5 笔，含代发） · 其中代发 ¥700.00（2 笔） · 待发放 ¥900.00（1 笔）`；
  面板 `张三 2/¥1,500.00（其中代发 1/¥500.00）+ 赵六 1/¥600.00 + 李四 1/¥200.00（其中代发 1/¥200.00） + 王五 1/¥0.00 + 待发放 1/¥900.00 = ¥3,200.00`；
  打印明细 `张三 2 笔 已发小计 ¥1,500.00（其中代发 1 笔 ¥500.00）`、`赵六 已发小计 ¥600.00 + 待发小计 ¥900.00`、
  `李四 ¥200.00（其中代发 ¥200.00）`、`王五 ¥0.00`，总计 `6 笔 ¥3,200.00`；
  打印汇总 `张三 1500 / 赵六 600 / 李四 200 / 王五 0 / 待发放 900`，总计 `6 笔 ¥3,200.00`；
  考勤年度表逐行已发 `1500 + 200 + 0 + 600 = ¥2,300.00 == 总览「已发放」¥2,300.00`；
  「已发放」筛选下 `5 笔 ¥2,300.00`、「待发放」筛选下 `1 笔 ¥900.00`（两种筛选 `A + C = 总计` 都成立）。
- **文档**：FAQ 第 14 条按新口径重写（并注明 1.8.5 曾扣掉代发、1.8.6 已纠正），补两种清单的差别与工资条单人视角说明；
  《使用说明书》§4/§5 同步（年度表「已发」含代发、两种清单口径、工资条例外）。
- 四道闸：`pnpm run typecheck` 0 错误；`pnpm test` 315/315；`pnpm run test:roundtrip` 70/70；`pnpm build` 成功。


[1.8.5] 2026-09-16
- 口径决策落地：CTO 拍板的 §6 三条（+ 用户追加的「代收不算已发」）一次性做完并随本版发布。
  报告 docs/审查与报告/口径一致性排查-20260916.md §6 的「需决策」三项全部闭环（处理状态见报告 §9）。
  先写测试复现旧行为、再改代码；三条互不重叠的维度成了新的发布门槛等式（见下）。
- **决策四：代收（收款人非本人）不算「已发放」，单列「代发」**
  · 新口径：`已发（本人）` = 填了发放日期 **且** 收款人 = 本人；`代发（代收）` = 填了日期但收款人非本人；
    `待发放` = 没填日期。三者互不重叠，恒等式 **A + B + C = 全部合计**（金额与笔数都成立）。
  · 唯一判定 `isPaidSelf()` 收在 `src/lib/payments-stats.ts`，发放统计 / 年度汇总 / 工资条打印三处共用
    （`tests/caliber-guards.test.ts` 静态守卫：页面里不许再出现 `owner === receiver` 的第二套判定）。
  · 发放记录页汇总行改成三段（实测）：
    `6 笔 · 合计 ¥3,800.00 · 已发（本人）¥2,400.00（3 笔） · 代发 ¥200.00（1 笔） · 待发放 ¥1,200.00（2 笔）`；
    「按实际收款人」面板变成「人员行 + 代发 + 待发放」，人员行的金额只是本人收款（代收笔数只作提示文字）。
  · 两种打印清单（明细/汇总）都把「代发」「待发放」独立成节/成行，表头小字写全：
    「只计已填发放日期的记录；代收（收款人非本人）不计入已发，单列代发；待发放不算已发。」
  · **数字会变化及原因**：「已发放」从此不含代收的钱 —— 发放页汇总/面板/两种打印、考勤年度表、
    总览「已发放」KPI 都会变小，差额移到「代发」；代收记录本身照常显示并保留「代收」标注。
  · 顺手统一工资条打印：`已打款合计（本人）`改用同一判定，并修掉一个老缺陷 ——
    `src/routes/query.tsx` 的 `buildSlips` 在 `map` 里把 `owner` 丢掉了，`x.owner === name` 永远为 false，
    **「已打款合计（本人）」以前恒为 ¥0.00**；现在照实显示本人收款，另单列「他人代领 ¥X」。
    **数字会变化**：工资条的「已打款合计（本人）」与「未打款」从错误的 0 / 全额变成正确值。
- **决策一：待发放（无日期）单列一组 + 按当前工作年归集**
  · 面板与两种打印清单里新增「待发放」分组（笔数/金额单列，**不进任何人的已发合计**）。
  · 无日期记录的年度归属从「整本台账最早的一年」改为**当前工作年**（`fallbackPayYear`）。
    **数字会变化及原因**：跨年台账里，早年年份的「待发放」会变成 0，当前年拿到全部待发放
    （旧口径会把同一笔归到整本最早的那一年，用户在当前年看不到）。
  · 界面与打印表头各加一行小字「无日期的待发放记录按当前年份（YYYY）显示」。
  · 硬约束进测试：**明细逐笔之和 = 汇总各行之和 = 总计 = A + B + C**，在「含待发放（状态=全部）」
    与「不含待发放（状态=已发）」两种筛选下都成立（单人选混合数据同样成立）。
- **决策二：发给「本年无考勤记录者」的钱补进年度汇总表**
  · 年度工资汇总表补一行：备注「本年无考勤记录」，全年 0、已发照实列、未发为负数（照实）；
    补行对象是「本年有本人收款、但一次考勤内容都没有」的人（含已删人员 / 手写名字）。
  · 目的：**年度表逐行「已发」之和 == 总览「已发放」KPI**（口径自洽，实测两侧都是 ¥2,400.00）；
    原「另有 N 笔…」差额提示保留作安全网（补行之后恒为 0，不再需要它救场）。
  · 表格新增「备注」列；「工天加班」标签页仍只列本年有出勤的人。
    **数字会变化及原因**：年度表会多出这样的行（全年 0、已发照实、未发负数），逐行之和因此等于 KPI。
- **决策三：组合险（互挂保单）加明确标注**
  · 不改金额/人数算法（每张保单各自计费是业务事实，去重会改口径）。
  · 保险页与打印件《团体保险人员清单》都加上：
    「组合险（互挂保单）：同一个人可能同时出现在两张保单上，人数与保费按各保单分别计算，不合并去重。」
    （文案唯一实现在 `src/lib/insurance.ts` 的 `COMBINED_POLICY_NOTE`，两处引用由守卫测试查住）。
  · 页面 `syncLinked` 的互挂目标计算抽成 `src/lib/insurance-stats.ts` 的 `linkedPolicyTargets()`（§12.1 纯函数）。
  · `docs/使用与部署/常见问题解答(FAQ).md` 补第 14 条（已发放为什么少）/ 第 15 条（组合险），
    `使用说明书.md` 补组合险与发放三维修的解释。
- 结构（§12.1）：发放的统计/打印口径全部收在 `src/lib/payments-stats.ts`（页面只留骨架），
  打印件 `payment-sheets.tsx` 只渲染传进来的数字（守卫测试禁止它自己再 `reduce` 合计）。
- 测试 307 → **314**（314 通过 / 0 todo）：`tests/payments-stats.test.ts` 14 条（含四种视图下的等式）、
  `tests/attendance-summary.test.ts` 9 条（含决策二等式与决策一归年）、`tests/insurance-stats.test.ts` +2、
  `tests/caliber-guards.test.ts` +1 条守卫（isPaidSelf 唯一实现 + 组合险标注两处引用 + 打印口径小字）。
- 端到端（构建产物 + 临时 `DATA_DIR=/tmp/gongdi185` + 端口 4611 + 真实浏览器）：
  灌入「待发放 2 笔 / 本年无考勤者收款 1 笔 / 代收 1 笔 / 互挂组合险 2 张」的边界数据，界面数字与手算逐项对齐：
  发放页 `6 笔 · 合计 ¥3,800.00 · 已发（本人）¥2,400.00（3 笔） · 代发 ¥200.00（1 笔） · 待发放 ¥1,200.00（2 笔）`；
  面板 `张三 1/¥1,000 + 李四 1/¥800 + 赵六 1/¥600 + 代发 1/¥200 + 待发放 2/¥1,200 = ¥3,800.00`；
  打印明细 / 打印汇总的总计都是 `6 笔 ¥3,800.00`（「已发放」筛选下 `4 笔 ¥2,600.00`；单人选赵六 `2 笔 ¥1,500.00`）；
  考勤年度表逐行已发 `1000 + 800 + 0 + 0 + 600 = ¥2,400.00 == 总览「已发放」¥2,400.00`；
  保险 PICC-2026-0001 人天 20 / 保费 ¥2,000、PICC-2026-0002 人天 10 / 保费 ¥4,000（甲/乙同时出现在两张保单上，不去重）。
- 四道闸：`pnpm run typecheck` 0 错误；`pnpm test` 314/314；`pnpm run test:roundtrip` 70/70；`pnpm build` 成功。



[1.8.4] 2026-09-16
- 修一个用户实测报的打印缺陷 + 清掉上一轮优化留下的 4 条小尾巴。
- **打印缺陷（用户实测）**：发放记录页「打印明细 / 打印汇总」把**整个页面**都印出来了
  （左侧导航、台账/年份切换、筛选栏、统计行、明细表格）。
  根因：`window.print()` 与打印件都在，但屏幕内容没有套 `no-print`，而打印规则只有
  `@media print { .no-print { display:none } }`（用户实测：`src/routes/payments.tsx` 的屏幕块缺 no-print）。
  修法：**照个人查询页的既有范本**（`src/routes/query.tsx`：屏幕内容 `<div className="no-print …">` +
  打印件 `print-only` 放在包裹之外）把发放页改齐；报销单 / 合同对账单 / 保险合同清单 / 个人查询工资条
  用同一把尺子复查，本来就是对的（打印件在屏幕态 `display:none`，打印态只剩清单）。
  打印媒体验证（`emulateMedia("print")`）：发放明细、发放汇总各一张截图
  （`browser-screenshots/print/payments-明细-全部人.png` / `payments-汇总-全部人.png`，另附同页 A4 PDF），
  断言「`.no-print` 可见数 = 0、打印件可见、`.print-only` 之外无可见文字」成立。
  回归护栏进 `tests/ui-guards.test.ts`：含 `window.print()` 的页面必须有 no-print 包裹、打印件在包裹外，
  并自带坏样本自检（这条守卫能抓出本次的缺陷）。
- **台账上传的解压炸弹防护**（`src/routes/api/ledger.ts` + 新模块 `src/lib/ledger-transfer.ts`）：
  · 解压前用 `content-length` 粗筛，解压时用 zlib 的 `maxOutputLength` 限制**解压后**大小；
  · 上限默认 **32MB**，可用 `LEDGER_MAX_MB` 覆盖（非法值回落默认，封顶 4096MB）；
  · 超限 → **413**（格式错仍是 400），两种都给可读中文原因、**绝不写盘**、记一条 `logServer("warn", …)`。
    E2E 实测：100MB 全零压成 100KB 上传 → 413，且 `books/default/ledger.json` 的 sha256 前后一致
    （拒绝的写入一个字节都没落地）；正常 ~1MB 台账 gzip 上传 → 200 且落盘内容与提交一致。
  · 测试：`tests/ledger-transfer.test.ts` +4（炸弹 413 / 上限覆盖 / 1MB 正常 / content-length 粗筛）。
- **`LEDGER_GZIP=off` 开关**：下行 GET 不再压缩；上行**客户端**也不压缩（`/api/health` 回
  `ledgerGzip:false`，客户端据此跳过 CompressionStream，见 `src/lib/ledger-gzip-flag.ts`）。
  **服务端仍然接受 gzip 请求体** —— 新老客户端互通，老客户端（缓存了旧 JS）继续压缩上传照样能存；
  开关只影响我们自己发出去的字节。E2E 实测：off 时 GET 无 `content-encoding`、未压缩 PUT 200、
  gzip PUT 仍 200；`LEDGER_GZIP` 不设时照旧压缩下发。测试 +3。
- **`/api/backup` 空 body 不再「回 ok 却写空备份」**：0 字节（或没有 body）以前会一路走到
  `saveBackup()`，把 `data/backups/考勤表.xlsx` 这个「最新备份」固定名覆盖成 0 字节。
  现在路由在写盘前 **400** 拒绝（`saveBackup()` 里再兜一层：0 字节直接不写），
  并且 `saveBackup()` 改成原子写（temp + rename，先落时间戳那份再更新固定名）。
  E2E 实测：空 body → 400，`考勤表.xlsx` 的 sha256 前后一致；正常 xlsx → 200 且两份都落盘。
  测试：新增 `tests/backup-guard.test.ts`（4 条，含「目录内容逐字节不变」断言）。
- **日志超限改为滚动，不再停写**（`src/lib/log.server.ts`）：
  · 当天 `YYYY-MM-DD.log` 达到 `LOG_MAX_MB`（默认 8MB）时滚动到 `YYYY-MM-DD.log.1`，
    已有 `.1` 就写 `.2`，依次递增（编号取目录里最大编号 +1，封顶 999）；**当天现场不再被丢掉**；
  · `.log.N` 已纳入保留策略（`isManagedLogFile` 认这个形状），过期一起删 ——
    当初选「停写」正是因为滚动件没人清会永久堆积，这次两件事一起解决；
  · 保留策略照旧只删过期的 `YYYY-MM-DD.log*`，**`update.log` 永不删**（含 `update.log.1`）；
    清理时机不变（进程启动后首次写 + 每日首次写，不在每次写的热路径上扫目录）；
  · 单行就超上限时不会无限滚动（宁可这一份超一点，也不丢这一行）。
  E2E 实测（临时 DATA_DIR + `LOG_MAX_MB=0.0004`）：主文件 252B、生成 `.log.1`–`.log.5`、
  15 行一条不丢；重启后清理把 `2026-08-01.log`/`2026-08-01.log.9`/`2026-08-02.log.3` 删掉，
  `update.log` 与 `update.log.1` 保留。测试 +4（滚动 / 保留清理 .log.N / 单行超限 / 编号规则）。
- **手机端两处布局修补**（顺手修，只动尺寸不动信息架构）：
  · `src/components/shell.tsx`：主内容区缺底部内边距，底部固定导航（`min-h-12` + 安全区）会压住
    最后一行内容（实测「上一页/下一页」、行内「编辑/删除」被导航遮住点不到）→ 改
    `px-3 pt-4 pb-20 md:px-8 md:pt-6 md:pb-6`；
  · 设置页卡片在网格里被内容撑到 408px（列宽只有 366px），外层 `overflow-x-hidden` 一裁右边的字被切掉
    → `settings.tsx` / `members-card.tsx` 的网格补显式 `grid-cols-1`（手机单列的隐式 auto 轨道按
    max-content 撑开）、`accounts-card.tsx` 的 section 加 `min-w-0`。
- 自动测试 291 → **307**（307 通过 / 0 todo），70 个「导出→导入」对拍全过。
- 四道闸：`pnpm run typecheck` 0 错误；`pnpm test` 307/307；`pnpm run test:roundtrip` 70/70；`pnpm build` 成功。




[1.8.3] 2026-09-16
- 口径一致性专项（第 2 步：其余模块）—— 把「同一份数据在列表/汇总/分组/下拉/打印里对不上」的
  同型缺陷一次查完并修掉。报告：docs/审查与报告/口径一致性排查-20260916.md。
- 总览 KPI 与考勤年度汇总统一（`src/lib/attendance-summary.ts`）：
  · 「无日期的旧发放算哪一年」原来两套算法（总览取整本台账最早的一年、考勤页只用该年+考勤年份），
    同一年的「待发放」两处数字会不一样；现在统一走 fallbackPayYear()。
    **数字会变化**：台账里存在比当前年更早、但没有考勤记录的年份时，该类无日期发放会归到最早那一年。
  · 「已发放」口径统一：KPI 仍是本年**全部**有日期的发放（钱打出去了就不能从 KPI 消失），
    年度表逐行之和是它的子集。发给「本年没有考勤记录的人」的差额现在会在考勤页显式提示
    （「另有 N 笔 ¥X …」），不再让人对着差额找错账。
  · 总览「班组」面板补「未分班组」一行：面板人数之和现在等于「在册人员」KPI（以前没填班组的人
    从面板里静默消失）。**数字会变化**：面板会多出一行「未分班组 N 人」。
- 考勤「有没有内容」判定收敛成唯一实现 `hasContent`（src/lib/work.ts）：
  纯备注行（如整月「工伤休息」）在月度卡/已录月份/年度汇总/工资条/整本导出里都算有内容，与 1.8.0
  起 Excel 导出的口径一致。**数字会变化**：纯备注月份从「空表」变「已录入」，「已录月份 X / 12」计数 +N；
  年度汇总会列出该人（金额 0）；个人查询的「打印 N 张」会包含只有备注、没有打款的人（E2E 实测 4 → 5 张）。
  金额一律不受影响（工资计算继续用 hasWork，纯备注行的出勤/加班/补助/扣款都是 0）。
  整本 Excel 的「汇总」「工天加班」两表以前只认工天，会出现「月表有这一行、汇总没这个人」，现在同口径。
- 筛选下拉的「空值桶」补齐（`src/lib/buckets.ts` 的 groupBuckets + ALL_BUCKETS 哨兵）：
  · 报销「全部报销人」：改为按**当前范围**取桶并新增「未填报销人」——以前从所有年份取，
    会列出本年一笔都没有的报销人（选了 0 行），而没填报销人的记录在下拉里选不到；
  · 保险「全部队长」：新增「未分班组」并改为只取**当前保单**的队长（以前取所有保单的非空队长）；
  · 人员「班组」、个人查询的按班组快捷勾选：补「未分班组」。
  **数字会变化**：选了某个发放方/报销人/队长/班组时，原先被漏掉的空值记录会补上，对应合计随之变大。
- 保险统计同源：筛选后的「在保/已结束人数」与「累计人天/保费合计」以前一个全量一个筛选后
  （同一行里两个口径），现在都按当前筛选后的名单统计；打印件按班组汇总的表尾 = 分组各行之和。
- 结构：合同合计与总览「应收」收敛到 `src/lib/contracts-totals.ts`（行为不变）；
  保险/报销的筛选统计抽到 `src/lib/insurance-stats.ts` / `src/lib/expenses-stats.ts`；
  报销的 PAY_METHODS/needsVoucher 下沉 `src/lib/expense-rules.ts`（组件 re-export，调用方零改动）。
- 回归防线 +29：新增 tests/attendance-summary.test.ts、insurance-stats.test.ts、expenses-stats.test.ts、
  contracts-totals.test.ts、caliber-guards.test.ts（静态守卫：下拉必须走 lib/buckets、发放页统计/打印
  只能来自 payments-stats 且不许再截断前 12 人、总览与考勤必须共用同一函数、合同合计只有一个实现）。
  自动测试 262 → 291。
- 复核后判定不是缺陷（有证据，未改）：影像资料「共 N 份」= 列表行数、照片页无计数汇总、
  操作记录「共 N 条」= 筛选后行数（E2E 实测 3/3、2/2）。需产品决策的 3 项见报告 §6。

[1.8.2] 2026-09-16
- 口径一致性专项（第 1 步：发放记录页）—— 同一份数据在「①列表条数 ②汇总数字 ③按收款人面板
  ④筛选下拉选项」四个口径下必须对得上，屏幕与打印纸也必须对得上。
- 修复（用户报的实例）：发放记录页「按实际收款人入账（只计已填日期的；待发放不算已发）」
  统计显示不全、选「全部发放方」时漏项。两处根因：
  · 发放方下拉用 `[...new Set(ranged.map(p => p.source).filter(Boolean))]` 造选项，把**空发放方**
    那一桶删掉了：没填发放方的记录在列表和汇总里都算数，但下拉里没有这一项，逐发放方取数时
    取不到它们的钱（实测一本 2 笔的台账：五冶 ¥1,500 + 一局 ¥300 = ¥1,800，漏掉空发放方的
    ¥1,000）。现在下拉多一项「未填发放方」，选中它显示的就是这批记录；「全部发放方」仍然真的全。
  · 「按实际收款人入账」面板写死 `.slice(0, 12)`：超过 12 个实际收款人只显示金额最高的 12 个，
    面板金额之和 < 汇总「已发」金额（实测 13 个收款人各 ¥100 时：面板 ¥1,200、汇总 ¥1,300，
    差 ¥100）。现在面板列出全部分组，标题栏显示「共 N 人 · N 笔 · ¥X」。
  **数字会变化及原因（预期行为）**：① 选中某个发放方时，原先被漏掉的空发放方流水不再丢失，
  对应的合计/已发/待发/代收会变大（只有空发放方参与筛选时才变）；② 实际收款人超过 12 个的
  台账，面板会多出原本被截掉的人与金额（汇总数字本来就有它们，现在两处一致）。
  没填发放方、或收款人不超过 12 个的台账，数字不变。
- 新增：发放记录页按实际收款人的**两种打印清单**（按钮在统计文字旁边；旁边可选「全部实际收款人」
  或某一个人）。① **明细清单**：按人分节，每笔一行（发放日期/实际收款人/收款人（代收标注）/
  发放方/金额/备注）+ 该人小计，整节不拆页；② **汇总清单**：每人一行（笔数/合计/含代收 N 笔）+
  总计。两者与屏幕共用同一个纯函数（`src/lib/payments-stats.ts`），表头小字写明口径
  （只计已填发放日期的记录；待发放不算已发），并跟随当前筛选（区间/发放方/状态/搜索）；
  当前筛选没有已发记录时按钮禁用，不会弹出空白打印页。
- 结构（§12）：发放页的区间筛选、状态/发放方/搜索筛选、汇总、按收款人分组、下拉选项、
  打印数据全部下沉到 `src/lib/payments-stats.ts`（页面只留骨架，行为不变的部分逐字搬运）；
  筛选下拉的「桶」抽出唯一实现 `src/lib/buckets.ts`（`ALL_BUCKETS` 哨兵 + 「未填」桶，
  不再用 `filter(Boolean)` 删掉空值那一桶）；`ymKey` 的唯一实现移到 `src/lib/dates.ts`
  （`ym-pick.tsx` re-export，页面导入路径不变，防第二套口径）。
- 同批口径统一：考勤「有没有内容」的判定。纯备注行（如整月「工伤休息」）此前在月度卡 /
  已录月份 / 年度汇总里按「空」处理，与 1.8.0 起 Excel 导出「备注也算有内容」的口径不一致。
  现在 `hasContent` 是唯一实现（`src/lib/work.ts`，工资计算继续用 `hasWork`）。
  **数字会变化**：纯备注月份从「空表」变成「已录入」，总览/设置里的「已录月份 X / 12」计数会 +N；
  金额一律不受影响（纯备注行的出勤/加班/补助/扣款都是 0）。
- 回归防线 +12：`tests/payments-stats.test.ts` 锁住四口径一致与两种打印
  （待发放不算已发、空发放方不漏、代收标注、金额 0、跨年区间、筛选后口径、
  明细逐笔之和 == 汇总里该人合计 == 总计、选单人时总计只算那个人、空结果不产空白页）。
  自动测试 250 → 262。

[1.8.1] 2026-09-16
- 架构优化方案 20260916 第 1 波落地（候选 A–E）：坏提交更拦得住、失败更看得见、保存更快。
- 可观测性（候选 D）：日志加级别开关（LOG_LEVEL，默认 info）、保留天数（LOG_KEEP_DAYS，默认 14 天）
  与单文件上限（LOG_MAX_MB，默认 8MB，到顶当天停止写文件并留一条提示，日志自己不会把数据盘撑满）；
  启动器对每个请求记「耗时 + 状态码」，5xx 必记一条、耗时超 SLOW_MS（默认 2000ms）记「慢请求」；
  只记路径不记查询串与 Cookie。应用内日志与启动器日志共用同一套环境变量语义。
- 台账传输压缩（候选 B）：上行（保存）用浏览器 CompressionStream 压 gzip，实测一本 1.05MB 的台账
  压到 52KB；下行（读取）在客户端接受 gzip 时压缩响应。老浏览器、不支持压缩或压缩失败时
  **自动退回不压缩**，功能不受影响；服务端不认识的 content-encoding → 400 并说明原因（不是 500），
  未压缩的老请求照常 200；X-Ledger-Revision 与「台账文件坏了 → 503」的语义都没动。
- 影像目录索引缓存（候选 C）：照片/文档目录的列目录结果做进程内缓存，键含数据目录与台账 id，
  失效三路齐全 —— 目录 mtime 变化、TTL（ASSETS_CACHE_MS，默认 3000ms，0=关闭）、写路径主动失效
  （上传/删除/归入后立刻可查、立刻不可查）。只缓存文件名列表，不缓存文件内容；回落目录顺序不变。
- 静默失败收敛（候选 E）：审计写入失败不再空 catch（记 error 日志，「操作记录没进去」这类问题可查）；
  Excel 导入与旧数据迁移的数值解析统一走 src/lib/num.ts 的唯一实现（原实现从 excel/common.ts 提取，
  经 barrel 转出，现有导入路径零改动）：千分位「1,200」、货币符号、全角数字、单位后缀不再被静默
  写成 0，读不出来才按 0 并留 warn 痕迹；Excel 侧新增 13 条测试兜底。
- 质量闸门（候选 A）：新增 tests/guards-paths.test.ts（守卫测试引用的源码路径必须存在，拆文件忘改
  守卫路径会让 pnpm test 直接红）；ci/check.workflow.yml 补「Excel 往返对拍」一步；
  package.json 新增 pnpm run check（typecheck → test → test:roundtrip → build 一条命令）。
  注意：GitHub 上的 check.yml 仍需在网页创建（规范禁止本地改 .github/workflows/）。
- 依赖瘦身：package.json 依赖 16 → 13（移除已无引用的 archiver / dayjs / srvx），
  pnpm-lock.yaml 为纯删除（-290 行，零漂移），构建与测试结果不变。
- 回归防线：自动测试 172 → 250（+78），70 个「导出→导入」对拍用例照旧全过。
- 第 2 波（发版 tag 之后继续提交，版本号不变）：
  · 写接口输入口径（候选 F）：`PUT /api/photo` 与 `PUT /api/doc` 对「写函数会静默跳过的入参」
    （dataUrl 不是 data:image/…;base64、名字/id 去掉非法字符后为空）改为回 400 ——
    原先回 200 {ok:true} 却没落盘，用户会以为传上去了。判定与写函数共用同一份正则/清理逻辑；
    新增 tests/api-input-guards.test.ts（12 条：行为用例「非法入参确实不写盘 / 合法入参确实落盘」
    + 结构守卫「写 handler 必须有鉴权、必须在第一次写盘前有 4xx/重定向拒绝路径、body 解析必须有兜底」）。
  · 低风险可用性打磨：「新建台账 / 新建账户」「立即备份 Excel」「开始归入」加请求中禁用态
    （防连点建出两本同名台账、生成两份备份、同时跑两遍归入）；删除操作记录的确认框写明
    「删哪条 + 删除后无法恢复」；新建台账失败时给出错误提示（原先会静默失败）。
  · 面向用户的文档：新增 docs/使用与部署/使用说明书.md 与 常见问题解答(FAQ).md
    （FAQ 覆盖：更新后版本没变、图片上传失败、保存冲突 409、权限不够、备份与迁移、忘记密码、
    页面 500、导入条数不对、清空数据能否恢复、docker.sock 合规路径）；部署说明.txt §七
    补齐「不需要网页更新」的三步合规路径。

[1.8.0] 2026-09-16
- 版本号按规范进位（Z 上界 19）：1.7.19 之后应为 1.8.0；1.7.20 为误发（同 1.6.20 先例），不再延续。
  版本号规则（Y≤9、Z≤19）写进 tests/changelog.test.ts 守卫：当前版本违规直接拦下，不再靠人记；
  开发规范 §1 违规先例补记 1.7.20。
- 全面检查（见 docs/审查与报告/全面检查-20260916.md）修掉 3 个小项：
  · 照片保存改为「新文件先 rename 就位、再清旧文件」（与 1.7.17 saveDoc 同一原则）：
    原顺序在 rename 失败的一刻会两头空——旧照片已删、新照片还是临时名被清掉。
  · 整本导出「全部年份」范围：只有备注、没有工天的月份（如整月工伤休息）现在也会
    生成考勤 sheet —— 月份识别口径与导出行口径统一（备注也算有内容），这类记录不再导出即丢。
  · 启动器的请求级 500 现在也落 data/logs/日期.log（原来只进 stdout，
    NAS 上按日期翻日志看不到任何 500；未捕获异常此前已落盘，这次把被接住的请求错误也补上）。
- 回归防线 +3：savePhoto「先就位后清旧」源码守卫、导出月份识别含备注的口径守卫、
  启动器请求级错误落盘守卫。测试总数 169 → 172（172 pass + 0 todo）。
- 文档：docs/使用与部署/ 下 5 份滞后说明（目录结构/部署说明/说明/程序文件说明/GITHUB上传说明）
  按现状集中重写（未闭环清单第 11 条闭环）；package.json 版本字段补齐同步（此前滞后）。

[1.7.20] 2026-09-14
- 测试与结构补全（不改任何行为）：
  · 保险结算逻辑从 insurance.tsx 抽成纯函数模块 src/lib/insurance.ts
    （在保判定 / 人天计算含保单期夹紧 / 替换边界 / 空记录），页面改从模块导入；
  · 新增 tests/insurance.test.ts 8 条用例，锁住 §4 保险结算口径
    （当天算 1 天、越界夹紧不计、结束为空算到今天、按天比较不看时分）；
  · 本轮一并合入 1.7.19 后补的 utils/changelog/xlsx-center 共 15 条用例。
- 回归验证：typecheck 通过，169 个测试全绿，70 个 Excel 往返对拍全过，构建通过。

[1.7.19] 2026-09-14
- 结构重构第二轮（UI 层与更新模块拆分，模块化/低耦合专项续，见
  docs/审查与报告/结构重构-20260914.md），不改任何行为：
  · shell.tsx（1247 行）→ src/components/shell/ 组件目录：导航/品牌/主题/年切换/
    台账切换/Windows 更新卡/版本日志/各登录与兜底屏，各件单文件。
  · expenses.tsx / contracts.tsx / settings.tsx 三个大页面的「编辑器弹窗」拆成独立组件
    （expense-editor.tsx / contract-editor.tsx / settings/ 五张卡片），路由文件只留页面骨架。
  · update.server.ts（1176 行）→ src/lib/update/ 六个模块：consts（常量）/
    log（更新日志与容器现场）/ version（版本检查与拉取候选）/ docker（引擎交互与镜像清理）/
    updater-script（内联替换脚本）/ apply（执行与后台任务）；update.server.ts 保留为
    转发出口，路由与测试导入路径不变，8 条源码守卫测试随迁移更新扫描目标。
- 回归验证：typecheck 通过，146 个测试全绿，70 个 Excel 往返对拍全过，构建通过。

[1.7.18] 2026-09-14
- 结构重构（模块化 / 低耦合专项，见 docs/审查与报告/结构重构-20260914.md），不改任何行为：
  · 解开 dates ↔ wage 循环依赖：出勤判断 hasWork 抽到独立模块 work.ts。
  · NAS 开关 nasEnabled 从同步引擎（nas-sync）抽到 nas-flag：照片组件不再需要依赖整个同步引擎。
  · excel.ts（1320 行）按实体拆成 src/lib/excel/ 目录：common 共享层 +
    people/attendance/payments/expenses/insurance/contracts/full 七个实体模块，
    原 excel.ts 只做 re-export，调用方零改动。
  · nas-fs.server.ts（1184 行）拆成三层：paths.server（数据目录/台账上下文/原子写）、
    assets.server（照片与文档的查找/写入/删除/归入）、nas-fs.server 只留台账/审计/备份/台账册。
    依赖单向 paths ← assets ← nas-fs；「影像归入本台账」改为由调用方读出台账传入，
    影像层不再依赖台账存储。
- 回归验证：typecheck 通过，146 个测试全绿，70 个 Excel 往返对拍全过，构建通过。

[1.7.17] 2026-09-13
- 整体复查（见 docs/审查与报告/全量复查-20260913.md）修掉 8 项：
  · 一键更新不再写死 /data：更新任务文件写到 DATA_DIR 下，更新容器的 DATA_DIR 环境变量
    和「数据挂载识别」都按本机实际挂载来（以前自定义容器内挂载点时更新会静默失败；
    找不到数据挂载时只记警告不再猜飞牛默认路径）。
  · saveDoc 改为「新文件 rename 就位 → 写指针 → 清旧文件」：崩溃窗口内旧文件/旧指针始终还在，
    不再两头空（未闭环清单第 9 条闭环）。共享文件（别的 id 指针还指着）不被误删。
  · 读台账的连带开销降下来：ensureDirs 每个「数据目录×台账」只跑一次（原来每次读都
    重复 30 来个 mkdir + 重写说明.txt）；合同扫描件补名在没有缺名合同时不再扫影像目录。
  · 操作记录上限 2000 → 20000，截断最老记录时记日志（以前静默丢）。
  · 登录限速表超限自动淘汰（原来只增不减）。
  · 启动器请求体超限拒绝后不再继续缓冲（慢速灌数据的连接内存不再涨）。
  · Windows 一键更新：安装目录含批处理特殊字符（& | < > ^ % ! " 等）时明确拒绝并提示，
    不再生成坏 .bat。
  · 文档更正：错误文件名是 .gondi-update-error.txt（AGENTS.md/README/使用说明里多写了一个 g）。
- 回归防线 +7：saveDoc 覆盖写/换名/共享保护的往返用例与「rename 先于 sweep」源码守卫、
  ensureDirs 缓存键含 DATA_DIR 的守卫、更新链路不写死 /data/不猜宿主路径的三条守卫。
  测试总数 139 → 146（146 pass + 0 todo）。

[1.7.16] 2026-09-12
- 新增 UI 约定守卫测试（tests/ui-guards.test.ts，3 条）：
  · 用了 useGuardedClose 的弹窗，按钮直接 onClick={onClose/onCancel} 会被测试拦下（防误关回退）；
  · 页面/组件里再写本地 round2 或手写 Math.round(x*100)/100 会被拦下（金额取整唯一来源：wage.ts）；
  · 页面/组件里再定义本地 today 函数会被拦下（当天日期唯一来源：dates.ts 的 localToday）。
  三条守卫均做过负向验证（故意改坏能被抓到）。测试总数 136 → 139。
- 顺手清扫：报销/文件页/文档组件里 3 处手写取整改用 round2；报销页 todayYmd() 包装删除，直接用 localToday()。

[1.7.15] 2026-09-12
- 修复 `pnpm dev` 开发服务器整站 500：SSR 加载 react 报 `module is not defined`。
  根因：`vite.config.ts` 里 `ssr.noExternal: true`（当初为让 Docker 镜像免装 node_modules 而加）在 dev 模式下
  把 CJS 的 react 也内联进 rolldown-vite 的模块运行器当 ESM 跑。
  现在 `noExternal` 只在构建时生效（`command === "build" ? true : []`），dev 恢复默认外部化。
  生产产物验证仍自包含（server.js 无外部 require），dev 全页面 200。
- 文档：未闭环清单里「pnpm dev 起不来」一条闭环。

[1.7.14] 2026-09-12
- 全量复查（操作一致性专项，见 docs/审查与报告/全量复查-20260912.md）：
  · 弹窗防误关补齐：人员/报销/合同/发放/保险五个编辑器的「取消 / 关闭 / ×」此前直接关闭、绕过脏检查，
    填到一半点了照样丢。现在所有出口统一走确认（没改过内容时不打扰）。
  · 保险页弹窗样式与其它页统一（移动端底部抽屉、桌面居中），不再是另一套外观。
  · 编辑器顶部按钮顺序按规范 §6.3 统一：删除 → 辅助操作 → 关闭 → 保存。
  · 金额取整统一：报销/合同页删掉各自复制的 round2，保险页 10 处手写取整，全部改用工资模块带容差的 round2。
  · 考勤月表防丢：改了没保存就切月/返回总览时会先问一句，并有「有未保存的修改」提示。
  · 操作记录页改/删失败不再假装成功（以前不看响应，失败也提示「已保存」）。
  · 保险页当天日期改用公共的 localToday()，删掉重复实现。
- 文档：开发规范 §10 用例数更正为 136+70；docs/README 未闭环清单同步；AGENTS.md 删一条过时待办。
- 已知：`pnpm dev` 在本机因框架层 react CJS 互操作起不来（rolldown-vite 7.3.1 + react 19.2.8），
  生产路径（build + node app/server/index.mjs）正常，待后续排查。

[1.7.13] 2026-09-11
- 全量功能走查（服务端接口 + 权限矩阵 + Excel 往返 + 页面手动走查），共确认并修复 26 个问题。
- 严重（数据 / 权限）：
  · 导出页「总台账」点了必 404：客户端叫 ledger-export、服务端只认 export。服务端加别名 + 客户端改回 export。
  · op=createUser 完全忽略 perms、一律套预设 —— 想给"只看考勤"实际给了整套只读（能导出身份证/银行卡）。现在按 perms 精确分配。
  · 只有 members.manage 的成员能给自己的 addMember 传 perms="*" 提权到全部权限。现在拒绝改自己的权限，
    并且不能授予自己没有的权限。
  · 操作记录文件损坏时 POST 会把整份历史覆盖成 1 条（实测 97 条 → 1 条）。现在损坏时拒绝写入（503），与 PUT/DELETE 口径一致。
  · /api/year 传非数字（year=abc）时 NaN 绕过范围校验，把台账 year 写成 null、years 里塞 null。现在严格校验整数年份。
  · 整本/考勤导入认不出 sheet 标题里的年份（「2025年3月考勤」）→ 恢复旧年备份会整本落到当前年。现在标题也参与识别。
  · 重复导入同一份整本会让发放/报销翻倍。现在按内容键去重，并在确认框里写明跳过多少条。
  · 派生表「工天加班」被当成考勤数据，产生 month=0 的幽灵记录。现在派生表（工天加班/汇总/资金对照/影像资料）统一排除。
  · 保险"在保"人员（结束日期为空）往返后被写成日期列里的「在保」，二次导出变「已结束」。现在日期列只写日期。
  · 手填带格式的数值（1,200 / ¥300 / 300元 / 22天）被静默清成 0。新增容错数值解析，覆盖所有数值列。
  · 「总计」行被当成真实记录（凭空多发/多报一笔）。合计/总计/小计/累计现在一律跳过。
  · 合同：只留「合同管理表」时报量/已付合计丢失；扫描件、有无合同、无合同原因往返丢失；
    明细 sheet 排在管理表之前时明细整批丢；明细空日期被填成 YYYY-01-01；未记税率的开票明细凭空生成不含税金额 —— 均已修。
  · 保险合同文件、被保人备注往返丢失。已补列并读回。
  · 考勤"增加"模式按姓名整人跳过：某人某月冲突会把他所有月份的行一起丢掉。现在按 姓名+年+月 精确跳过。
  · 只有备注、没有工天的考勤行导出即丢（如"工伤休息"）。备注现在也算有内容。
  · 报销金额 0 被"数量×单价"重算、数量 0 被改成 1。单元格有值（含 0）就按值用。
- 健壮性 / 可观测性：
  · 非 JSON / 非 form 的请求体在 7 个接口上会 500（未登录也能打出 500）。现在统一 400，不再解析失败就崩。
  · 上传文件名 ≥255 字节会在写盘时 500（ENAMETOOLONG）。现在友好 400。
  · 伪造超大 Content-Length（声明 60MB 只发 2 字节）会把连接挂住。启动器加了 52MB 请求体上限，直接 413。
  · 未捕获异常只进 stdout，NAS 上按日期翻 data/logs 什么都看不到。现在也写进 data/logs。
- 测试：新增 5 条接口约定守卫（导出 kind 与服务端一致、createUser 读 perms、成员提权防护、读 body 必须有兜底、
  启动器上限与崩溃日志）；把走查用的 70 用例 Excel 往返套件搬进仓库（pnpm run test:roundtrip）。
  pnpm test 136 例全绿，往返套件 70/70。

[1.7.12] 2026-09-11
- 把 `gongdi-updater` 说清楚：它是**一键更新时临时起的容器**（不是镜像），替换完容器就退出。
  1.7.10 起故意不自动删除它，是为了更新失败时还能查它的日志
  （「查看更新日志」里那句「更新容器：已退出（exit N）」就是它）。
  · 它随时可以手动删：不影响台账，也不影响下次更新（下次更新会重新建一个）。
  · 「清理旧镜像」现在会顺手删掉它 —— 否则它作为「已停止的容器」会把上一次更新用的镜像
    一直占住，那个镜像就永远删不掉（几百 MB）。
  · 统计「镜像被谁占用」时忽略已退出的更新容器，只认真正在跑的容器。
- 测试：130 例全绿（todo 0）。
[1.7.11] 2026-09-11
- 人员导出/导入不再丢「调薪历史」（最后一条已知缺陷，测试里的 todo 清零）：
  人员信息表新增「工资历史」列（一段可读的 JSON），导出再覆盖导入会原样回来，
  不会再出现「导出备份 → 改两个人 → 覆盖导入」把调薪历史静默清空的情况。
  这一列被手改坏时按「没有历史」处理，不会连累同一行人员的其它字段；
  导入仍然是「同名跳过 / 可覆盖」，不受影响。
- 测试：129 例全绿（todo 0）。
[1.7.10] 2026-09-10
- 找到并修掉「一键更新一直不生效」的真正原因（1.7.8 / 1.7.9 两版都猜错了方向，这次是实测确认的）：
  更新容器要执行的那段脚本是**语法错误**的。脚本由模板字符串生成，写法里用了 "\n"，
  而模板字符串会把它变成**真正的换行**，于是生成的 .cjs 里字符串字面量跨了行，
  node 在解析阶段直接 SyntaxError 退出（stderr 没人看），容器又被 AutoRemove 删掉 ——
  界面只看到「更新已受理」，容器其实一个都没换、日志里一个字都没有。
  （1.7.6 修的是「创建更新容器时请求体被丢掉」，那次之后容器能创建了，但脚本本身跑不起来，
   所以更新始终没生效。）
  · 脚本改用 log() / fail() 统一写日志，不再有跨行的字符串字面量；
  · 新增**语法守卫测试**：把生成的脚本真的交给 JS 引擎解析一遍（new Function），
    这类「整段脚本没法运行」的错误以后不可能再溜过去。
  · 1.7.8 / 1.7.9 加的「镜像 ID 比对、按提交 sha 拉、读镜像内版本」留着 ——
    用来对付镜像加速站缓存旧 latest 是有用的，只是**不是这次的病根**（之前归因不准确，特此更正）。
- 更新容器不再「一声不响地死掉」：
  · 任务改从环境变量传、脚本走 Cmd 内联，不再依赖 data 挂载（挂载不对也不会静默退出）；
    脚本第一步就先写一行「更新容器已启动」，stdout 也写（docker logs 能看到）。
  · 启动两秒后检查它是否还活着：已经退出就**当场报错**并把它的输出贴出来（不会再默默换个空操作）。
  · 不再自动删除更新容器：失败后它还在，点「查看更新日志」能看到
    「更新容器：已退出（exit N）」以及它自己的输出。
- 测试：新增「脚本必须能被 JS 引擎解析」「更新容器不依赖挂载 / 日志可查」等用例；
  全库 127 例（126 pass + 1 todo）。

[1.7.9] 2026-09-10
- 「更新说成功、重启后版本没变」这次能自动绕开（注意：1.7.10 查明真正原因是更新脚本语法错误，不是镜像缓存；这版的两道保险留着仍有用）
  现场：1.7.7 点更新到 1.7.8，镜像发布 1 分钟后点的，
  拉到的还是 1.7.7 那个镜像——加速站按标签缓存 latest）：
  · 拉镜像的候选顺序改为 ①`<镜像仓库>:sha-<main 最新提交>`（加速站不缓存这种一次性标签，只能回源拉，
    基本一定是最新构建）②各仓库的 `latest` ③环境变量里配置的镜像。
    仓库只发布 `latest` 与 `sha-<sha>` 两种标签（没有版本号标签，见 AGENTS.md），所以「按提交拉」是绕开缓存最有效的一招。
  · 拉完镜像会**把镜像里的 VERSION.txt 读出来**（起一个只跑 `cat` 的临时容器，读完立刻删）：
    版本不比本机新就自动换下一个源；所有源都不是新的就报错并**停止更新**（容器、台账都不动）。
  · 更新日志与状态里都会写「镜像内版本 X」，以后这类问题一眼可判；提示也直说
    「本次拉到的镜像就是 X，是镜像加速站还没刷新，等 5–10 分钟再点一次」，不再只说「版本没变」。
- 测试：新增镜像内版本解析、候选顺序、状态带出镜像版本等用例；全库 122 例（121 pass + 1 todo）。

[1.7.8] 2026-09-10
- 「更新成功、重启后版本没变」不再发生（1.7.9 又加了「读镜像内版本 + 按提交 sha 拉」两道保险）：
  · 拉完镜像先按镜像 ID 比对——若拉到的就是当前正在跑的那个镜像（镜像加速站 ghcr.1ms.run 缓存
    还没刷新，现象就是「更新说成功、版本没变」），就自动改试下一个源（ghcr.io 直连）；
    所有源都还是旧的就明确报错并**停止更新**（容器、台账都不动），不再白重启一次。
  · 提示里直接说明是镜像站缓存，并给出「等 5–10 分钟再试 / 换成 ghcr.io 的镜像」两条出路。
- 更新时顺手清理 NAS 上的旧镜像（一份镜像几百 MB）：
  · 换容器成功后自动删掉上一个版本的镜像（老容器已经删了，镜像没人用，删不掉也不影响更新，只记日志）。
  · 新增「清理旧镜像」按钮（设置 → 软件更新 / 版本号弹窗里，管理员）：先列出可清理的数量与体积，
    确认后逐个删除，并显示释放了多少 MB。历史上积攒的几份一次清掉。
  · 安全边界：只删名字里带 gongdi-ledger 的镜像，且必须「不是当前镜像、没有被任何容器引用」
    （含已停止的容器），不会动到 NAS 上别的容器在用的镜像。
- 补齐三处「日期/号码没校验严」的老问题（都是审计里挂着待办的小缺陷）：
  · 不存在的日期不再入库：`2026-02-31`、`2026-04-31`、平年 `2026-02-29` 一律判空。
    以前只查「日 ≤ 31」，这类日期会被 `new Date()` 静默进位成 3/3，出勤天数被凭空放大
    （`daysBetween("2026-01-01","2026-02-31")` 原来算出 62 天）。闰年 2/29 仍然合法。
  · 调薪生效日 `fromDate` 没补零（`2026-7-1`）也能当月生效：字符串比较会把它排到
    `2026-07-31` 之后，导致那条调薪当月读不到、工资静默回退成当前工资。
    现在比较前统一补零，并容忍 `2026.7.1` / `2026年7月1日` 等写法；乱写的值按无效处理。
  · 身份证 16/17 位不再被当成「还没输完」放行：以前会存进人员资料，
    且 `parseIdCard` 会按 15 位解析出一个错误生日。现在按「15 位或 18 位」报错并提示实际位数。
- 测试：上述三条的 todo 用例转为正式用例（todo 4 → 1），并新增镜像比对/可删镜像筛选/更新脚本
  删旧镜像等用例；全库 118 例（117 pass + 1 todo）。剩下 1 条 todo 是「人员导出→导入会丢调薪历史」，
  属 Excel 往返改造，单独排期。

[1.7.7] 2026-09-10
- 「上次更新结果」直接显示在更新卡片里：成功/失败、开始与结束时间、失败原因一句话；
  失败时额外说明「容器仍在旧版本上跑、台账数据没动」，不用再靠猜。
- 新增「查看更新日志」：在应用内直接看 data/logs/update.log 与 data/.gongdi-update-error.txt
  的最后 120 行（只读、需管理员），不必再开 NAS 文件管理器一层层点进去找原因。
  为此应用侧现在也写同一份 update.log（开始更新 / 拉镜像成功或失败 / 已启动更新容器 / 结果），
  容器重启后依然能看到「上次更新到底怎么了」。
- 测试：新增 tests/update-log.test.ts（并发追加不丢行、只回尾部 120 行、超 64KB 按行截断不切半行、
  未开启持久化时不抛）。

[1.7.6] 2026-09-10
- 修复飞牛「一键更新」真正的原因（本版重点，之前几版都在查这个）：
  创建更新容器时把容器配置当成了 dockerReq 的第三个参数直传，而该参数期待的是 { body: … }，
  于是配置被当成「没有请求体」补成了空对象 {}，Docker 回
  「config cannot be empty in order to create a container」——只要点「更新」就必失败，
  而手动 ./一键拉取.sh 走 docker compose，不受影响，所以看起来像"手动能成、应用内不行"。
  · 现在按 { body: … } 传，并且 dockerReq 对「配置直传」这种历史写法也会兜底识别，
    不会再静默变成空请求体。
  · Docker 的错误体（{"message":"…"}）现在取成一句话显示，界面上不再出现半截 JSON。
- 测试：新增守卫用例，扫源码里所有 dockerReq 调用，第三参数必须是 { body } 或 { stream }，
  防止这个「请求体被丢掉」的错误再犯。

[1.7.5] 2026-09-10
- 「查看」影像资料不再新开浏览器标签页，改成应用内弹窗预览：
  · PDF（合同扫描件、发票、回单等）内嵌显示，图片直接显示；
  · 点「关闭」、点弹窗外的空白处、或者按 Esc 都能关；弹窗里还有「下载」，不用先关再回列表点；
  · 不再出现「看完要手动切回列表再点下一次」，也不会因为链接 60 秒失效而提示「文件不在」。
- 不能内嵌预览的类型（Excel、Word 等）不再新开一个空白标签页，改为在弹窗里明确提示
  「请点下载用本机软件打开」。

[1.7.4] 2026-09-10
- 一键更新改为「立刻返回 + 后台执行 + 前端轮询进度」（反代环境下必须这样做）：
  原来点更新会挂一个 1~5 分钟的长请求，反向代理（nginx 默认 proxy_read_timeout 60s）会把它
  掐断并回一个 HTML 504，浏览器解析 JSON 失败 → 只能显示兜底的「更新失败」，而服务端其实
  还在拉镜像。现在 POST 立即返回「已开始更新」，进度用 GET /api/update?status=1 轮询；
  重复点击不会起第二个更新任务。
- 同源校验兼容反向代理：除 Host 外也接受 X-Forwarded-Host（含逗号列表）。反代若把 Host 改写成
  内网地址，原来会把通过域名访问的合法更新请求判成 403「来源不一致」；被拒时会记日志并说明改法。
- 修复「一键更新」报「更新失败」却查不出原因：
  · 客户端把「请求被中断」和「服务端没给原因」一律显示成兜底的「更新失败」。现在服务端的原因
    会原样显示；请求中断时会去轮询 /api/version 核对真实版本，明确说「已更新到 x」还是「仍是 y」。
  · 更新尝试、拉镜像成功/失败、更新容器启动、后台任务异常都写 data/logs/update.log
    （容器内的更新脚本同样写），失败还会写 data/.gongdi-update-error.txt。
  · 非 Error 型异常不再产生空错误信息（空原因正是「更新失败」这四个字的来源）。
- 飞牛（Docker）更新重建容器更稳（「手动一键拉取能成、应用内更新不行」的另一半根因）：
  · 原来把 inspect 出来的整份网络配置（含 IPAddress / IPAMConfig / MacAddress / Aliases）原样
    喂回 /containers/create，不少 Docker 版本会直接报 invalid endpoint settings；现在只按网络名
    重新挂载。手动更新走的是 docker compose（由 compose 重新组装网络），所以不受影响。
  · 更新顺序改为「创建新容器 → 停老容器 → 启动新容器 → 成功后才删老容器 → 改名接管」，
    任何一步失败都会把老容器拉回来（回滚），不再出现「更新失败后应用直接消失、只能上 NAS 手动救」。
- Windows 解压版更新：原来启动更新脚本后 800ms 就退出进程，HTTP 响应常常还没发到浏览器，
  浏览器看到网络中断 → 界面报「更新失败」，而更新其实已经开始。现在先让响应发出去再退出。

[1.7.3] 2026-09-10
架构审核（A–F）主体落地。本版含**影像存放位置变更**，请先看第 1 条。

A 影像按台账隔离（重要，防跨台账越权）：
- 照片、合同扫描件等影像从「全局公共目录」改为按台账存放：<影像根>/<台账id>/<分类>/。
  以前台账之间能互相读到、覆盖、删除对方的身份证照和合同扫描件；删台账也只删数字数据，影像残留。
- 读取会回落历史公共目录（只读），所以**升级后老照片不会消失**；旧目录里不属于本台账的文件不会被删。
- 新增「设置 → 影像归入本台账」：按本台账的人员姓名与合同/报销 id，把历史公共目录里的影像复制进来
  （只复制、不删除、不覆盖，可随时回退）。全部归入并归档旧目录后，可设 PHOTO_LEGACY_FALLBACK=off 关闭回落。
- 删除台账时会连该台账自己的影像目录一起删除。

B 台账保存更安全：
- 读台账不再回写文件（原来读一次可能顺手改写整本，绕过写队列和版本校验，会覆盖并发保存的新数据）。
- 保存冲突（409）不再只有提示：会拉取最新版本号，让你选「用本机数据覆盖服务器」或「放弃本机改动」。
- 重新加载会覆盖本机未保存改动时，先弹确认。
- 服务端校验台账结构：明显坏掉的写入（例如空对象）会被拒绝，不再可能把整本台账写坏。

C 权限：
- 权限 id 改为单一来源声明表，页面与接口写错权限名会**编译不过**（以前写错只是静默不生效）。
- 敏感导出（人员/整本/考勤/发放）需要「导出权限 + 人员查看权限」，以前只要导出权限就能导出身份证/银行卡。
- 操作记录写入要求有改动权限（只读账号不能伪造/灌水记录）。
- 保存整本台账的权限判据客户端与服务端统一：权限不足会明确提示，不再出现「提示已保存其实没存上」。

D 可观测性与数据自保：
- 新增服务端日志：data/logs/日期.log（同时输出到容器日志）。权限拒绝、保存冲突、台账/账户数据读取失败、
  操作记录写入失败都会留痕——以前这些错误被静默吞掉，事后无从查起。
- 台账文件、账户文件损坏时不再当成「空数据」：拒绝写入并在前端显示专门的提示页，
  不再引导「初始化管理员」（那会把损坏但可能可恢复的 accounts.json 覆盖掉）。
- 账户文件、操作记录文件改为随机临时名 + 串行写入（并发时不再互相覆盖或截断）。
- 操作记录补全：影像上传/更换/删除、合同明细增删、导入替换、单条发放删除等以前不留痕的操作，现在都记。

E 交付：
- app/ 标记为构建产物（GitHub 语言统计与 diff 折叠），「产物必须与源码同步」由 CI 校验。
- 发版与合并解耦的做法写在 ci/README.md（需在 GitHub 网页改 workflow）。

其他：新增 90 个零依赖回归测试（pnpm test）与 CI 质量闸门（ci/check.workflow.yml）。

[1.7.2] 2026-09-10
- 修复「操作记录」丢记录（重要）：审计写入原来用固定的临时文件名（audit.json.tmp）且读—改—写不排队，
  连续操作时两次写入会互相搬走对方写了一半的文件，rename 失败 → 这条记录就没了；客户端又把失败静默吞掉，
  表现就是「老记录（登录/创建管理员）看得到，新操作一条都不进去」。现在临时名带随机后缀并串行写入
  （实测 20 次并发全部落盘），写失败会明确提示并记服务器日志。
- 操作记录补上影像类操作：上传/更换/删除照片、合同扫描件、发票、回单等，以前完全不记。
- 老版本放在 data/audit.json（不分台账）的历史记录现在能读回来，并在下一次写入时自动并入。
- 管理员改/删操作记录时，若读取结果为空则拒绝写入，避免一次读取失败把历史清空。
- 照片上传/删除失败不再提示「已保存」：以前不看返回值，失败也报成功（照片其实只在浏览器里）。

[1.7.1] 2026-09-10
- 台账保存（重要）：修复「空台账存不进去」——服务器上还没有 ledger.json 时（全新安装、新建台账），GET 返回的版本号与保存时的校验基准口径不一致，第一笔保存被误判成「台账已被其他设备修改」并永远 409，数据只留在浏览器里。两端现在统一走同一个版本函数。
- 新建 / 切换台账：先保存当前台账未推上去的改动，再切台账；切到或新建的数据为空时清掉上一本台账的残留内存数据，不再把上一本台账整本复制进新台账（也避免切换瞬间把旧状态推给新台账）。
- 工资条打印：已打款只算打进本人名下的款，代收他人的钱单列一行；不再出现「未打款」为负数。
- 批量设置工资：勾选「记为工资历史（今天生效）」时，对没有调薪记录的人自动补一条调薪前基准历史，过去月份真正保持原工资，不再被追溯重算（与确认框文案一致）。
- 合同导出再导入：报量金额不再被误取「含税金额」而放大、开票金额不再翻倍、收款单号与影像文件名不再丢失；导出的「资金对照」「影像资料」两张派生表不再被当成数据表重复导入。
- 整本台账导入：跨年度导出的考勤按各 sheet 名里的年份入库，不再全部落到第一年。

[1.7.0] 2026-09-09
- 总览超龄提醒展开名单：新增「超龄人员名单」标题；每行显示姓名、性别、生日（年月日）、身份证号（打码）。
- 版本号按规范进位（Z 上界 19）：1.6.19 之后应为 1.7.0；此前误发的 1.6.20 不再延续，本版起按 X.Y.Z 规则推进。

[1.6.20] 2026-09-09
- 总览超龄提醒展开名单：身份证号做中间位打码（如 1101********1210）；列表加高度上限与滚动（超龄人多时首页不被拉长）。

[1.6.19] 2026-09-09
- 总览「超龄提醒」可点击展开：显示超龄人员的姓名与身份证号（男≥55 / 女≥45）。

[1.6.18] 2026-09-09
- 人员信息导出/整本台账导出补「餐补/天」列（1.6.15 给导入模板加列后导出方向漏了，导出再导入会丢餐补）。
- 批量工资：按月计薪时工资历史条目的日工资保留原值，不再误写为月薪数值；「只填空白」确认文案说明会同时记一条今天生效的工资历史。
- 个人查询打印：仅打款记录模式标题改为「打款记录」并隐藏工资/加班背景字段；仅工资模式只印有出勤月的人（避免空单）。

[1.6.17] 2026-09-09
- 个人查询打印新增「仅打款记录」模式：打印内容三选一（工资+打款记录 / 仅工资 / 仅打款记录）。仅打款记录只印该区间有打款记录的人，隐藏工资明细与应发说明，便于单独核对打款。

[1.6.16] 2026-09-09
- 报销单打印：打款账户（银行卡号）每 4 位一组加空格（如 9999 9999 9999 999），便于核对；页面显示与编辑仍保留连续数字。

[1.6.15] 2026-09-08
- 人员导入模板补「餐补/天」列（之前模板缺列，导入后餐补只能手填）。
- 移除仓库内旧静态模板（app/public/templates/*.xlsx，8 月旧结构且无代码引用）：空白模板一律由软件内「下载模板」按钮实时生成，列结构与当前版本同步；README/目录结构说明同步更正。
- 核对确认：考勤/发放/报销/合同/保险人员模板列结构均与最新导入字段一致。


[1.6.14] 2026-09-08
按《收官体检报告-20260908》处理：
- 批量工资：升级按钮前确认（写明人数与改动）＋默认「记为工资历史（今天生效）」选项——勾选后过去月份保持原工资、不追溯重算；提示文案讲清影响。
- 按区间导出：起止选反时自动交换并提示，不再静默导出空表。
- 考勤模板年份缺省改为当前年（不再写死 2026）。
- 影像资料搜索对缺失字段做防御（不再因旧数据缺字段抛错）。
- 照片上传口径说明：文件选择器为显式操作，不加二次确认；拖拽上传保留原有确认。


[1.6.13] 2026-09-08
终审遗留 3 项修复：
- 合同列表合计行标签修正（主行「含税」、次行「不含税」），数值与标签对应。
- 合同状态「完成」纳入绿色集合（与完工/结算完成一致）。
- 身份证 15 位老证校验分支移到长度判断之前（此前不可达，15 位证实际未校验出生日期）。


[1.6.12] 2026-09-08
按《审查报告-UI与数据逻辑-20260908》逐条修复：
数据：
- 工资条「打款记录」按查询区间过滤（与屏幕口径一致，不再出现负数未打款）。
- 合同列表合计行分列「含税/不含税」两个数，与各行口径对得上。
- 身份证出生日期防 JS Date 溢出（2 月 30/31 不再通过校验）；查询起止日按当月天数校正（不溢出到下月）。
- 工资条头部有工资历史时显示「按各月生效工资（见明细）」，不再用当前值误导。
交互：
- 合同编辑器上传扫描件前，若有未保存改动先确认，不再静默整单保存。
- 考勤「单删」与「批量删」统一：确认后立即保存本月。
- 整本导入新增确认清单（各项条数），不再选中即写入。
- 人员导入对文件内重名去重（只留第一行，其余按重复处理）。
- 合同状态标签色调按中文状态修正（完成类绿色）；开票表单切换合同时重置；出勤天数超 31 提示；照片上传挂档案原名（改名未保存不再错挂）。


[1.6.11] 2026-09-08
终审微修：改密/重置密码与登录口令统一 trim（首尾空格不再导致账户锁死）；scrypt 哈希与校验口径与旧版完全一致。


[1.6.10] 2026-09-08
按《代码复审报告-20260908》核验修正：
- README 版本同步到 1.6.10；注明「多设备避免同时编辑」。
- 安全：账户密码改 scrypt + 随机盐（新密码写入即用；旧密码登录后自动升级，改密/重置也全用新格式）；登录/验证支持旧格式兼容比对。
- 修复：removeMember 跨台账越权（非管理员只能操作自己有权管理的台账）；保单号去重条件与注释一致（无 id 新增也按保单号合并/拦截）；更新检查接口需登录；excel.ts 提取 EntryInput 类型别名（消除 4 处断言）。


[1.6.9] 2026-09-06
按《代码审查纪要》逐条处理：
- 依赖可复现性由已入库的 pnpm-lock.yaml 保证（package.json 保留 latest：当前 pnpm 对 h3-v2 别名依赖重新解析会失败，故不改为固定版本）；README 当前版本同步。
- 整本合同导入替换时清理旧合同的条目（不再残留孤儿数据）。
- 人员保存对空 id 防御（同名重复建档更保险）。
- 工资历史匹配改用「当月最后一天」：29/30/31 号生效的调薪当月即生效。
- 更新错误信息放宽到 500 字符（Docker 报错不再被截断吞掉）；清理无效代码。

[1.6.8] 2026-09-06
- 清单表格列宽自动贴合内容（不再左右拉伸留白），字体不截断；内容超出容器时横向滚动，窄表居中显示。
- 覆盖：人员/发放/照片/影像资料/查询等列表页及所有宽表（考勤、保险人员、合同、报销）。

[1.6.7] 2026-09-06
UI 布局调整：
- 内容区大屏居中（max-w-7xl），不再贴左。
- 移动端顶栏更挤时横向滑动，不再挤压品牌与末尾下拉；极窄屏隐藏用户姓名块。
- 「已有同名文件」弹窗与主题一致（不再固定经典蓝白）。
- 照片/导出页筛选按钮统一为胶囊规格；筛选按钮可访问性标签补齐（台账下拉、菜单按钮）。
- 浏览器地址栏/手机顶部主题色跟随所选的界面风格。
- MOVIEPILOT 横幅渐变改用专用类（不再全局拦截渐变工具类）；次要文字色各主题加深一档。

[1.6.6] 2026-09-06
全部界面按新版设计统一完成：
- 团体保险「保单」改为卡片网格：保单号/公司/状态标签 + 保险期、保费、在保人数、组合险，点卡片查看人员，编辑/删除在卡内。
- 设置页改为两列卡片网格（界面风格、账户成员、密码、年度、批量工资、数据、软件更新各占一卡）。
- 其余页面（人员/考勤/发放/报销/合同/导入导出/查询/照片/影像/记录）标题、筛选、表格均已按统一设计风格，与四套主题自动适配。

[1.6.5] 2026-09-05
全面复审修复（安全 / 数据 / 界面）：
安全：
- Excel 依赖升级（SheetJS 0.20.3）：修复原型污染与 ReDoS 两个高危。
- 台账接口权限收窄：读取全量要「人员查看」；写入整本要「人员/考勤编辑或设置」权限，照片/文件权限不能再清库改账。
- 登录/密码验证限速：同一用户连错 5 次锁 5 分钟；新密码最少 8 位（老密码不变）。
- 会话令牌改为随机会话盐派生，不再随密码推导；改密码后其他设备自动下线；Cookie 加 HttpOnly。
- 账户与审计文件改为原子写（断电/强杀不再截断损坏）；上传/备份读取前先限体积（照片 20MB、文件与备份 50MB）。
- 更新仅 POST + 同源校验；非法 URL 编码返回 400；加 nosniff 安全头；照片删除按姓名精确匹配（不再前缀误删）。
数据：
- 保险替换：已结束人员不再被延长保险段（取原结束与替换日前一日的较早者），提示说明。
- 保单号重复：新增/改号撞车直接拦截并提示，不再静默覆盖。
- 保险使用天数夹紧到保单期内，手填越界不计费。
- 整本 Excel 导入现在能恢复保险：保单（含组合险挂接）/ 保险人员按保单号去重回填。
- 考勤导入按行内年份导入（跨年度表格不再全归最早一年）。
- 人员编辑改名重名拦截（与总览/Excel 口径一致）；默认日期统一本地时区（东八区早晨不再差一天）。
界面：
- MOVIEPILOT 风格语义色加深（警告/成功/危险对比度）；登录/建管理员等门禁界面跟随所选主题；设置页底部补版本号与更新记录入口；主题底色同步到页面根元素。

[1.6.4] 2026-09-05
- MOVIEPILOT 风格改为白色系：白底 + 粉·紫·蓝彩色渐变点缀，大圆角活泼卡片（不再用暗黑）。
- 界面风格四套：原版 / 新版 / 苹果 Mac / MOVIEPILOT（白底彩色渐变）。

[1.6.3] 2026-09-05
- 新增「MOVIEPILOT 风格」：白底彩色渐变卡片风格，侧栏/顶栏白色磨砂。
- 界面风格现有四套随时切换：原版 / 新版 / 苹果 Mac / MOVIEPILOT。

[1.6.2] 2026-09-05
- 界面风格改为下拉菜单选择，点「保存」才生效；默认原版界面。
- 新增「苹果 Mac 风格」：系统蓝 + 苹果灰背景、毛玻璃侧栏/顶栏、无边框大圆角卡片、更柔和的阴影。
- 设置里三套风格随时切换：原版 / 新版 / 苹果 Mac。

[1.6.1] 2026-09-05
- 设置里可切换「新版界面 / 原版界面」：只影响显示，不动数据，随时可换。
- 新版总览去掉折线图和环形图，保留 KPI 卡、班组进度条、快捷入口。
- 检查更新移出顶部栏，放到「设置」页最底部。

[1.6.0] 2026-09-05
界面全新改版（参考 B 端仪表盘风格）：
- 登录/建管理员/账户登录改为品牌渐变卡片。
- 主界面改为：左侧图标导航 + 顶部栏（台账/年份/版本/退出），移动端底部标签栏不变。
- 总览改版：渐变横幅 + 彩色 KPI 卡 + 月度应发/发放折线图 + 应发构成环形图（数据实时计算）+ 班组进度条。
- 全局主题：浅紫蓝渐变背景、大圆角卡片、胶囊筛选、状态标签统一。
- 其余模块页面沿用原有布局，自动适配新主题。

[1.5.7] 2026-09-05
打印规范统一：合同对账单的报量/开票/收款小表和整张对账单整块不拆页，与保险汇总、工资条排版习惯一致。

[1.5.6] 2026-09-05
保险打印：班组汇总表整体不跨页——明细清单可以分多页，汇总表完整跟在后页，不再被拆开；行高压缩更紧凑。

[1.5.5] 2026-09-05
保险打印：人员清单与班组汇总尽量同页打印（两表之间留空），整页放不下时才自然换到第二页。

[1.5.4] 2026-09-05
团体保险打印清单底部新增「按班组汇总」：按队长分组统计人数、累计人天、保费金额，附合计行，与清单筛选（队长/在保状态）同步。

[1.5.3] 2026-09-05
细节修复：
- 身份证号 15/17 位也能校验出生日期，不只看 18 位。
- 保单号重复自动拦截，导入保险人员若姓名相同按编号合并，避免重复档案。
- 发放统计、文件下载、导出等权限与模块权限对齐（查看者也能下载对应模块的文件）。
- 考勤编辑一行一人的表格行号稳定，表格滚动不串行。
- 保存到服务器失败不再静默；上传/下载文件名清理换行等特殊字符。
- 保险导入提示语与模板一致（姓名/队长/备注）。
- Windows 启动：先等服务就绪再打开浏览器，停止脚本返回码修正；打包脚本校验 node.exe 完整性。
- 构建产物改为先暂存再整体替换，避免中途故障留下半成品目录。

[1.5.2] 2026-09-05
权限补全：合同/保险/报销/考勤文件与导出按类型校验查看/编辑/导出权限；备份接口加权限与大小限制。
修复：发放统计口径统一、报销重挂账凭证不失效、人员确认弹窗含工资历史、批量工资「只填空白」不再改计薪方式和加班规则、无日期发放导出不重复、保存到服务器失败会提示。
Excel 导出和备份加入保险保单/保险人员两个表。
Windows 启动脚本与一键脚本目录清单更正（中文目录 + 保险合同）；删除冗余的 docker-workflow.yml。

[1.5.1] 2026-09-05
新增《开发规范.md》：版本号、发布流程、数据模型、计算逻辑、API 安全、UI 统一协议（页面骨架/表格/弹窗/按钮/筛选/打印/文案）等强制约定，后续所有改动照此执行。
统一各模块空状态文案（操作记录/发放/个人查询）。

[1.5.0] 2026-09-05
修复 Docker 自动更新失败：程序调用 Docker 接口时，POST「无请求体」的调用会触发 Docker 报「invalid JSON: got EOF」，现统一补最小空 JSON，拉镜像/停容器/建容器/启动都正常。

[1.4.19] 2026-09-04
安全与数据修复（全面审计后集中修复）：
- 修复静态文件路径穿越漏洞（../ 可读服务端文件）。
- /api/year 增加登录与权限校验；软件更新仅管理员可触发。
- 人员保存只按编号匹配，不再因重名覆盖他人档案。
- 删除保单自动清理其它保单的组合险挂接，避免孤儿数据增殖。
- 考勤导入「替换」模式修复冲突人员被丢弃的问题。
- 台账数据改为原子写入，避免写一半损坏。
- 修复合同「导出对账表」按钮点击崩溃、状态下拉为空。
- 工资历史：生效日期为空不再参与匹配（编辑时校验必填）。
- 总览「应发合计」跳过已删除人员，与考勤汇总口径一致。
- Windows 更新包根目录补放启动/停止脚本；镜像按版本号打标签。
- 清理历史遗留的 1.2.23 版本常量；类型检查清零（23 个错误全部修复）。

[1.4.18] 2026-09-04
团体保险日期只显示日期、不显示时间：清单和打印里的开始/结束只显示年月日，编辑时只选日期（时间固定开始 00:00、结束 23:59）。

[1.4.17] 2026-09-04
「被保人」改叫「参保人员」，标题移到人员清单表头上方一行（保单号跟在后面）。

[1.4.16] 2026-09-04
被保人标题后的保单号加「保单号：」标签，一眼看懂。

[1.4.15] 2026-09-04
团体保险：
- 增加「在保 / 已结束」状态筛选，配合队长筛选一起用。
- 清单和打印都显示保费合计（累计人天 + 保费合计），筛选后合计按筛选结果计算，不筛选时等于总保费。

[1.4.14] 2026-09-04
修复组合险同步 bug：之前同步时会把来源保单的人员名单丢掉，导致组合后其中一个保单人员不显示、只算一个保险。现在同步时保留来源保单名单，再复制到挂钩保单，两张保单都正常显示人员、各自结算。

[1.4.13] 2026-09-04
保险导入模板简化：只填「姓名/队长/备注」，不用填开始/结束日期，导入时自动用保单的保险期起止。替换仍是单个手动替换。

[1.4.12] 2026-09-04
修复导入日期差一天：Excel 日期不再转成 Date 对象（避免时区导致提前/延后一天），改为按序列号用 UTC 解析。人员/考勤/发放/报销/合同/保险导入统一修复。

[1.4.11] 2026-09-04
团体保险组合险：挂钩改叫「组合险」，只需在一个保单上选一次，系统自动把另一张保单反向挂上（互相挂钩），换人/增删/导入双向同步。

[1.4.10] 2026-09-04
团体保险替换规则细化：
- 替换人：开始 = 生效当天 00:00，结束 = 保险到期时间。
- 被替换人：结束 = 生效前一天 23:59。
- 「在保」显示改为「在保 X 人 · 已结束 Y 人」，更直观。

[1.4.9] 2026-09-04
团体保险打印简化并修复乱版：
- 只打印 姓名/队长/开始时间/结束时间/使用天数/保费，按队长筛选结果打印，打印当前所选保单。
- 修复打印时页面其它内容混入（加 no-print），并设置 A4 页边距，减少浏览器默认页眉页脚干扰。

[1.4.8] 2026-09-04
团体保险：
- 导入人员时，没填开始/结束日期的默认用这个保单的保险期起止。
- 「在保」判定改为：没有结束日期、或结束日期还没到今天 = 仍在保（导入后不再显示在保 0）。

[1.4.7] 2026-09-04
团体保险：
- 新增「打印清单」：打印某个保单的人员名单，含每人开始/结束时间、使用天数、保费金额。
- 挂钩的两个保单共用一套名单：新增/替换/删除/导入会双向同步到挂钩保单。

[1.4.6] 2026-09-04
团体保险「在保」显示更清楚：加「人」单位，表头「在保/总」改为「在保/共」。

[1.4.5] 2026-09-04
团体保险：
- 开始/结束时间精确到时分：开始默认 00:00，结束默认 23:59，按实际时长算天数/保费。
- 上传的保险合同自动命名为「保单号-购买公司-开始年月日」。
- 支持两个保单挂钩：人员清单同步，替换/新增/删除/导入会一起作用到挂钩保单（各自按自己的保险期结算）。
- 导入模板说明更清楚：结束日期留空 = 仍在保，填了 = 已结束。

[1.4.4] 2026-09-03
所有编辑弹窗防误关：改过内容后点遮罩或按 Esc 会先问「有未保存的更改，确定关闭吗？」，确认才关；没改内容则直接关闭。避免鼠标滑出窗口把填到一半的内容丢掉。
团体保险：结算列改称「保费」，标注公式「每人每天 = 每人保费 ÷ 保险期天数」「每人保费 = 每人每天 × 使用天数」；Windows 启动时预建「保险合同」目录，保单资料统一落到 data。

[1.4.3] 2026-09-03
团体保险增强：
- 保单新增：购买保险的公司（手填）、每人保费、人数、保额/人（额度）、总保费（自动=每人保费×人数）。
- 结算按天算到每个人头上：每人每天保费 = 每人保费 ÷ 保险期天数；每人结算 = 每天保费 × 该人使用天数，列表和汇总都显示。
- 保单支持上传多份保险合同（一个保单可上传 2 份及以上），可查看/下载/替换/删除。
- 模块仍独立，不与任何其它模块挂钩。

[1.4.2] 2026-09-03
新增「团体保险」独立模块：保单管理（保单号/名称/保险公司/保险期起止）、被保人管理、按队长筛选、保险期天数与个人使用天数统计。
- 被保人与「人员」模块完全隔离，每个人挂在某个保单下。
- 支持「替换」：点某人替换，填新姓名和开始日期，系统自动把原人结束日期 = 新开始日期。
- 支持 Excel 导入被保人（增加/替换可选），模板含姓名/队长/开始/结束/备注。
- 修复合同表头「序号/项目名称」吸顶列背景色与其它表头不一致的问题。

[1.4.1] 2026-09-03
导出页样式与导入页统一：导出项改成卡片布局，每项带标题和说明，导出按钮风格一致。

[1.4.0] 2026-09-03
操作记录页新增按时间区间导出：选起止日期（可只填一头），点「导出」把该区间记录存成 Excel。列表与导出共用同一套筛选。

[1.3.19] 2026-09-03
操作记录改成分页，默认 20 条/页，可切换 10/20/30/40/50 条，不再一页全部铺开。
导入导出拆成两个页面：「导入」单独一页，「导出」单独一页，各自按权限显示。

[1.3.18] 2026-09-03
身份证号校验：
- 输入满 18 位后自动校验格式、出生日期和第 18 位校验码。
- 校验不通过时字段下方红字提示，保存时拦截。
- 自动生成性别、年龄、生日功能保持不变（零联网、零成本）。
- 人员编辑器新增「工资变更历史」区域，可添加多条工资记录。
- 考勤计算自动根据年月匹配对应的工资记录，变更日前后工资自动切换。

人员餐补功能：支持按正常出勤天数计算餐补，加班折算的工天不算。
- 人员表新增「餐补/天」字段，在人员编辑器工资区域设置。
- 工资历史记录也支持餐补，变更日期后自动切换。
- 考勤月表新增「餐补」列，汇总显示总餐补。
- Excel 导出考勤表包含餐补列。

导入功能增强：人员、考勤、发放、报销、合同五个模块支持「增加/替换」模式。

Windows 版本修复：
- 启动.bat 等待服务就绪后再打开浏览器。
- 首次运行自动创建 data 目录结构。
- 绑定 127.0.0.1 避免防火墙拦截。
- 支持文件夹任意改名/移动。

[1.2.25] 2026-09-02
修复软件内「检查更新/更新」时空请求体导致 invalid JSON 报错，升级 POST 请求改为发送空 JSON body。

[1.2.24] 2026-09-02
统一操作按钮为「编辑/删除」。报销单打印默认不显示「票据」列，打印前可勾选。个人查询姓名支持输入+下拉选择并增加清空按钮。照片上传后显示缩略图预览。修复 Docker 镜像无需 node_modules 即可启动，支持界面一键更新容器。
整理出可读源码目录 src/，按页面和模块分开，便于备份和交给别人改。飞牛仍运行 app/，台账 data 不动。

[1.2.22] 2026-08-31
版本号后面带发布日期，例如 1.2.22 2026-08-31。

[1.2.21]
检测更新改走国内加速，超时不再卡住。点「检查更新」会重新向 GitHub 查询。仓库已经公开仍然提示连不上时，再点一次即可。本机版本读软件自带说明，不再被 data 里旧文件带偏。

[1.2.20]
报销单去掉筛选/打印用日期，编辑里不再放大图，列表和编辑只留查看/下载/复制/删除。挂账可取消，已报销也可标回未报销。几笔一起打款的凭证改叫「收报销款-金额-几笔」。上传遇到同名文件会问替换还是增加，增加会在后面加 -2。

[1.2.19]
导入导出主按钮改叫导出总台账。可按全部年份、按年或按区间导出，并分项导出人员、考勤、发放、报销、合同。

[1.2.18]
GitHub 有新版本时左下角会提醒。飞牛点更新会自动拉镜像并重启容器，Windows 仍下载解压包。台账 data 不会动。第一次请先运行一次一键拉取。

[1.2.17]
照片管理点「更改」弹出编辑，和发放、报销一样。点遮罩或 Esc 关闭。

[1.2.16]
发放编辑弹窗不再三列挤在一起。导入导出补全：年台账、人员、发放、报销、合同都能单独进出，字段按台账里实际有的全部写出。

[1.2.15]
发放记录搜索只查实际收款人和收款人，不再把发放方、备注算进去。搜「张明明」不会再列出发放方带这个名字的其他人。

[1.2.14]
合同弹窗可用 Esc 关闭，顶部保存栏滚动时仍钉住。上传合同/报量/发票/回单文件名不再弄错。总览「已发放」不计待发放。

[1.2.13]
报销、发放、合同点「更改」改为和人员一样的弹窗。点遮罩或 Esc 关闭。

[1.2.12]
报销、发放、合同：点「更改」编辑框直接出现在这一行下面，不用滚到表底。

[1.2.11]
发放记录点「更改」会滚到表下面的编辑框，和报销单一样。

[1.2.10]
发放记录分页贴在明细表上下，不再被入账汇总挤没。点「更改」页面滚到编辑框，和报销单一样。

[1.2.9]
报销、发放、合同、人员清单可选 10/20/30/40/50 条/页。点「更改」编辑区在表上面，不用翻到底。

[1.2.8]
发放顶部常驻填写框去掉。右上角「新增发放」或行里「更改」才出编辑。报销/发放/合同/人员：「更改」在勾选框右边，点一行是勾选。

[1.2.7]
发放记录点一行可改实际收款人、收款人、日期、金额、发放方、备注，保存要确认。报销/发放/合同/人员明细：点一行是勾选，点「更改」才打开编辑。

[1.2.6]
拖文件上传要先确认。版本号显示成「版本号：x.x.x」，缩放跟着页面走。报销清单底下有合计：不勾是当前筛选，勾了是已选。

[1.2.5]
打印报销单：表头四列是报销人、收款人、开户行、打款账户；清单只留序号、项目、购买时间、金额、备注、票据。编辑里卡号改叫打款账户，和打印一致。

[1.2.4]
「给谁报销」改成收款人，和开户行、卡号绑在一起，不跟人员名单挂钩。填过一次下次可下拉选。几笔可一起打到同一个收款人。未报销打款日期为空。打印报销单带收款人、开户行、卡号。

[1.2.3]
上传同名文件不再覆盖，会另存成「原名-2」。只有点更换/替换才换掉这一条自己的文件。几笔共用一张凭证时，改其中一笔不会冲掉别人的。按钮更紧凑，手机上编辑区按钮会折成两列。

[1.2.2]
去掉「载入示例 11 笔」。现金购买也可以上传凭证，不强制；转账/微信等仍然要传。

[1.2.1]
报销单能正常打开。编辑区和合同页同一套按钮：关闭、打印报销单、删除、保存。删除是醒目的红底。人员列表里单人删除也改成红底。

[1.2.0]
报销单：侧栏「报销单」，现金不用传凭证，其他支付要传。凭证按「项目名称-金额」存在 data/photos/报销凭证。几笔可以共用一张。一起报销要记报销人、给谁、打到哪个账户，共用一张打款凭证，存在 data/photos/报销打款。搜索报销人或账户可查。未报销可勾选、按日期打印。打印报销单在「影像资料 打印/不打印」前面，按钮和合同页同一套。

[1.1.6]
上传合同电子版后立刻记为有合同，不用再点保存。
磁盘文件名就是「项目名称-合同电子版」，拷到 NAS 也能自动认出来。
合同列表只显示报量金额和开票金额（按该行含税/不含税）。
扫描件列不会再被收没。Excel 里的「无合同原因」会写进备注。
导出的合同表也改成报量金额、开票金额。

[1.1.5]
合同电子版改成「保存合同信息」正下方的整条虚线框，绿色大按钮「上传合同电子版」，点合同名称会滚到这里。

[1.1.4]
保存合同/人员时弹出简要确认窗，列出改了哪些项。
项目进度增加「结算已开票」。
对账单不再显示经营人员。
报量按含税/不含税只显示对应金额，开票显示开票金额。
合同扫描件按钮在「保存合同信息」旁边。人员弹窗保留 X，可用 Esc 关闭。

[1.1.3]
合同扫描件文件名改为「项目名称-合同电子版」，例如 某某住宅-合同电子版.pdf。

[1.1.2]
合同扫描件按项目名称存盘。有文件就是有合同，没传就是无合同；原因写在备注。

[1.1.1]
有无合同按是否已上传扫描件自动显示。没传就是无合同，原因写在备注，不再单独必填。
合同编辑区最上面上传扫描件。点合同名称会滚到这里。

[1.1.0]
合同原件可上传扫描件，存到 data/photos/合同扫描件。
