# 审查报告：UI / 交互与数据逻辑（2026-09-08）

> 范围：`src/routes` 全部页面、`src/components`、`src/lib` 计算与导入导出链路。基线：1.6.11（提交 9176400）。
> 已验证：`tsc` 0 错误、`vite build` 通过；两处 Date 溢出行为已用 Node 实测确认。
> 先说好的：权限门控（Can/Need + 服务端 withTenant 双层）、删除/保存的二次确认与「改了 N 项」差异预览、编辑弹窗防误关、Excel 导入的冲突处理面板、保险天数夹紧到保单期——这些都做得扎实，下面只列问题。

---

## 🔴 数据逻辑（建议修）

### 1. 打印工资条的「打款记录」不按查询区间过滤 —— 签字单据数字对不上
- **文件**：`src/routes/query.tsx` L90-100（`buildSlips`）
- **问题**：工资条页面写「按上面查的 {区间} 生成」，月份明细确实按区间，但打款记录是 `payments.filter(x => x.owner === name && x.date)` —— **全部历史打款**，不过滤区间。于是单子底部「已打款合计 ¥X · 未打款 ¥(区间应发 − 全部历史已打款)」，老工人历史打款多时会打出**负数未打款**。
- **对比**：屏幕上的区间查询（L310-315）是按起止日期过滤的，屏幕和打印件两个口径。
- **修复**：`buildSlips` 的 pays 按 span 起止（或起止日）过滤，与屏幕口径一致。

### 2. 合同列表「报量金额」合计行与各行口径不一致
- **文件**：`src/routes/contracts.tsx` L472
- **问题**：行内按合同各自口径显示（含税合同显示含税、不含税显示不含税），但合计行是 `totals.reportIncl ? money(totals.reportIncl) : money(totals.reportExcl)`。`splitTax` 对任何报量都算出含税值，所以**只要有报量，合计永远显示「含税合计」**——全部合同都是不含税录入时，各行加总 ≠ 合计行（差一个税点）。
- **修复**：合计行分别列出「含税合计 / 不含税合计」两个数，或按行口径汇总。

### 3. 身份证出生日期校验被 JS Date 溢出绕过（已实测）
- **文件**：`src/lib/idcard.ts` L46、L52-53
- **实测**：`new Date("1990-02-31T00:00:00")` → 1990-03-03（溢出进位，不是 NaN）。`validateIdCard` 用 `isNaN` 判断「出生日期无效」，**2 月 30/31 日这类错号能通过校验**，且 `parseIdCard` 的生日、年龄、性别随之全错。
- **修复**：构造后回读比对：`if (birth.getMonth()+1 !== 月 || birth.getDate() !== 日) return "出生日期无效"`。

### 4. 查询页起止「日」可溢出到下个月（已实测）
- **文件**：`src/routes/query.tsx` L283-290 + `src/components/ym-pick.tsx` L87（日固定 1–31）
- **实测**：截止选「2 月 31 日」→ `new Date(2026,1,31)` = 3 月 3 日，isNaN 拦不住 → **打款区间多算 3 天**（起始日同理）。
- **修复**：YmPick 的日选项按当前年月动态生成（28/29/30/31），或在 query 里 clamp 到当月最后一天。

---

## 🟡 交互一致性

### 5. 合同扫描件上传会隐式保存整个表单
- **文件**：`src/routes/contracts.tsx` L707-714（`ContractScanBox onFileName → onSave(next)`）
- **问题**：在编辑器里改了合同金额/税率还没点「保存」，此时上传扫描件，`onSave(next)` 把**全部未确认改动一起静默保存**，绕过了 confirmEdits 的「改了 N 项」确认。与「保存合同信息」按钮的口径不一致。
- **修复**：上传只回写 `scanFileName` 一个字段（调 store 的 patch 而非整单 onSave），或上传前提示「会先保存当前改动」。

### 6. 考勤：单个删除不保存，批量删除立即保存
- **文件**：`src/routes/attendance.tsx` L343-356
- **问题**：点行尾垃圾桶（removeAt）只改本地状态，要再点「保存本月」才真正生效——但行已从屏幕消失，用户直接离开会认为已删（实际没删）；而「删除所选」（removeSelected）却**立即保存**。两个删除路径行为相反。
- **修复**：统一为立即保存，或统一为「待保存」并在页面上提示有未保存改动。

### 7. 整本导入无预览、无确认
- **文件**：`src/components/excel-import.tsx` L482-559（`FullBookImport`）
- **问题**：选中文件后立即合并写入人员/考勤/发放/报销/保险并触发 NAS 全量推送。其他四类导入（人员/考勤/发放/合同/报销/保险人员）都有确认面板，唯独风险最大的整本导入没有。
- **修复**：至少加一步 `confirm("将新增 X 人、考勤 N 条、发放 N 条…，确认导入？")`。

### 8. 人员导入对文件内重名不去重
- **文件**：`src/components/excel-import.tsx` L56-66（`PeopleImport.onFile`）
- **问题**：`byName` 只含现有人员；Excel 里同一名字出现两行且都不在册时，两行都进 `fresh`，导入后产生**两个同名档案**（页面编辑器是拦同名的，导入路径绕过了）。考勤/发放/照片都按姓名关联，同名档案后续全部串号。
- **修复**：`fresh.push` 前用 `seen` 集合拦第二次出现的名字，归入 conflicts。

### 9. 工资条头部显示「当前工资」，与明细的历史工资对不上
- **文件**：`src/routes/query.tsx` L163
- **问题**：`工资：{wageLabel(s.person)}` 取的是**现在**的工资；调薪过的人，明细行按各月历史工资计算，头部却显示最新值，工人对着单子会问「到底哪个数」。
- **修复**：有工资历史时头部改显示「按各月生效工资（见明细）」。

---

## 🟢 小项（不急着修）

| # | 位置 | 问题 |
|---|---|---|
| 10 | `contracts.tsx` L455 | 状态 Badge 色调判断用英文 `"finished"/"aborted"`，永远匹配不到中文状态 → 所有状态都是黄色，「完成」应为绿色。 |
| 11 | `attendance.tsx` L507 | 出勤天数可填 40+/月，无上限提醒（`days > 31` 时 toast 提示即可）。 |
| 12 | `contracts.tsx` L896 | InvoiceBook 税率 `useState(contract.taxRate \|\| 9)` 不随切换合同更新（编辑器重开才刷新）。 |
| 13 | `people.tsx` L622-624 | 编辑器里改名后未保存就传照片，照片挂在新名字下，与档案旧名脱节（保存后名字变了才对得上；不保存就成了孤儿照片）。 |

---

## 结论

- 🔴 4 项数据逻辑问题里，**第 1 项（工资条打款不按区间）优先级最高**——那是要给工人签字的单据；第 3、4 项同源（JS Date 溢出），各一行可修。
- 🟡 5 项交互不一致里，第 5、6 项涉及「保存口径」，建议统一。
- 整体交互完成度明显高于一般内部工具（确认、防误关、导入冲突面板齐全），上述问题多为「多路径口径不一致」而非缺失。


---

## 处理状态（2026-09-08，提交 1.6.12）

✅ 全部 13 项已修：1 打款区间过滤、2 合计分列含税/不含税、3 身份证日期溢出回读比对、4 查询起止日按当月天数 clamp（ym-pick 日选项动态）、5 扫描件上传前确认未保存改动、6 考勤单删与批量删统一立即保存、7 整本导入确认清单、8 文件内重名去重、9 工资条头历史提示、10 状态色调按中文状态、11 出勤天数>31 提示、12 切换合同重置开票表单、13 照片挂档案原名。

---

## 终审（2026-09-08，提交 958bf3e / 1.6.12）

逐项核验了 `958bf3e` 的全部 src diff，`npx tsc --noEmit` 与 `npx vite build` 均通过，四处版本号（VERSION.txt / app/VERSION.txt / package.json / README）一致为 1.6.12。

| # | 项目 | 核验结果 |
|---|---|---|
| 1 | 工资条打款区间过滤 | ✅ `buildSlips` 按起止日过滤，口径与屏幕一致（owner 或 receiver） |
| 2 | 合同合计分列含税/不含税 | ⚠️ 已分列，但新引入标签错误（见下方遗留 ①） |
| 3 | 身份证防 Date 溢出 | ✅ 实测 2月31日/平年2月29 被拒、闰年2月29 正常；18 位校验与 parse 均已修 |
| 4 | 起止日按当月天数 | ✅ ym-pick 日下拉按当月天数生成，且 value 做 clamp |
| 5 | 扫描件上传前确认 | ✅ dirtyRef + confirm，文案明示会一并保存 |
| 6 | 考勤单删立即保存 | ✅ `onSave(keep)` 传删后数组，与批删口径一致 |
| 7 | 整本导入确认清单 | ✅ confirm 列出人员/考勤/发放/报销/保单条数 |
| 8 | 文件内重名去重 | ✅ seen 集合，文件内重名归入冲突并提示 |
| 9 | 工资条头历史提示 | ✅ 有工资历史时显示「按各月生效工资（见明细）」 |
| 10 | 状态 Badge 色调 | ⚠️ 英文匹配已改中文，但 `CONTRACT_DONE` 漏了「完成」（见遗留 ②） |
| 11 | 出勤 >31 提示 | ✅ toast.warning |
| 12 | 切换合同重置开票表单 | ✅ useEffect 监听 contract.id 重置全部字段 |
| 13 | 照片挂档案原名 | ✅ 编辑时用 `person?.name`，新建时用表单名 |

### 遗留问题（建议再修一轮）

1. **合同合计行标签写反（新引入）**：`contracts.tsx` L476 显示 `含税 {money(totals.reportExcl)}`——标签是「含税」、数值却是不含税（reportExcl）。应改为 `不含税 {money(totals.reportExcl)}`；主行 L475 的 reportIncl 也建议补「含税」标签。
2. **状态 Badge 漏「完成」**：`CONTRACT_DONE` 只含 完工/结算完成/结算已开票/退质保金，而 `CONTRACT_STATUSES` 里最末态「完成」仍是黄色，建议加入集合。
3. **既存死代码（非本次回归）**：`idcard.ts` `validateIdCard` 里「15 位老身份证」分支写在 `s.length < 18 → return ""` 之后，永远不可达，15 位证实际不校验出生日期。要么把 15 位分支移到长度判断之前，要么删掉该分支。

> 结论：13 项修复全部落地，无编译/版本回归；但第 2、10 项各留一个尾巴（①②为一行级修改），修完即可收官。


---

## 终审（第二轮，2026-09-08，提交 1.6.13）

✅ 遗留 3 点全部闭环：
① 合计行标签修正（主行补「含税」标注，次行改「不含税」）；
② CONTRACT_DONE 补「完成」；
③ validateIdCard 15 位分支移到长度判断之前（此前不可达），实测 15 位老证校验生效。
