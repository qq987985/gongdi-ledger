# 工地台账 使用说明

版本 **1.1.3**。人员、考勤、工资发放、合同收款。用浏览器操作。

**数据只在 `data` 文件夹。** 软件、镜像、容器、GitHub 仓库都可以删或重装，只要 `data` 在，台账就能恢复。

仓库：`https://github.com/qq987985/gongdi-ledger`  
镜像：`ghcr.io/qq987985/gongdi-ledger:latest`  
国内拉镜像：`ghcr.1ms.run/qq987985/gongdi-ledger:latest`

---

## 0. 你手头会有哪几个包

| 压缩包 | 给谁用 |
|---|---|
| `gongdi-dual-1.1.3.zip` | 同时要 Windows 办公室电脑 + 飞牛 NAS |
| `gongdi-windows-1.1.3.zip` | 只要 Windows |
| `gongdi-nas-1.1.3.zip` | 只要飞牛，本机 Docker 构建 |
| `gongdi-github-1.1.3.zip` | 覆盖上传到 GitHub 仓库，让 Actions 自动出镜像和 Windows 包 |

Windows 的 `data` 和 NAS 的 `data` **互不相通**。要从 NAS 拷到电脑，把整份 `data` 拷过去覆盖。

---

## 1. 完整目录架构

### 1.1 双版本压缩包（解压后）

```
工地台账-1.1.3/
├── 说明.txt                 ← 先看这个（三分钟上手）
├── 使用说明.md               ← 本文件
├── 目录结构.txt              ← 只讲文件夹
├── Windows解压即用/
│   ├── 启动.bat
│   ├── 停止.bat
│   ├── VERSION.txt
│   ├── 说明.txt
│   ├── app/                  ← 程序（不要改）
│   ├── node/node.exe         ← 自带 Node，不用另装
│   └── data/                 ← 第一次点启动.bat 后自动出现
└── Docker-NAS/
    ├── Dockerfile
    ├── docker-compose.yml           ← 本机构建（推荐）
    ├── docker-compose.github.yml    ← 拉 GitHub 镜像
    ├── 一键部署.sh                   ← 本机构建并启动
    ├── 一键拉取.sh                   ← 拉镜像并启动
    ├── 初始化目录.sh                 ← 只创建 data 空目录
    ├── VERSION.txt
    ├── .dockerignore
    ├── 部署说明.txt
    ├── 使用说明.md
    ├── 目录结构.txt
    └── app/                         ← 程序（不要改）
```

### 1.2 飞牛 NAS 部署好之后（正确样子）

路径固定：`/vol1/1000/docker/attendance/`

```
/vol1/1000/docker/attendance/
├── data/                         ← 台账，永远不要删、不要解压覆盖
│   ├── accounts/
│   ├── books/
│   ├── photos/
│   │   ├── id/
│   │   ├── bank/
│   │   ├── ic/
│   │   ├── 报量单/
│   │   ├── 发票/
│   │   ├── 收款回单/
│   │   ├── 考勤影像/
│   │   └── 合同扫描件/
│   ├── backups/
│   └── templates/
├── app/
├── Dockerfile
├── docker-compose.yml
├── docker-compose.build.yml
├── docker-compose.github.yml
├── 一键部署.sh
├── 一键拉取.sh
├── 初始化目录.sh
├── VERSION.txt
├── .dockerignore
├── 部署说明.txt
├── 使用说明.md
└── 目录结构.txt
```

把 `Docker-NAS` 里的文件解压到 `attendance/`，和已有的 `data` **平级**。不要多套一层 `Docker-NAS` 文件夹，也不要解压进 `data`。

### 1.3 Windows 解压好之后

```
任意盘:\工地台账\
├── 启动.bat
├── 停止.bat
├── VERSION.txt
├── 说明.txt
├── app\
├── node\node.exe
└── data\                      ← 第一次启动后自动创建
    ├── accounts\
    ├── books\
    ├── photos\
    │   ├── id\
    │   ├── bank\
    │   ├── ic\
    │   ├── 报量单\
    │   ├── 发票\
    │   ├── 收款回单\
    │   ├── 考勤影像\
    │   └── 合同扫描件\
    ├── backups\
    └── templates\
```

整份文件夹拷到 U 盘或别的电脑也能用（64 位 Windows）。

### 1.4 GitHub 仓库应有的文件

```
qq987985/gongdi-ledger
├── .github/workflows/docker.yml    ← 网页上看不见时，用根目录 docker-workflow.yml 粘贴
├── docker-workflow.yml
├── Dockerfile                      ← COPY app，不在 NAS 上 npm 构建
├── docker-compose.yml              ← 拉镜像
├── docker-compose.build.yml        ← 本机构建
├── docker-compose.github.yml       ← 国内拉镜像
├── .dockerignore
├── .gitignore
├── VERSION.txt                     ← 第一行是版本号，例如 1.1.3
├── README.md
├── 使用说明.md
├── 目录结构.txt
├── 部署说明.txt
├── 一键部署.sh
├── 一键拉取.sh
├── 初始化目录.sh
├── win/
│   ├── 启动.bat
│   ├── 停止.bat
│   └── pack.sh
└── app/                            ← 必须完整，缺文件首页会 500
```

**不要把 `data`、`node`、压缩包传上 GitHub。**

### 1.5 data 文件夹架构（备份只拷这里）

这些目录由 `一键部署.sh`、`初始化目录.sh`、`启动.bat`、容器启动命令自动创建。你也可以手建，缺哪个补哪个。

```
data/
├── accounts/                 登录账号、密码哈希、台账名单、权限、用户状态
├── books/                    每一本台账一个子文件夹
│   └── <台账编号>/
│       ├── book.json         台账名称、编号、创建人
│       └── ledger.json       人员、考勤、发放、合同、操作记录
├── photos/                   所有照片、影像，不按台账编号分
│   ├── id/                   身份证正反面    张三-身份证-正面.jpg / 张三-身份证-反面.jpg
│   ├── bank/                 银行卡    张三-银行卡.png
│   ├── ic/                   IC卡      张三-IC卡.jpg
│   ├── 报量单/
│   ├── 发票/
│   ├── 收款回单/
│   ├── 考勤影像/
│   └── 合同扫描件/
├── backups/                  软件里导出的 Excel 备份
└── templates/                导入模板（可把软件自带模板拷到这里）
```

**必须创建的空目录（共 12 个）：**

```
data
data/accounts
data/books
data/backups
data/templates
data/photos
data/photos/id
data/photos/bank
data/photos/ic
data/photos/报量单
data/photos/发票
data/photos/收款回单
data/photos/考勤影像
data/photos/合同扫描件
```

说明：

- `books` 下面的编号文件夹不用手建，新建台账时软件会建。
- 照片不在 `books` 里，在 `data/photos`。
- **不要**在 `data` 里放 `VERSION.txt`。旧版 compose 曾把版本文件挂进 data，文件不存在时 Docker 会建成**文件夹**，打开首页会 500。脚本会自动删掉这个名字。
- 不要往 `data` 里解压软件包。

---

## 2. 飞牛 NAS：docker compose 部署

推荐一直用**本机构建**。GitHub 拉镜像是备选：仓库传完整、Actions 全绿、Packages 设成 Public 之后再用。

### 2.1 本机构建（推荐，和现在好用的 1.1.3 一样）

1. 把 `Docker-NAS`（或 NAS 安装包）解压到 `/vol1/1000/docker/attendance/`
2. 确认 `data` 还在
3. SSH：

```bash
cd /vol1/1000/docker/attendance
chmod +x 一键部署.sh 一键拉取.sh 初始化目录.sh
./一键部署.sh
```

等价手打：

```bash
cd /vol1/1000/docker/attendance
docker compose down --timeout 8
docker rm -f attendance-app
docker compose up -d --build --force-recreate
```

`docker-compose.yml` 要点：

- `build: .` 本机构建
- `image: gongdi-ledger:1.1.3`
- 端口 `8501:8080`
- **只挂** `/vol1/1000/docker/attendance/data:/data`
- **不要**再挂 `VERSION.txt`

打开：`http://192.168.0.222:8501`（改成你的 NAS IP）  
左下角应显示 **1.1.3**。用原来的账号登录。

### 2.2 拉 GitHub 镜像（Actions 成功并且 Packages 为 Public 之后）

```bash
cd /vol1/1000/docker/attendance
./一键拉取.sh
```

等价手打：

```bash
docker compose -f docker-compose.github.yml pull
docker compose -f docker-compose.github.yml up -d
```

国内镜像名：

```yaml
image: ghcr.1ms.run/qq987985/gongdi-ledger:latest
```

官方：

```yaml
image: ghcr.io/qq987985/gongdi-ledger:latest
```

`ghcr.io` 在国内常 EOF。`ghcr.1ms.run` 若 403，到 GitHub → Packages → `gongdi-ledger` → Package settings → 改成 **Public**。仓库 Public 不等于 Packages Public。

### 2.3 反代

反代到公网必须走登录，不要裸奔。

---

## 3. Windows 办公室电脑

1. 解压 `Windows解压即用` 或 `gongdi-windows-1.1.3.zip`
2. 双击 `启动.bat`
3. 浏览器打开 http://127.0.0.1:8501
4. 第一次创建管理员账号
5. 关掉黑窗口，或双击 `停止.bat`

防火墙询问时选允许。需要 64 位 Windows，不用单独装 Node。

升级：左下角版本号或「设置 → 软件更新」→ 检查更新 → 更新。只换程序，不覆盖 `data`。  
更新包来自 GitHub Releases 里的 `gongdi-windows.zip`。

---

## 4. 怎么发布到 GitHub

仓库已有：`qq987985/gongdi-ledger`。把 `gongdi-github-1.1.3.zip` 解压后的文件覆盖到仓库根目录。

**网页一次最多传 100 个文件。** 本包超过 100 个，请用下面任一方式：

- GitHub Desktop / `git add` + `git push`
- 先传整个 `app/` 并 Commit，再传其余文件再 Commit
- `.github` 在 Windows / 飞牛解压后经常看不见。打开仓库 → Actions → 新建工作流，把根目录 `docker-workflow.yml` 全文贴进 `.github/workflows/docker.yml`

推到 `main` 后 Actions 自动做两件事：

1. 打 Docker 镜像 `ghcr.io/qq987985/gongdi-ledger:latest`
2. 打 Windows 包，放到 Releases，文件名 `gongdi-windows.zip`

你每次更新只要：

1. 覆盖完整 `app/`
2. 改 `VERSION.txt` 第一行（修问题 `1.1.3`→`1.1.4`，加功能 `1.2.0`）
3. 推 `main`

YAML 不用每次改。

版本号规则：修问题改第三位，加功能改第二位，大改改第一位。左下角点版本号看最近 10 条说明。

---

## 5. 账号与权限

- 第一个注册的是管理员
- 管理员可创建、停用、删除账号，改登录名、显示名、密码
- 普通账号可以不自建台账，使用别人共享的台账
- 管理员和台账创建人按模块授权：人员、考勤、发放、合同、照片、查询、设置等
- 管理员能看、改所有台账
- 操作记录：全员可看自己相关的；只有管理员能改记录

忘记管理员密码：不要删 `data`。用备份的 `data/accounts` 恢复。

---

## 6. 功能模块

**人员**  
姓名、班组、日工资（可 0）、加班规则必填才能算工资。加班：按小时 / 按工天折算 / 月薪。  
身份证填了会自动性别、年龄。有效期、签发机关可手填。人员编号即 IC 卡号。照片在人员里上传，自动存到对应目录并命名。可扫描文件夹匹配已有照片。

**月度考勤**  
按年按月：出勤天数、加班小时、补助、扣款。可上传考勤影像。导入时会问哪年哪月。重复行提示跳过或覆盖。年度工资汇总只显示该年有出勤的人。可新增年、删除年（只删该年考勤）。

**发放记录**  
实际收款人才是入账人，可以替别人收。没有发放日期也可以导入（已上报未打款），以后批量补日期。可按时间段查询，可多选删除。

**个人查询**  
按一个月，或从某年某月到某年某月。可打印工资条，可选是否附打款记录。信息可一键复制。

**合同**  
报量（含税/不含税）、开票（税率、电子发票）、收款（总金额、总包代付农民工、到分包公司）。  
按合同收 = 月报量含税 × 合同比例。  
剩余款 = 开票含税 − 已付。  
影像：报量单、发票、收款回单、合同扫描件。有扫描件=有合同，没传=无合同，原因写备注。可打印对账单。

**照片 / 影像**  
点击放大、复制。可筛选人名导出 Word（身份证标准尺寸、正反面分开）。

**导入导出 / 设置**  
各模块有模板。导入智能去重。设置里可备份 Excel、管账号权限。清空功能慎用。

---

## 7. 照片文件名

放到 `data/photos/` 对应子目录，或在人员页上传。jpg / png 均可。

```
张三-身份证-正面.jpg
张三-身份证-反面.jpg
张三-银行卡.png
张三-IC卡.jpg
```

人员页只显示正面，边上点「查看反面」。旧文件 `张三-身份证.jpg` 仍能匹配到正面。

合同扫描件放到 `data/photos/合同扫描件/`，文件名是「项目名称-合同电子版」（如 `某某住宅-合同电子版.pdf`）。有文件=有合同，没传=无合同，原因写在备注。

也可先拷进目录，再在人员里点扫描。

合同影像建议：

```
项目名-开票月份-金额
项目名报量-月份-金额
项目名-月份          （收款）
考勤-考勤月份
```

---

## 8. 以后怎么更新

| 环境 | 怎么更新 | 会不会动 data |
|---|---|---|
| 飞牛本机构建 | 覆盖 `app`、Dockerfile、compose、脚本、VERSION.txt，再 `./一键部署.sh` | 不会 |
| 飞牛拉镜像 | GitHub 发布成功后 `./一键拉取.sh` | 不会 |
| Windows | 软件内点更新，或解压新 Windows 包但留下旧 `data` | 不会 |

永远只覆盖软件文件，永远不要删 `data`。

---

## 9. 常见问题

**打开变成 `{"status":500,"unhandled":true,"message":"HTTPError"}`**  
1.0.0 / 1.0.1 拉镜像或打包不完整会这样。用 1.1.3 本机构建。若 `data/VERSION.txt` 是文件夹，脚本会删掉这个名字。

**网页一直转圈**  
`docker rm -f attendance-app` 再 `./一键部署.sh`。不要删 data。

**照片没有**  
文件名要带姓名和类型。扫一次。照片在 `data/photos/`，不在 `books`。

**国内 pull ghcr.io 失败 EOF / 403**  
改用 `ghcr.1ms.run/qq987985/gongdi-ledger:latest`，并把 Packages 设为 Public。不行就继续本机构建。

**GitHub 网页拖文件没反应**  
一次超过 100 个文件。分两次 Commit，或用 Git 客户端。

**Windows 点更新没反应**  
Releases 里要先有比本机新的 `gongdi-windows.zip`。NAS 没有「点更新」，用脚本重建或拉取。

**解压后多了一层文件夹，一键部署说缺少 app**  
文件必须和 `data` 平级。把里层文件移到 `attendance/`。

管理员账号自己记牢。备份只拷 `data`。建议飞牛每天凌晨把整个 `data` 同步到网盘，不必停容器。
