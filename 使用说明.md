# 工地台账 1.0.1

数据只在 data。软件、镜像、容器都能删，data 在就能恢复。

## Windows
解压 Releases 里的 gongdi-windows.zip，双击 启动.bat，打开 http://127.0.0.1:8501

## NAS
/vol1/1000/docker/attendance/data 不要删。
image: ghcr.1ms.run/qq987985/gongdi-ledger:latest
docker compose pull && docker compose up -d
打开 http://NAS的IP:8501

## 更新
GitHub 覆盖 app/ 并改 VERSION.txt 第一行。Actions 自动出 Docker 和 Windows 两份。
NAS：compose pull。Windows：软件内点更新。
