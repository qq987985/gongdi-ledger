# 收官体检报告 · 2026-09-08

> 范围：前两轮报告（代码复审 + UI与数据逻辑）**之外**的全部源码——`settings / export / files / photos / doc-actions / photo-slot / file-pick / pay-fields / xlsx-center / shell` 剩余段、全部 `api/*` 路由、`nas-fs.server` 路径安全、`accounts.server` 口令策略。基于提交 `513203c`（1.6.13）。
> 结论先行：**发现 1 个建议修的中等问题（批量工资追溯改历史 + 无确认），3 个低优先级小项**；安全面（鉴权、注入、路径穿越、XSS）全部通过。

---

## 🟡 建议修复（1 项）

### 1. 设置页「批量工资」一键改写全员，无确认、不写工资历史
- **文件**：`src/routes/settings.tsx` `BatchRules.apply`（L943-966）、按钮区（L1032-1040）
- **问题**：
  - 「应用到所有人」一点就把**所有人**的计薪方式 / 工资 / 加班规则改掉，没有任何确认弹窗——这是全库影响面最大的一个按钮。
  - 更深一层：批量改的是当前工资字段，**不写 `wageHistory`**。`getWageAt`（`src/lib/wage.ts` L90-97）在没有历史记录时回退到当前工资 → 已经过去的月份会按新工资**追溯重算**，历史工资条、查询页数字全变。人员编辑器里工资历史靠手动维护，批量入口连「会影响历史月份」的提示都没有。
- **修复建议**：
  1. 「应用到所有人」加 `confirm`（写明影响人数）；
  2. apply 前提示「只改当前工资，历史月份会按新工资重算；要保留历史请先在人员页录调薪历史」，或给 apply 加「同时记录调薪历史（生效日=今天）」选项。

---

## 🟢 低优先级小项（3 项）

| # | 位置 | 问题 |
|---|---|---|
| 2 | `src/routes/export.tsx` L91-96 | 按区间导出时不校验 起 ≤ 止（查询页有 swapped 检测，导出页没有）；选反了会静默导出空表。 |
| 3 | `src/routes/api/file/$kind.ts` L165 | 模板年份缺省值硬编码 `2026`（`Number(... || "2026") || 2026`），跨年不跟进。 |
| 4 | `src/routes/files.tsx` L140 | 搜索用 `x.includes(s)`：类型上均为 string，但旧数据若缺 `remark` 字段会在搜索时抛错；建议 `(x || "").includes(s)` 防御。另：照片走文件选择器上传无确认（`photo-slot.tsx` onFile），拖拽上传却有确认（`file-pick.tsx` confirmDrop），口径可统一。 |

---

## ✅ 本轮专项通过项

- **API 鉴权**：`api/*` 全部经 `withTenant` + 细分权限（doc/photo/ledger/file/audit/year/backup/update），导出含敏感字段要求 `export.use`，台账拉取要求 `people.view`，更新要求 admin。
- **路径穿越**：`saveDoc` 剔除 `/\`，`safeName / safeId / safeBookId` 剔除 Windows 保留字符，`savePhoto` MIME 白名单（png/webp/bmp/jpg）。
- **XSS**：唯一 `innerHTML`（doc-actions 同名文件对话框）经 `escapeHtml` 五字符转义。
- **口令策略**：服务端强制 ≥8 位（setup / 改密 / 建新用户），不只靠前端。
- **文件上传**：doc 50MB、照片 20MB 双重限制（content-length + 实际大小）。
- **shell.tsx**：门禁状态机（boot/setup/login/nobook/app）无越权路径；切换台账后重新拉权限与数据。

---

## 总结

到这里，代码库三条主线（复审报告 → UI/数据逻辑报告 → 本次收官体检）全部扫完。修掉上面第 1 项，这套系统在我这里就没有已知待办问题了。


---

## 处理状态（2026-09-08，提交 1.6.14）

✅ 全部闭环：
① 批量工资：三处按钮均加确认（写明人数/改动）；新增「记为工资历史（今天生效）」默认勾选，勾选后写入 wageHistory（替换今天记录），过去月份不追溯；未勾选时提示写明影响。
② 区间导出：起止选反自动交换并提示。
③ 模板年份缺省改当前年。
④ 影像搜索 `String(x||"")` 防御；照片选择器为显式操作保持无确认（拖拽已有确认），口径差异已注明为取舍。
